## 1. Red phase：以失敗測試重現缺陷

- [x] 1.1 在 `scripts/cash-skills/tests/test_installer_runtime.py` 新增 design IC-15 對照表列出的全部十三個案例，順序與該表一致；跨兩個 gate 的案例拆成 launcher 與 installer 兩個測試函式，使 task 2.1 與 2.2 各自的完成時點都有可達的驗收。其中「Source repository 提示優先」需要一個本 task 新增的 helper：`make_self_source` 會同時移除 receipt 與 manifest，而 source repository 無法以 `--init-receipt` 或 direct install 產生 receipt，因此該案例必須以 helper 合成一份含兩筆 stable、全部 runtime 與 24 個 skill records 的合法 receipt。驗收：IC-15 表中標記為 red 的案例在未修改 launcher 與 installer 的狀態下全部失敗，且每個失敗的斷言訊息指向該案例的目標行為（device 比對被拒／訊息分類、指令或版控前提缺失／缺少範圍閘門／錯誤碼不符），而非共用 exit code 或缺少 fixture 等不相關表象；標記為 guard 的案例在同一狀態下通過（實作後是否仍通過由 task 5.1 與 5.3 驗證，不屬本 task 的完成判準）。

## 2. Green phase：兩個 receipt gate

- [x] 2.1 修改 `.cash-skills/bin/cash` 的 receipt gate：依 IC-1 讓 stable record 的通過條件恰為 digest、mode 與 inode 三項相等，比較式不得包含 record 的 device 欄位；依 IC-2 補上範圍判準並以形狀專屬訊息 `receipt identity is invalid` 失敗；依 IC-3 以 digest 為分類軸；依 IC-4 讓 content drift 立即 fail closed、identity drift 延後到逐檔 runtime records 驗證完成後才決定回報內容，延後期間命中既有出口時以該出口回報，且 MUST NOT 為此新增 skill 檔案的 digest 比對、MUST NOT 把 runtime generation 納入前提，並在 receipt gate 內對 runtime records 取 digest 時傳入 `error_code="receipt_invalid"`；依 IC-5 設定三支訊息、只在既有 `FAILURE_HINT` 為空字串時設定 identity hint、且只在最終確定回報 identity drift 時才設定該 hint；依 IC-6 使用指定的 hint 文字，並使 canonical 版控前提 fragment 完整落在單一原始碼行（可在 `; ` 邊界拆行、不得在 fragment 內部拆），否則 IC-13 的 parity 斷言會失敗。驗收：task 1.1 中屬於 launcher 的測試函式全部轉綠。
- [x] 2.2 修改 `.cash-skills/lib/cash_cli/installer.py` 的 `validate_installed_receipt`：依 IC-7 讓 stable 分支的通過條件恰為 digest、mode 與 snapshot inode 三項相等且不比較 snapshot device，函式簽章、非 stable record 的 conflicts 累積行為與 receipt path mismatch 檢查皆不變；依 IC-3 與 IC-4 套用相同分類與 installer 面的指引前提（涵蓋 runtime 與 skill records，不含 runtime generation）；依 IC-8 產生三支訊息，identity 訊息在指令以外的散文部分指名該 target 的 resolved 絕對路徑且不自加前綴、指令文字採 IC-8 釘死的字串（只含 project-relative path、不內嵌絕對路徑、不使用 launcher 版本的措辭）、第三支比照 IC-5 接上共用的下一步句且其前段同樣指名該 target；並使 canonical 版控前提 fragment 完整落在單一原始碼行。此契約同時適用於 `install_target` 與 `install_vendored_target` 兩個呼叫點，且不得依賴任一路徑的版控查詢結果。驗收：task 1.1 中屬於 installer 的測試函式全部轉綠，含以 `--vendor` 路徑重跑的版控前提斷言。
- [x] 2.3 依 IC-9 修改 `installer.py` 的 version-control diagnostic 字串，只以 inode 描述 fail-closed 的成因；該 diagnostic 的唯讀性、查詢失敗時靜默略過與不改變分類的行為不變，且不得成為 identity 指引的前置條件。驗收：`test_version_controlled_receipt_is_reported_without_index_changes` 仍全綠，且該字串不再出現 device。
- [x] 2.4 依 IC-10 驗證未受影響的面。驗收：以 `git diff` 證明 `rebind_receipt_stable_identity`、receipt 組裝與發佈路徑未被修改，並執行 `test_invalid_receipt_is_execution_error_not_domain_result`、`test_managed_mode_drift_is_conflict_without_force` 與 `test_launcher_transition_and_journal_v3_contract_surface_is_explicit` 確認全綠。

## 3. Bundle 版本與 launcher transition 治理

- [x] 3.1 依 IC-11 完成版本與 transition 登錄：`cash-skills.version` 內容改為 2.13.0，`installer.py` 的 `BUNDLE_VERSION` 同步為相同字串，`APPROVED_LAUNCHER_TRANSITIONS` 在保留既有那一筆的前提下追加兩筆，new digest 均為 task 2.1 定案後 `.cash-skills/bin/cash` 的實際 SHA-256、introduced version 均為 2.13.0，old digest 分別為 `76fe6dd649b1df558ef374d055ca2c5fe4c40a9200b32a1da1d41ca631c3d52f` 與 `592345fffa009998d48008857ad903d89b0e5f0986d141a5fec26368b527c8a4`。此 task 與 task 2.1 的 launcher 編輯必須落在同一個 commit。驗收：兩筆新 entry 的 new digest 與工作樹中 launcher 的實際 digest 相符。
- [x] 3.2 在 task 2.1、2.2、2.3 與 3.1 全部完成之後，以 install-cash-skills.fish 的 --self 模式重新發佈 `.cash-skills/manifest.tsv`。此順序是必要的：manifest 同時記錄 bundle version、runtime generation 與每一筆 record 的 digest，而 `installer.py` 本身是 runtime record，中途發佈會在後續編輯時再度失效。manifest gate 的驗證迴圈走全部 records，而 `.cash-skills/bin/cash`（stable）與 `.cash-skills/lib/cash_cli/installer.py`（runtime）都在其中，因此 task 2.1、2.2、2.3 之中**任何一個先動**，本 repo 的 `.cash-skills/bin/cash` 就會以 `manifest_invalid` 不可用，直到本 task 完成為止；該窗口涵蓋 2.1、2.2、2.3 與 3.1，無法靠調換順序縮短。期間只能以 install-cash-skills.fish 或 `PYTHONPATH=.cash-skills/lib python3 -m cash_cli.main` 操作，cash-apply 的 per-task CLI 記錄需延後到本 task 完成後補上。驗收：manifest 的 bundle_version 為 2.13.0、runtime_generation 與 launcher digest 與工作樹一致，且 `.cash-skills/bin/cash list --json` 恢復可用。
- [x] 3.3 執行 bundle 版本與 launcher history contract test。驗收：`scripts/cash-skills/tests/test_bundle_version_history.py` 全綠，且 history gate 認定 new launcher bytes 的 introduced version 恰為 2.13.0，追加的 skip transition 不被視為重複或非法。

## 4. Guidance 與文件同步

- [x] 4.1 依 IC-12 更新 `AGENTS.md` 與 `CLAUDE.md` 的 CASH 受管區塊，載明四件事：manifest 缺失的 receipt-based target 出現 stable record identity drift 的 `receipt_invalid` 時可由 `--init-receipt` 復原、content drift 不得以重新簽發處理、`.cash-skills/receipt.tsv` 被納入版控時須先解除追蹤再重新簽發；以及 identity drift 這個入口只在診斷「僅」指名該 stable record 時適用、診斷同時指名 `runtime` 或 `skill` record drift 時改為指示還原該 record 或從可信 source 重新安裝而非重新簽發；且不得把 fresh clone 或任何取得方式陳述為無條件可以重新簽發的理由。驗收：兩份檔案的受管區塊逐 byte 相同，`skill-checks.fish` 既有斷言的全部 literal 仍存在，且區塊內不出現 `fresh clone` 字樣；另以第三支診斷的實際字串（launcher 的 `stable record identity drift: <stable>; runtime record drift: <path>. …`）逐字核對受管區塊，確認依該區塊判斷的下一步是還原或重新安裝，而非 `--init-receipt`。
- [x] 4.2 依 IC-14 更新 `CASH-SKILLS.md`：說明 receipt 的 stable device 欄位不參與 identity 比對、只作為 machine-local provenance 並受形狀閘門約束，identity 比對條件為 digest、mode 與 inode 三項，版控保護的鑑別力由 `st_ino` 承擔；並把既有的「對已 drift 的 receipt-only target 重跑 init 會把該 drift 合法化」限定為 content drift，指出 identity drift 是允許重新簽發的入口。驗收：`skill-checks.fish` 對該文件既有斷言的全部 literal 仍存在，且該句不再是無限定的全稱敘述。
- [x] 4.3 依 IC-14 更新 `CASH-INIT-RECEIPT.md`：在「何時需要初始化」的說明中加入 stable record identity drift 這個入口，並說明 content drift 不適用、前提不成立時的下一步、以及版控前提。驗收：`skill-checks.fish` 對該文件既有斷言的全部 literal 仍存在。
- [x] 4.4 依 IC-13 更新 `scripts/cash-skills/tests/skill-checks.fish`：為 task 4.1 的四件事各補一條 `assert_contains` literal 斷言，與既有 literal 清單並列；另補一條斷言證明 IC-6 的 canonical 版控前提 fragment 逐字同時出現於 `.cash-skills/bin/cash` 與 `.cash-skills/lib/cash_cli/installer.py`；再把 canonical Cash guidance baseline SHA-256 更新為新受管區塊的實際 digest。baseline 必須在 task 4.1 文字完全定案之後才計算。驗收：`fish scripts/cash-skills/tests/skill-checks.fish` 的 guidance 群組全綠；且刻意刪掉四條新 literal 中任一條**並同步重算 baseline digest** 後該套件仍以非零結束——不重算 digest 的話 baseline 斷言必然先失敗，無法證明 `assert_contains` 真的生效。

## 5. 整合驗證

- [x] 5.1 執行 `fish scripts/cash-skills/tests/skill-checks.fish`、`fish scripts/cash-skills/tests/skill-checks.fish namespace-scan`、`fish scripts/cash-cli/tests/cli-checks.fish` 與 `.cash-skills/bin/cash validate --all`。驗收：四者全綠。
- [x] 5.2 端到端驗證修復路徑：建立一個 fixture receipt-based target，把其 receipt 兩筆 stable records 的 device 欄位改成與現地不同的合法非負整數，確認該 target 的 launcher 可正常執行；再從本 repo 對它依序執行三項並全部成立——direct（`--target`）dry-run 不以 stable receipt drift 失敗、`--vendor` dry-run 同樣不失敗、以及一次 `--vendor` real run 完成遷移並回報既有分類（前兩項覆蓋 scenario 的第一條 THEN，第三項覆蓋第二條 THEN「安裝或遷移依既有分類正常完成」，dry-run 通過 preflight 不足以覆蓋它）；另建立一個 launcher 停在 `592345fff…` 的 fixture target，確認它能升級到 2.13.0 而非以 approved transition 缺失失敗。該 fixture MUST 以「從 first-parent history 取出該 launcher digest 所屬 commit 的 bundle source，先用它安裝出 lagging target，再從新 source 對它執行」的方式建構；MUST NOT 以「先用現行 source 安裝再覆寫 launcher bytes」建構——那會使 receipt 的 stable digest 與現地不符，preflight 先以 content drift raise，根本走不到 launcher transition 的判定。驗收：上述三項與 lagging target 的升級皆成立，且 fixture 目錄之外零寫入（`--vendor` real run 會寫入該 fixture target，屬預期）。
- [x] 5.3 逐項核對 design IC-15 的對照表：新增 requirement 的十三個 scenario 各自對應到 task 1.1 中已通過的測試函式，跨 gate 的案例對應到兩個函式，且 red／guard 標記與該案例在實作前的實際結果相符。驗收：對照表無未覆蓋項，每一項指向具體的測試函式名稱，且沒有任何 guard 案例被當成 red evidence。
