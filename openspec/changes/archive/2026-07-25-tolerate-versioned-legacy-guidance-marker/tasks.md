## 1. 測試先行

本節全部任務都改同一個檔案 `scripts/cash-skills/tests/test_installer_runtime.py`，因此沒有任務標記 `[P]`。

red-first 規則與其三個例外：本節每一項都必須先確認因當前實作而失敗，例外有三個：1.3、1.6 前者與 1.7。三者的對照組都不是現行實作，因此在現行實作下皆綠，作用是鎖住某個性質不被錯誤實作破壞。1.3 是回歸鎖，對照組是「未導入字尾容忍的實作」。1.6 前者的對照組是「一個只排除 `>` 而未排除 `<` 的假想錯誤實作」；若要一次性反證其價值，可用 tasks 3.3 的對照組手法建一個只排除 `>` 的 pattern 跑一次。1.7 的對照組是「完成 2.1 但未完成 2.3 的中間實作」——現行實作對 source 帶字尾的三種形狀本來就都以非零結束（單側帶字尾落入計數不相等，兩側皆帶字尾則 `canonical_guidance` 拋 source guidance has no Cash block），任何 fixture 形狀都無法讓 1.7 的存活斷言在修復前變紅；真正在修復前為紅的是 1.6 後者對 1.7 全部 case 追加的診斷斷言。其餘各項若寫出來在修復前就是綠的，表示 fixture 的形狀不對，見 1.5 的說明。

全部成功情境的橫向斷言：本節每一個成功情境都必須附帶「managed span 以外的 bytes 逐 byte 不變」與「檔案 mode 不變」兩項斷言。此為 Implementation Contract IC6 的橫向要求，由本前言單一承接，各 task 不逐項重述。

scenario 承接對應：`## MODIFIED Requirements` 會整塊取代 **Cash guidance deployment**，因此該 requirement 原有的三個 scenario 一併出現在 delta 中。**Missing、Spectra-only 與 mixed guidance 收斂** 由 1.1 與 1.2 承接——只有它們逐條斷言了收斂結果（恰好一個 canonical Cash block、legacy block 被移除、span 外 bytes 與 mode 不變）。**Guidance marker malformed** 由 1.5 承接。**Guidance snapshot 與 parent identity 綁定** 逐字保留且不受本次改動影響——它治理的是 preflight snapshot 與 parent identity 的重新驗證，與 marker 形式無關——故本 change 不為它新增任務，這是刻意而非缺漏。

- [x] 1.1 新增 **帶字尾的 marker 被辨識並收斂** 與 **Missing、Spectra-only 與 mixed guidance 收斂** 的 mixed 分支測試，建兩個 fixture 以避免形狀互斥。fixture A 的 `AGENTS.md` 與 `CLAUDE.md` 首行即 `<!-- SPECTRA:START v1.0.2 -->`，對應 `##### Example: 帶版本字尾的 legacy start marker` 的形狀；fixture B 在 legacy block 之前另有一段 project-owned 文字，用以涵蓋 span 前側 bytes 的保留。兩者都在 legacy block 之後放一個合法但非 canonical 內容的 Cash block，並在 Cash block 之後再放一段 project-owned 文字。執行 installer 後對兩者皆斷言：exit code 為 0；檔案不再含任何 `SPECTRA:` marker；檔案恰含一個 Cash block 且其內容等於 source 的 canonical block。此測試在修復前必須因 marker 判定而失敗。

- [x] 1.2 新增 **帶字尾的 marker 被辨識並收斂** 與 **Missing、Spectra-only 與 mixed guidance 收斂** 的 legacy-only 分支測試：target guidance 只含 legacy block、沒有 Cash block，且該 legacy block 取「START 帶字尾、END 不帶字尾」——貼合語料與 `##### Example: 帶版本字尾的 legacy start marker` 的形狀。斷言必須是排他形式：exit code 為 0、結果檔案不含任何 `SPECTRA:` marker、且恰含一個 Cash block 其內容等於 canonical block。理由同 1.4 case 二：若取兩側皆帶字尾並用包含式斷言，現行實作對該輸入是計數 0 對 0、於檔尾附加 canonical block 而 exit 0，兩條斷言在修復前都成立、測試不會 red。此測試覆蓋 `render_guidance` 中 legacy 存在而 Cash 不存在的遷移分支。

- [x] 1.3 新增 **字尾容忍不改變獨立成行無字尾 marker 的 span** 的回歸測試：target guidance 中每個名稱的全部 marker 都獨立成行且都不帶字尾。斷言處理結果與字尾容忍導入前完全相同。此為 red-first 的第一個例外，屬回歸鎖。此 fixture 同時是 Implementation Contract IC2 的可重跑驗收基準，取代對本機特定檔案 offset 的一次性斷言。

- [x] 1.4 新增 **帶字尾的 marker 被辨識並收斂** 剩餘兩側的覆蓋，以驗證 delta 中「字尾對 `CASH` 與 `SPECTRA` 兩個名稱、`START` 與 `END` 兩種種類一致適用」這條 MUST。四個 name 與種類的組合必須全部有案例：`SPECTRA:START` 由 1.1 涵蓋；case 一為 target 的 legacy `SPECTRA:END` 帶字尾，斷言該 legacy block 仍被正確定位並移除；case 二對應 `##### Example: 帶字尾的 Cash marker 自我修復`，同一 fixture 內 `CASH:START` 與 `CASH:END` 兩者皆帶字尾，斷言檔案恰含一個 Cash block、其內容等於 canonical block、且不含任何帶字尾的 `CASH:` marker。斷言必須是排他形式：現行實作對兩端皆帶字尾的 block 是計數 0 對 0、回傳無 span、於檔尾附加 canonical block 而 exit 0，因此若只斷言「含 canonical block」與「exit 0」，兩條在修復前都成立、測試不會 red——1.1 已用「恰含一個 Cash block」堵住同型陷阱，此處沿用。若只覆蓋三個組合，實作者對未覆蓋的那一側沿用字面比對時全部測試仍會綠，而該 MUST 已被違反。另在 1.1 的 fixture A 上加一個廉價斷言以驗證 IC1 的「字尾不被解析」：把字尾由 `v1.0.2` 換成另一段任意不含 `<` 與 `>` 的內容重跑一次，斷言輸出 bytes 完全相同——只用單一字尾無法區分「略過字尾」與「認得 v1.0.2」。

- [x] 1.5 新增 **帶字尾 marker 違反判定仍 fail closed** 的測試，涵蓋五種形式各一個 case。每個 case 的 fixture 形狀必須逐字寫死，否則測試在修復前就是綠的：現行實作對「只有單側帶字尾」的輸入必然造成計數不相等而已經 fail closed，只有兩側都帶字尾、或完全沒有對側時，計數才回到 0 對 0、`marker_span` 直接回傳無 span、installer 照常成功。因此反序、尾側非獨立行、巢狀三個 case 的 fixture MUST 讓該名稱的 start 與 end 兩者皆帶字尾；重複 case MUST 放兩個帶字尾的 start 且不放任何對側 end；孤立 case 放一個帶字尾的 start 且無對側。非獨立行的 case 必須寫成尾側形式：行首錨定不在本次範圍，marker 之前同行有普通文字時實作會接受，不得為此新增斷言。每個 case 斷言 installer 以非零結束、在首次 target write 之前失敗（target 的 runtime、skills 與 receipt 皆未被建立或修改），且加上 `--force` 仍然失敗；並斷言 red-first 成立——修復前該 fixture 使 installer 以 exit 0 結束，修復後以非零結束。此任務同時是 **Guidance marker malformed** 在字尾容忍導入後仍然成立的驗證。

- [x] 1.6 新增 **字尾不跨越註解界定符** 與 **全部 marker 失敗診斷具名檔案** 的測試。前者的 fixture 其某一行讓 `<!-- CASH:START` 出現兩次而只由行尾單一個結尾符號收束，斷言 span 起點落在合法 marker 處而非行首、且該行稍前的 bytes 在遷移後仍逐 byte 存在——若字尾只排除 `>` 而未排除 `<`，那段 bytes 會被捲入 managed span 並在替換時刪除，且因尾側緊接換行而不會被任何既有判定攔下。此為 red-first 的第二個例外，見本節前言。後者對 1.5、1.7 與 1.8 的全部失敗 case 斷言診斷包含出問題的 guidance 檔案的 project-root 相對路徑，涵蓋 `marker_span` 的三個既有例外在 target 側（1.5）與 source 側（1.8）、`render_guidance` 的巢狀例外與 2.3 新增的 source 字尾例外；並斷言 source 側 case 的訊息含 source 限定詞而 target 側 case 不含——兩側的相對路徑取值相同，只斷言路徑子字串會讓兩側同時通過而發現不了消歧缺失。

- [x] 1.7 新增 **Source canonical marker 不得帶字尾** 的測試，兩個 case：source 的 `AGENTS.md` 其 `CASH:START` 帶字尾、以及其 `CASH:END` 帶字尾。兩者各斷言 installer 在首次 target write 前以非零結束，且沒有任何 target 檔案被建立或修改。兩個 case 都必要：delta 與 IC5 都要求兩側，只測 start 會讓「只對 `CASH:START` 加檢查」的實作通過全部斷言。source 可在既有 harness 產生的暫存 bundle 副本上構造，不需改動 repo 自身任何檔案。此測試鎖住 D3 記載的散播風險——若無此鎖，帶字尾的 source marker 會被逐 byte 寫入每一個 registered target，是本次改動中影響面最大的失敗模式。

- [x] 1.8 新增 **全部 marker 失敗診斷具名檔案** 在 source 側由 `marker_span` 觸發的 case，對應 `##### Example: source 側的 marker 失敗具名`。1.5 的五個 case 全部在 target 側，1.7 的兩個 case 打到的是 2.3 新增的 IC5 例外而非 `marker_span`，因此在本 task 之前沒有任何情境會讓 `marker_span` 在 source guidance 上拋例外——IC4 最核心的那條「`canonical_guidance` MUST 傳入帶 source 限定詞的標籤」因而無可執行驗證：實作者只要在 2.3 的新訊息裡寫死 source 限定詞、而 `canonical_guidance` 呼叫 `marker_span` 時照舊傳相對路徑，全部既有斷言仍會綠而 IC4 已被違反。fixture：source 的 `AGENTS.md` 含兩個 `CASH:START`（或任一違反非獨立行、反序的形狀），斷言 installer 以非零結束、沒有任何 target 檔案被建立或修改、且診斷同時含 source 限定詞與相對路徑。source 可在既有 harness 產生的暫存 bundle 副本上構造，不需改動 repo 自身檔案。

## 2. 實作

- [x] 2.1 在 `.cash-skills/lib/cash_cli/installer.py` 的 `marker_span` 實作 **Cash guidance deployment** 新增的 marker 可接受形式：start 與 end 的定位由字面 bytes 的 `count` 與 `index` 改為對「`<!-- ` 加 name 加 `:` 加種類，其後可選一段以單一空白起始、不含 `<`、`>`、CR 與 LF 的字尾，再接 ` -->`」的掃描。字尾必須同時排除 `<` 與 `>`：排除 `>` 擋住向後吞噬，排除 `<` 擋住向前吞噬同行稍前的另一個註解起始序列。字尾對 `START` 與 `END` 一致適用。三層防護的判定改為建立在掃描結果上：出現次數取代 `count(start)`，「匹配結尾緊接換行」的次數取代 `count(start + b"\n")`，第一個匹配的起始位置取代 `index`。四個既有判定的觸發條件不變，span 結尾仍延伸至 end marker 所在行的行尾之後。字尾內容不得被解析、比較或用於任何回傳值。

- [x] 2.2 在 `.cash-skills/lib/cash_cli/installer.py` 實作 Implementation Contract IC4：`marker_span` 新增一個能唯一識別該 guidance 的標籤引數，其全部例外訊息附上該標籤。標籤不得只是相對路徑——`canonical_guidance` 與 `render_guidance` 的 `relative` 取值是同一組 `AGENTS.md` 與 `CLAUDE.md`，只傳路徑會使兩側的失敗訊息逐字相同。`canonical_guidance` 傳入帶 source 限定詞的標籤，沿用該函式既有兩個 source 專用例外的消歧慣例；`render_guidance` 傳入 target 側的相對路徑。各既有例外維持既有語意，僅附加該標籤。不得新增任何其他診斷分支——「形式不被辨識」的獨立診斷已列為 Non-Goal，理由見 design D6。

- [x] 2.3 在 `.cash-skills/lib/cash_cli/installer.py` 的 `canonical_guidance` 實作 Implementation Contract IC5：對 source 的 Cash start 與 end marker 額外要求不帶字尾，帶字尾時在首次 target write 前 fail closed，且該例外訊息必須包含帶 source 限定詞的標籤。

- [x] 2.4 更新 `CASH-SKILLS.md` 中描述 guidance marker 治理的段落，三處都要改。其一，該段目前把「未知版本 marker」列在會 fail closed 的清單中，本次改動使該句變假，改寫為：marker 名稱與結尾符號之間的字尾會被容忍並略過、不被解析。其二，補上 IC5 新增的 source 側規則：source 的 Cash start 與 end marker 不得帶字尾，帶字尾時在首次 target write 前 fail closed 且該字尾不會被寫入任何 target。其三，補上 source guidance 不得在任何位置出現形似 legacy marker 的文字——包括散文中的舉例——否則 `canonical_guidance` 對該名稱的定位會落入既有的 fail-closed 判定而在首次 target write 前失敗。文件措辭不應指名是哪一條內部判定——散文中的舉例通常落入非獨立行、獨立成行且無對側時才是計數不相等，兩者診斷字樣不同。其二與其三都是一個 source 檔擋掉全部 registered target 的失敗模式，是本次影響面最大的新行為，文件未說明則使用者無從理解為何全部 target 同時失敗；其三尤其需要寫出來，因為本 repo 的 `CASH-SKILLS.md` 今天就含有一行形似 legacy marker 的說明文字，該寫法在本專案是自然會出現的。改寫時 MUST 保留 `scripts/cash-skills/tests/skill-checks.fish` 的 `assert_guidance_and_docs` 對本檔綁定的全部 14 條字面（含 `fail closed`），否則該檢查會失敗。此檔案由 `openspec/specs/cash-skill-workflows/spec.md` 的 guidance 說明 requirement 治理，且沒有任何自動檢查會偵測這三處 drift，必須手動同步。

- [x] 2.5 執行 1.1 至 1.8 全部通過後，讀取當下的 `cash-skills.version` 與 `git show HEAD:cash-skills.version`，將該檔寫為嚴格大於兩者的下一個版本（撰寫本 task 時工作樹與 HEAD 皆為 `2.4.0`，故應寫 `2.4.1`；執行時 MUST 重新讀取而非沿用此處的示例值），並於 project root 執行 `./install-cash-skills.fish --self` 重建 receipt。MUST NOT 寫死常數：同一 workspace 的 sibling change `guard-post-archive-commit-allowlist` 也宣告調升該檔，並已於 commit `2c700eb` 提交並封存，寫死 `2.3.2` 會使 `check_history` 因 `2.3.2` 不大於 HEAD 而拋 bundle version must strictly increase，或在 sibling 尚未落地時靜默回退它的調升。理由：`.cash-skills/lib/cash_cli/installer.py` 屬 replaceable runtime record，未調升版本會使 bundle version history contract test 失敗；未重建 receipt 則 launcher 會以 receipt 驗證失敗擋下所有後續 `.cash-skills/bin/cash` 指令。此步驟必須在 2.1 至 2.3 之後，且必須早於第 3 節任何使用 launcher 的驗證。

## 3. 驗證

- [x] 3.1 執行 `.cash-skills/bin/cash validate --all`、`fish scripts/cash-cli/tests/cli-checks.fish` 與 `fish scripts/cash-skills/tests/skill-checks.fish`，三者必須全綠。`skill-checks.fish` 的 `installer-runtime` group 會以檔名執行 `scripts/cash-skills/tests/test_installer_runtime.py`，該檔以 `unittest.main()` 收尾，因此新增的測試案例被自動探索，不需修改 `scripts/cash-skills/tests/skill-checks.fish`。

- [x] 3.2 Implementation Contract IC8 的端對端驗收：兩個 target 不再因 marker 而 fail closed。執行 `./install-cash-skills.fish --all --dry-run`，斷言彙總的 `failed` 計數為 0，且 Tubify 與 player-log-downloader 不再出現 marker 相關的失敗訊息。此驗收不綁定 registry 條目總數。此 task 只跑 dry-run、不寫入任何 target，因此不需要內容層級的前置保護；收斂結果本身由 1.1 與 1.2 的 fixture 驗證。交付說明：真正的 guidance 刪除會發生在使用者日後某次真實安裝，而 `--dry-run` 不提供 byte-level 預覽，因此本 change 的完成回報必須告知使用者，那兩個 target 的 `AGENTS.md` 與 `CLAUDE.md` 會在下次真實安裝時各被刪去一段 legacy block（實測 1198 與 1212 bytes），且兩檔目前處於 uncommitted modified 狀態，建議先提交或另存副本。

- [x] 3.3 對本次改動後的實作重跑全語料一致性檢查：以 `git show HEAD:.cash-skills/lib/cash_cli/installer.py` 取出修復前的 `marker_span` 作為對照組（2.1 執行後舊實作已不在工作樹中，沒有對照組則此檢查會退化成自我比對而必然得到「不一致數 0」），對全機器已註冊 target 與本 repo 的 `AGENTS.md` 與 `CLAUDE.md`，逐一比對修復前後對獨立成行無字尾 marker 的定位結果，斷言不一致數為 0。此為 Implementation Contract IC2 在真實語料上的補充證據；IC2 的正式驗收基準是 1.3 的 fixture，本項是一次性佐證，不取代該 fixture。
