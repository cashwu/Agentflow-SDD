## 1. Resource validator precision

- [x] [P] 1.1 依design C1／C2重構三個resource validators：移除裸permissive token與重複guard，逐字實作D1的TDD三類、test-quality六類與tasks四類contradiction inventory，以不從detector registry推導的固定fixtures加入獨立additive mutations及`forbidden` rejection assertions，保留required marker removal／replacement的`missing` rejection，加入三類合法近義措辭acceptance cases，並在完成前檢查change-scoped task diff只含delivery path；delivery: `scripts/cash-cli/tests/test_graph_instructions.py`；verification: `PYTHONPATH=.cash-skills/lib python3 scripts/cash-cli/tests/test_graph_instructions.py`；regression: `fish scripts/cash-cli/tests/cli-checks.fish`，以及manual assertion確認本task change-scoped edit inventory恰為`scripts/cash-cli/tests/test_graph_instructions.py`；success: primary target以exit 0完成，且直接斷言detector／fixture category exact sets、每個additive contradiction命中`forbidden`、每個required-marker mutation命中`missing`、三類含舊裸token的合法措辭被接受；red: N/A，屬remaining-task的checker-only精煉，不修改production behavior且primary內的in-memory mutations提供鑑別力證據

## 2. Skill single-source guard precision

- [x] [P] 2.1 依design C3將`test_quality_gate_literals`替換為五個gate、每個gate各含繁中canonical原文與英文equivalent的十個固定clauses及in-memory validator；detector registry與mutation fixtures獨立定義並斷言gate／language exact sets，對四份skill執行live檢查，為每個clause加入append-injection mutation，加入一般`expected value`／verification prose acceptance cases，並在完成前檢查change-scoped task diff只含delivery path；delivery: `scripts/cash-skills/tests/skill-checks.fish`；verification: `fish scripts/cash-skills/tests/skill-checks.fish tdd-discipline`；regression: `fish scripts/cash-skills/tests/skill-checks.fish`，以及manual assertion確認本task change-scoped edit inventory恰為`scripts/cash-skills/tests/skill-checks.fish`；success: primary target輸出`PASS: tdd-discipline`且直接證明detector／fixture五個gate與`zh`／`en`exact sets、十個注入mutations各自被拒絕、合法泛用prose被接受、四份skill consumer與variant parity仍通過；red: N/A，屬remaining-task的checker-only精煉，不修改workflow behavior且primary內的in-memory mutations提供鑑別力證據

## 3. 精確度不變式的機械覆蓋

- [x] 3.1 對應requirement `Canonical discipline contract tests 精確辨識語意退化`；依design D1／D5與C1新增的兩項contract，把`red-after-edit`、`non-observable-result`、`framework-required`、`test-for-every-task`四個literal在detector registry與fixture inventory兩處同步改為D1的新值，新增一份與兩者都獨立定義、涵蓋D1全部13個category的逐字negation restatement inventory，先斷言其category exact set再逐一斷言每個literal都不是其否定句的子字串，並確認retired裸token非重用守衛涵蓋三個inventory；delivery: `scripts/cash-cli/tests/test_graph_instructions.py`；verification: `PYTHONPATH=.cash-skills/lib python3 scripts/cash-cli/tests/test_graph_instructions.py`；regression: `fish scripts/cash-cli/tests/cli-checks.fish`，以及manual assertion確認本task change-scoped edit inventory恰為`scripts/cash-cli/tests/test_graph_instructions.py`；success: primary target以exit 0完成，且直接斷言negation inventory的category exact set等於D1的13個category、13個literal各自都不是其否定句的子字串、四個更新後的literal仍不在對應canonical文本中、既有additive mutation仍命中`forbidden`且required-marker mutation仍命中`missing`；red: 先把negation inventory與不變式斷言加入而literal尚未更新，primary target必須以四個category的subTest失敗並具名指出literal仍是其否定句的子字串

- [x] 3.2 對應requirement `Testing discipline 單一來源檢查精確辨識重複語意`；依design D5與C3新增的contract，確認`assert_test_quality_single_source`在injection與acceptance檢查之後另行讀取canonical `DISCIPLINES["test-quality"]`並斷言五個`zh` clause仍是其逐字span，取得canonical失敗時fail closed；delivery: `scripts/cash-skills/tests/skill-checks.fish`；verification: `fish scripts/cash-skills/tests/skill-checks.fish tdd-discipline`；regression: `fish scripts/cash-skills/tests/skill-checks.fish`，以及manual assertion確認本task change-scoped edit inventory恰為`scripts/cash-skills/tests/skill-checks.fish`；success: primary target輸出`PASS: tdd-discipline`，且以detector與fixture的`zh` clause同步漂移mutation直接觀察到injection self-test仍通過而anchor具名指出該gate的clause已非canonical逐字span；red: N/A，屬remaining-task的checker-only精煉，不修改workflow behavior且primary內的同步漂移mutation提供鑑別力證據

## 4. 守衛輸入的錨定

- [x] 4.1 對應requirement `Canonical discipline contract tests 精確辨識語意退化`；依design D5與C1新增的兩項contract，確認retired裸token集合已以exact set斷言錨定，並為negation restatement inventory加上兩項內容斷言——每句非空且含`不`或`並非`、每句append到對應canonical文本後仍被該validator接受；同時確認inventory的13句逐字等於design D5所列，並在完成前檢查change-scoped task diff只含delivery path；delivery: `scripts/cash-cli/tests/test_graph_instructions.py`；verification: `PYTHONPATH=.cash-skills/lib python3 scripts/cash-cli/tests/test_graph_instructions.py`；regression: `fish scripts/cash-cli/tests/cli-checks.fish`，以及manual assertion確認本task change-scoped edit inventory恰為`scripts/cash-cli/tests/test_graph_instructions.py`；success: primary target以exit 0完成，且以in-memory mutation直接觀察到把13句restatement全部改為空字串時該內容斷言具名失敗、把retired token集合削減為單一元素時exact set斷言具名失敗；red: N/A，屬remaining-task的checker-only精煉，不修改production behavior且primary內的兩個mutation提供鑑別力證據

- [x] 4.2 對應requirement `Testing discipline 單一來源檢查精確辨識重複語意`；依design D5與C3新增的contract，確認`assert_test_quality_single_source`已斷言detector與fixture兩份inventory的clause值逐字相等，且該斷言與既有gate／language key set檢查並存；並在完成前檢查change-scoped task diff只含delivery path；delivery: `scripts/cash-skills/tests/skill-checks.fish`；verification: `fish scripts/cash-skills/tests/skill-checks.fish tdd-discipline`；regression: `fish scripts/cash-skills/tests/skill-checks.fish`，以及manual assertion確認本task change-scoped edit inventory恰為`scripts/cash-skills/tests/skill-checks.fish`；success: primary target輸出`PASS: tdd-discipline`，且以只縮短detector單一clause而fixture不動的mutation直接觀察到值相等斷言具名失敗並使群組非零結束；red: N/A，屬remaining-task的checker-only精煉，不修改workflow behavior且primary內的單方面漂移mutation提供鑑別力證據

## 5. 合法散文 fixture token inventory 錨定

- [x] 5.1 對應requirement `Testing discipline 單一來源檢查精確辨識重複語意`；依design C3把`expected value`與`verification target`移入具名`LEGITIMATE_PROSE_TOKENS` inventory，以inline fixed set斷言其exact set後再逐項確認`LEGITIMATE_PROSE`承載token，並確認同步清空inventory與fixture時仍由exact-set斷言具名失敗；delivery: `scripts/cash-skills/tests/skill-checks.fish`；verification: `fish scripts/cash-skills/tests/skill-checks.fish tdd-discipline`；regression: `fish scripts/cash-skills/tests/skill-checks.fish`，以及manual assertion確認本task change-scoped edit inventory恰為`scripts/cash-skills/tests/skill-checks.fish`；success: primary target輸出`PASS: tdd-discipline`；red: N/A，屬remaining-task的checker-only精煉，不修改workflow behavior且primary內的in-memory inventory mutations提供鑑別力證據
