## 1. Bundle version 前置

- [x] 1.1 將 `cash-skills.version` 內容與 `.cash-skills/lib/cash_cli/installer.py` 的 `BUNDLE_VERSION` 常數同步改為 `2.21.0`，且此修改必須先於任何其他受 bundle history gate 守衛的檔案改動；delivery: cash-skills.version, .cash-skills/lib/cash_cli/installer.py；verification: `python3 scripts/cash-skills/tests/test_installer_runtime.py InstallerRuntimeTests.test_bundle_version_constant_matches_the_version_file`；regression: 同檔的 `test_bundle_runtime_paths_matches_the_source_inventory`，因為版本 bump 不得改變 replaceable runtime 的路徑集合；success: 該測試以 `OK` 通過，且 `cash-skills.version` 與 `BUNDLE_VERSION` 皆為 `2.21.0`；red: N/A — 依 remaining-task 分類，本 task 是為後續受守衛編輯建立前置條件的版本宣告，沒有先失敗的行為斷言可寫。選用該測試的理由是它只比對 `BUNDLE_VERSION` 與 `cash-skills.version`、完全不觸及 manifest，因此在本 task 完成時點必定可判定；不以 `test_bundle_version_history.py` 作為 verification 的理由是：`check_history` 要求 `.cash-skills/manifest.tsv` 逐 byte 等於由工作樹版本導出的 canonical bytes，因此從本 task 完成到 4.1 的 `--self` 之間，該腳本必然以 `portable manifest is not canonical` 失敗；history gate 的完整通過由 4.3 承擔

## 2. Batch publication-mode 分派

- [x] 2.1 在 `.cash-skills/lib/cash_cli/installer.py` 新增 module-level `registry_publication_mode(source, record)`，整個函式主體包在單一 try 內、任何 exception 一律回傳 `"receipt"`，並依 design D2 的三支有序分割回傳 `"vendor"` 或 `"receipt"`（第 2 支同時要求 manifest 存在與 regular shape）；`run()` 的 `options.all` 迴圈在每個 record 既有的 `try` 之內呼叫它並分派到 `install_vendored_target` 或 `install_target`，vendored 分派的輸出行在 record 後附加 ` (vendored)`；delivery: .cash-skills/lib/cash_cli/installer.py, scripts/cash-skills/tests/test_installer_runtime.py；verification: `python3 scripts/cash-skills/tests/test_installer_runtime.py` 的 `test_batch_dispatches_vendored_and_receipt_targets`；regression: 同檔的 `test_batch_reports_each_target_and_summary` 與 `test_vendor_current_update_receipt_conversion_and_direct_mode_rejection`；success: 該測試通過，混合 registry 的 `--all` stdout 含 `(vendored)` 後綴行與 `updated=2`、`failed=0`，結束碼為 0，receipt record 的輸出行明確不含 `(vendored)`（以否定斷言釘住後綴不外溢，因為既有測試的 `assertIn` 對帶後綴的行同樣成立），且被分派到 vendored 路徑的 target 執行後其殘留 `.cash-skills/receipt.tsv` 不存在；red: 實作前該測試因 stderr 出現 `target is managed by a portable manifest; use --vendor`、stdout 出現 `failed=1` 而失敗

- [x] 2.2 為 `install_vendored_target` 新增預設關閉的 batch-only keyword 參數，開啟時在進入分類前重新確認 `.cash-skills/manifest.tsv` 仍存在、不存在則以 `InstallerError` fail closed，並由 `run()` 的 batch 分派帶入該參數；該參數同時 MUST 在該函式 journal recovery 成功後的自我遞迴呼叫上原樣轉傳（該遞迴以明列 keyword 重新呼叫自身，漏傳會讓重入以關閉狀態重新分類），此項以程式碼層級檢查完成而不列入 success marker，因為 manifest 缺失的測試情境在分類前即 fail closed、到不了 recovery 分支；明示 `--vendor` 不帶該參數因而行為不變；delivery: .cash-skills/lib/cash_cli/installer.py, scripts/cash-skills/tests/test_installer_runtime.py；verification: `python3 scripts/cash-skills/tests/test_installer_runtime.py` 的 `test_batch_vendor_dispatch_requires_manifest_at_classification`，該測試以 in-process `from cash_cli.installer import install_vendored_target` 直接對一個 manifest 缺失的 target 帶該參數呼叫；regression: 同檔的 `test_vendor_fresh_reports_update_and_portable_launcher_ignores_stale_receipt` 與 `test_vendor_receiptless_exact_partial_force_and_unknown_extra_adoption`，兩者以不帶該參數的明示 `--vendor` 涵蓋 receiptless adoption 與 fresh publication；success: 該測試通過，帶參數呼叫 raise 的 `InstallerError` 其訊息為新加的 manifest 重新確認診斷（因而與 `validate_target_prerequisites` 或 `require_vendored_paths_committable` 的前置 preflight 失敗可區分），且該 target 未被發佈 manifest、未刪除 receipt；red: 實作前該測試因 `install_vendored_target` 不接受該 keyword 參數而以 `TypeError` 失敗

- [x] 2.3 新增 `--all --dry-run` 對 manifest-present target 的契約測試，斷言回報 `would-update` 並帶 ` (vendored)` 後綴且該 target 零寫入；delivery: scripts/cash-skills/tests/test_installer_runtime.py；verification: `python3 scripts/cash-skills/tests/test_installer_runtime.py` 的 `test_batch_dry_run_reports_would_update_for_vendored_target`；regression: 同檔的 `test_batch_dispatches_vendored_and_receipt_targets`；success: 該測試通過，stdout 含 `would-update` 與 ` (vendored)`，且 target workspace snapshot 在執行前後相等；red: 實作前該測試因該 target 被回報為 `failed` 而非 `would-update` 失敗

- [x] 2.4 新增 unsafe manifest shape 的四種契約測試：symlink、FIFO 與 directory 落入 catch-all 的 receipt 路徑並各以既有診斷 fail closed、輸出行不帶後綴且不阻塞；hard-linked 的 regular manifest 通過 probe 第 2 支而進入 vendored 路徑，由 `read_regular` 的 single-link 檢查以 `unsafe regular file identity` fail closed 且輸出行帶 ` (vendored)` 後綴；delivery: scripts/cash-skills/tests/test_installer_runtime.py；verification: `python3 scripts/cash-skills/tests/test_installer_runtime.py` 的 `test_batch_unsafe_manifest_shape_fails_closed_without_blocking`；regression: 同檔的 `test_batch_reports_each_target_and_summary`，因為它會實際走過 `--all` 的 per-record 分派迴圈並對兩個 receipt record 斷言逐字輸出與 `updated=2`；本檔沒有以 installer mode 涵蓋 unsafe manifest shape 的既有測試——`test_manifest_present_unsafe_shapes_fail_before_open_without_receipt_fallback` 雖涵蓋同一組四種 shape，但它以 `run_target_cash` 斷言 target 端 launcher 的 `manifest_invalid` gate，不經過任何 installer mode；success: 該測試通過，symlink 情境的 stderr 含 `symlink managed boundary: .cash-skills/manifest.tsv`，FIFO 與 directory 情境的 stderr 含 `target is managed by a portable manifest; use --vendor` 且其 stdout 行不含 `(vendored)`，hard link 情境的 stderr 含 `unsafe regular file identity` 且 stdout 含 `failed: <path> (vendored)`，四種情境的 target 皆零寫入且該測試在其自身的 subprocess timeout 內完成；red: 實作前 hard link 情境因該 record 仍由 receipt 路徑以 `target is managed by a portable manifest; use --vendor` 拒絕、stdout 行不含 `(vendored)` 後綴而失敗

- [x] 2.5 新增 probe 例外不中止批次的契約測試：某個 record 的 `.cash-skills` 為 regular file 而非目錄時，probe 以 `managed parent is not a directory` 落入 catch-all、該 record 由 receipt 路徑以 `failed` 計數，其餘 record 仍被處理且 summary 仍輸出；delivery: scripts/cash-skills/tests/test_installer_runtime.py；verification: `python3 scripts/cash-skills/tests/test_installer_runtime.py` 的 `test_batch_probe_exception_does_not_abort_remaining_records`；regression: 同檔的 `test_invalid_later_registry_record_blocks_all_target_writes`；success: 該測試通過，stdout 同時含該 record 的 `failed:` 行、後續 record 的成功行與 `Summary:` 行，stderr 不含 Python traceback，且當 registry 另含 canonical source path 的 record 時其 stderr 為 receipt 路徑既有的 non-source 診斷而非 vendored 路徑的對應診斷；red: N/A — 依 pure-refactor 分類，本 task 釘住的是「新增 probe 之後該 record 的計數與 summary 行為逐字不變」；實作前該 record 已由 `install_target` 內同一個 `ensure_contained` 拋出被迴圈捕捉的 `InstallerError` 並計為 `failed`，本設計刻意不為此情境引入任何新的可觀察差異，因此不存在可先失敗的斷言

- [x] 2.6 新增 `--all --force` 對 drifted vendored target 的契約測試，斷言未帶 `--force` 時回報 `conflict` 且零寫入、帶 `--force` 時只收斂 replaceable managed bytes 與 mode class 而 project-owned 內容不變；delivery: scripts/cash-skills/tests/test_installer_runtime.py；verification: `python3 scripts/cash-skills/tests/test_installer_runtime.py` 的 `test_batch_force_converges_only_replaceable_vendored_bytes`；regression: 同檔的 `test_vendor_current_update_receipt_conversion_and_direct_mode_rejection`；success: 該測試通過，`--all` 回報 `conflict: <path> (vendored)` 且零寫入，registry 中版本較新的 vendored target 回報 `newer: <path> (vendored)`，`--all --force` 後 drifted managed bytes 等於 source 而 project-owned 檔案 bytes 不變；red: 實作前該測試因該 target 兩次都被回報為 `failed` 而失敗

## 3. Register 接受 vendored target

- [x] 3.1 移除 `run()` 的 `options.register` 分支中 portable manifest 的拒絕，保留 `canonical_target`、non-source 檢查、`validate_target_prerequisites`、去重與 atomic registry 寫入不變，並新增登錄 manifest-present target 的契約測試；delivery: .cash-skills/lib/cash_cli/installer.py, scripts/cash-skills/tests/test_installer_runtime.py；verification: `python3 scripts/cash-skills/tests/test_installer_runtime.py` 的 `test_register_accepts_manifest_present_target`；regression: 同檔的 `test_register_validates_target_before_registry_write`、`test_register_rejects_source_without_registry_write` 與 `test_vendor_current_update_receipt_conversion_and_direct_mode_rejection`；success: 該測試通過，`--register` 結束碼為 0、registry 檔案恰含該 canonical absolute path 一次，且該 target 未被安裝或修改；red: 實作前該測試因 `--register` 以 `target is managed by a portable manifest; use --vendor` 非零結束且 registry 零寫入而失敗

## 4. 發佈、文件與全套驗證

- [x] 4.1 在本 repository 執行 `./install-cash-skills.fish --self` 重新發佈 canonical portable manifest，使 `runtime_generation` 與 record digest 反映修改後的 `installer.py`；delivery: .cash-skills/manifest.tsv；verification: `./install-cash-skills.fish --self`；regression: `python3 scripts/cash-skills/tests/test_installer_runtime.py`，因為其 receipt 與 manifest serializer 斷言直接讀取重新發佈後的 inventory；success: 該指令回報 `Result: bootstrap`（尚有未發佈的受管改動時）或 `Result: current`（前面的 task 已依 Managed bundle publication 協定增量發佈完畢），兩者都代表 manifest 為 canonical 且反映最終的 `installer.py`；red: N/A — 依 remaining-task 分類，本 task 是受管 bundle 的發佈動作而非行為變更，沒有先失敗的行為斷言可寫

- [x] 4.2 更新使用者文件使其與新行為一致：`CASH-SKILLS.md` 的 batch 輸出格式段落補上 ` (vendored)` 後綴、`--register` 敘述補上接受 manifest-present target、reverse-conversion 與 receipt-based batch workflow 兩處敘述改為分派語意；`CASH-INIT-RECEIPT.md` 的 mode 對照表把 `--register` 列的「不得把 vendored target 納入 receipt workflow」與 `--all` 列的「對 registry 中全部 receipt-based target 批次安裝／升級」改為反映分派語意，並把「receipt target 的內容更新由 `--target`／`--all` 負責，vendored target 則由 `--vendor` 負責」改為反映 `--all` 也負責 regular manifest 的 vendored record；共 `CASH-SKILLS.md` 四處與 `CASH-INIT-RECEIPT.md` 三處；delivery: CASH-SKILLS.md, CASH-INIT-RECEIPT.md；verification: 手動斷言 `rg -n '\(vendored\)' CASH-SKILLS.md` 有命中，且 `rg -n 'receipt-based target 批次|不得把 vendored target 納入 receipt workflow|vendored target 則由 .--vendor. 負責|不會反向把 vendored target' CASH-INIT-RECEIPT.md CASH-SKILLS.md` 零命中；regression: `./scripts/cash-skills/tests/skill-checks.fish`，因為它對這兩份文件有正向 `assert_contains` 與三個 `assert_absent` 斷言；本 task 排在 4.1 的 `--self` 之後正是為了讓這個 regression 可執行——該腳本包含 bundle history gate，而 gate 要求 manifest 已重新發佈為 canonical；success: 該手動斷言的兩個條件同時成立——`CASH-SKILLS.md` 出現 ` (vendored)`，且四個過時 pattern 在兩份文件中皆零命中；red: N/A — 依 remaining-task 分類，文件敘述沒有自動測試邊界；`skill-checks.fish` 對這兩份文件的正向 `assert_contains` 不會對過時敘述失敗，其對 `CASH-INIT-RECEIPT.md` 既有的三個 `assert_absent` 也都不涵蓋本次要改寫的敘述，因此沒有先失敗的斷言可寫

- [x] 4.3 執行完整 skill 與 CLI 契約套件確認無回歸；delivery: (none)；verification: `./scripts/cash-skills/tests/skill-checks.fish`；regression: `./scripts/cash-cli/tests/cli-checks.fish`；success: `skill-checks.fish` 以零結束碼結束且未輸出任何 `FAIL:` 行；red: N/A — 依 remaining-task 分類，本 task 是既有套件的全量回歸執行，其行為斷言已由前述 task 各自的 red phase 建立

