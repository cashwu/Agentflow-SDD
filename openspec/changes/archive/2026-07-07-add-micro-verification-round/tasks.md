## 1. 共用模板：分級收斂與微型驗證輪

- [x] 1.1 依 design「決策一：finding 新增 layer 欄位，由 reviewer 標註、主 agent 保守覆核」修改 scripts/spectra-plus/template/review-loop-block.md 的 finding 欄位要求：欄位清單加入 `layer`（值僅 `design` / `text`）、分類 rubric、「分不清楚時一律標 `design`」規則、主 agent 在 confidence filter 時的單向覆核（僅允許 `text`→`design`）、A+B 去重合併時 `layer` 不一致一律取 `design` 的合併規則；「Reviewer output requirements」段首句 "Both reviewers" 等雙 reviewer 假設措辭改為輪型中立（如 "All of a round's reviewers"）。交付行為：模板明文承載 spec「Graded convergence and micro-verification round」的 layer 分類與合併契約，且微型輪下無殘留的雙 reviewer 措辭。驗證：內容檢視模板段落 + 任務 3.1 的 `layer` 斷言在重生輸出上通過。
- [x] 1.2 依 design「決策二：微型驗證輪的觸發與回退規則」與「決策三：微型輪計入回合上限並沿用 round file 結構」修改模板「Round limit and pass condition」：第 1 輪必為全量輪；decision 為 `next_round` 時依（無 surviving Critical 且所有 surviving Warning `layer == text` 且本輪為全量輪）導出下一輪為微型輪，否則全量輪；導出輪型在下一輪 reviewer spawn 前為暫定，decision 記錄後的任何 artifact 修改（apply-plus 加上 implementation file 修改；包含但不限於 fix action、機械自檢修正、validate 修正）實際改動行為或設計敘述時 —— 或無法確定是否僅為文字同步時 —— 必須改判為全量輪（僅允許 micro→full 單向），改判發生時在本輪 round file 的 `## Fix Actions` 末尾記一行改判註記；微型輪不得連續；微型輪計入 6 輪上限。交付行為：spec「spectra-propose-plus quality gate」與「spectra-apply-plus quality gate」新增的 next-round-type 導出規則（含 fix 後單向改判、保守 tie-breaker、改判註記）在模板中以 MUST 語氣存在。驗證：內容檢視 + 任務 3.1 斷言。
- [x] 1.3 修改模板「Fresh sub-agent calls」：全量輪維持恰好兩個 reviewer（A、B）並行；微型輪 spawn 恰好一個 fresh `Reviewer V — Verification`，其 context 明文包含 artifact paths（apply-plus 加上 changed-file list）、上一輪 round file 的 surviving findings 與 `## Fix Actions`、相關 `open` signals；驗證範圍限定為 fix 落點、修正傳播完整性、修正引入的新缺陷；輸出欄位與 confidence filter 與 A/B 相同。「Signals in reviewer context」的 "BOTH reviewers" 措辭改為輪型中立（如 "all of that round's reviewers"）。交付行為：模板承載 spec「Fresh sub-agent per round」修改後的輪型-人數契約與 Reviewer V context 契約。驗證：內容檢視 + 任務 3.1 的 `Reviewer V — Verification` 斷言。
- [x] 1.4 修改模板「Round file schema」「Round file language」「Decision record requirements」：`## Rating` 欄位清單加入 `round_type`（值僅 `full` / `micro`）、findings 欄位含 `layer` 且 reviewer 來源允許 `V`；「Decision record requirements」段同步要求主 agent 記錄 `round_type`；verbatim（不翻譯）清單加入 `layer`、`round_type` 與其值。交付行為：模板承載 spec「Round file output contract」修改後的 Rating 欄位契約，且模板內部兩處欄位清單一致。驗證：內容檢視 + 任務 3.1 的 `round_type` 斷言。
- [x] 1.5 修改 scripts/spectra-plus/template/apply-notes-block.md「Sub-agent reviewer requirement」段（最小改寫）：apply-plus 微型輪由 `Reviewer V` 履行 per-round `implementation-notes.md` 讀取義務（含 file-absent 即 Critical、未解決 `open-question` 即 Warning 的 severity 規則），全量輪維持 Reviewer A 履行；段末收尾句「Reviewer A findings already incorporate the notes context」一併改為輪型中立措辭（如 "that round's reviewer findings"）；保留既有句子前綴「Reviewer A — Adherence in the Sub-Agent Review/Rating/Fix Loop MUST」以維持 generator-checks.fish 既有釘死斷言。交付行為：spec「Graded convergence and micro-verification round」中 apply-plus 微型輪的 implementation-notes 義務在模板中無真空、無殘留的雙 reviewer 假設措辭。驗證：內容檢視 + 任務 3.1 的義務句斷言 + generator-checks.fish 既有斷言不變仍 PASS。

## 2. Generator 版本與重生

- [x] 2.1 依 design「決策四：落地於共用模板、generator 版本與測試斷言」將 scripts/spectra-plus/rules.yaml 兩個 skill 的 `spectraPlusVersion` bump 至 `1.3.0`、`spectraPlusUpdated` 更新為實作當日，並跑 scripts/spectra-plus/generate.fish。交付行為：四個衍生 SKILL.md（.claude 與 .agents 各兩個）重生成功且皆包含 `Reviewer V — Verification`、`round_type`、`layer` 關鍵內容；且因 spec「Generated plus skill version metadata」與「Repair checks plus metadata freshness」的 scenario 斷言已改以 rules.yaml 宣告值為準，本次與未來的版本 bump 不再造成 spec 字面 drift。驗證：generate.fish 輸出四組 wrote/validated 行（共 8 行）+ rg 檢查四個輸出檔。

## 3. 測試斷言與全套驗證

- [x] 3.1 [P] 更新 scripts/spectra-plus/tests/generator-checks.fish：`plus_version` / `plus_updated` 釘死值同步為 `1.3.0` 與實作當日；於共用 review-loop 斷言區新增 assert_contains —— `Reviewer V — Verification`、`round_type`、`layer`、微型輪不得連續關鍵句、微型輪觸發條件關鍵句、layer 保守規則（分不清楚時一律 `design`）關鍵句、主 agent 單向覆核關鍵句（MUST NOT reclassify a `design` finding to `text`）、fix 後 micro→full 單向改判關鍵句、改判保守 tie-breaker 關鍵句（無法確定時視同改動行為）、改判註記關鍵句（re-derivation note 記於 `## Fix Actions` 末尾）、A+B layer 合併取 `design` 關鍵句；apply-plus 斷言區新增微型輪 implementation-notes 義務句斷言。交付行為：design 驗收標準 (3) 列出的模板關鍵規則全數具防退化保護。驗證：該測試檔單獨執行 PASS。
- [x] 3.2 [P] 更新 scripts/spectra-plus/tests/repair-all-checks.fish：`plus_version` / `plus_updated` 變數同步為 `1.3.0` 與實作當日（其餘版本引用已使用變數，無硬編碼需要處理）。交付行為：repair 流程測試與新版本一致。驗證：該測試檔單獨執行 PASS。
- [x] 3.3 執行全部四個測試套件（generator-checks.fish、repair-all-checks.fish、auto-restore-checks.fish、installer-commit-guard-checks.fish）。交付行為：本 change 未破壞 generator、repair、auto-restore 與 commit guard 任一契約。驗證：四個套件全數輸出 PASS。
