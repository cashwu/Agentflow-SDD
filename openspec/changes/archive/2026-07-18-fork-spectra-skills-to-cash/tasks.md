<!--
Each task description states the observable contract and its verification target.
File paths are supporting context; completion is proven by the named checks.
-->

## 1. 建立 cash canonical skill inventory

- [x] [P] 1.1 依「Cash skills 採直接 canonical files」、「Cash skill inventory and ownership」與「Skill inventory and observable workflow」建立 Codex 的十二個 `.agents/skills/cash-*/SKILL.md`；每份檔案必須有正確 `cash-*` name、無 Spectra generated metadata，並以 `scripts/cash-skills/tests/skill-checks.fish` 的 Codex inventory/frontmatter assertions 驗證。
- [x] [P] 1.2 依「Cash skills 採直接 canonical files」、「Cash skill inventory and ownership」、「Skill inventory and observable workflow」、「Variant parity uses readable exhaustive manifests」與「Variant parity compares complete governed bodies」建立 Claude 的十二個 `.claude/skills/cash-*/SKILL.md`；只正規化 `/cash-*`／`$cash-*` invocation，無 tool 差異者完整相等，有 tool-capability 差異者逐行符合 readable exact per-skill diff manifest。
- [x] 1.3 依「Cash namespace routes workflows while Spectra remains the artifact engine」、「Spectra CLI and artifact schema remain shared」與「Scope boundaries」將 24 個 skill-to-skill transitions 全部正規化為 cash namespace，同時保留 `spectra` CLI 與 `openspec/` literals；以 bounded `rg` assertions 證明不存在 active `$spectra-*`、`/spectra-*` 或 `cash-*-plus` transition，且必要 Spectra CLI commands 仍存在。

## 2. 將 plus 行為併入正式 workflow

- [x] [P] 2.1 依「Propose/apply 吸收 plus behavior」、「Propose and apply absorb the former plus workflows」與「cash-propose quality gate」建立 Claude/Codex `cash-propose`，交付完整 artifact DAG、繁中非 spec artifacts、英文 specs、validation-before-review、Impact 15/16 boundary advisory、no-apply/no-park termination；以完整 baseline contract comparison、fixture proposal flow 與 `spectra validate` 驗證。
- [x] [P] 2.2 依「Propose/apply 吸收 plus behavior」、「Propose and apply absorb the former plus workflows」與「cash-apply quality gate」建立 Claude/Codex `cash-apply`，交付 implementation notes、Surgical & Simplicity discipline、tasks-complete gate、passed-only archive guidance 與 needs-design circuit breaker；以完整 baseline contract comparison 與 apply workflow contract assertions 驗證。
- [x] [P] 2.3 依「Cash commit preserves archive-first allowlisting」與「cash-commit archive-first allowlist」建立 Claude/Codex `cash-commit`，保留 selected-change archive-first allowlist、tracked source files、使用者確認的 customizations、明確選取的 spec-sync changes 與 archive outputs，並移除 generated-plus exception；以 isolated dirty-worktree fixtures 驗證各 allowlist 類別被納入、unrelated files 保持 unstaged、plus deletion 不再自動放行。
- [x] 2.4 對 `cash-propose` 與 `cash-apply` 完成 shared gate migration，逐條實作「Cash proposal quality gate」、「Cash apply quality gate」、「Shared graded review convergence」、「Review records use cash provenance」、「Cash review gates preserve governed inputs and signals」、「cash-propose quality gate」、「cash-apply quality gate」、「Round file output contract」、「Fresh sub-agent per round」、「Confidence-scored findings and filter」、「Sub-agent failure handling」、「Cash review loop writes signals after the loop ends」、「cash-propose reads open signals for prioritization」、「Graded convergence and micro-verification round」、「Review loop grader immutability」、「Review loop ledger output」、「Deterministic signal-derived self-checks」、「Accepted-risks ledger」、「Fix-loop design circuit breaker」、「Abort triage」、「cash-apply introduced-by evidence」、「Review round action obligation」、「cash-propose impact granularity advisory」；同時以 legacy removal assertions 覆蓋「spectra-propose-plus quality gate」、「spectra-apply-plus quality gate」、「Plus review loop writes signals after the loop ends」、「spectra-propose-plus reads open signals for prioritization」、「Apply-plus introduced-by evidence」與「Propose-plus impact granularity advisory」，並以 full-round、micro-round、seeded rerun、retry/failure、disposition、accepted-risk、signal check、signal matching、ledger、abort 與 action-obligation fixtures 證明所有 retained normative branch 只改 ownership/name 而未遺漏。
- [x] 2.5 依 modified「Signals shared layer location and file schema」、「Signals directory README contract」與「Signal status lifecycle is human-maintained」更新 cash writer/read behavior：保留 schema 與人工 `status`／`check` 規則，current provenance 改為 cash，並以 signal create/update/resolved/check-preservation fixtures 驗證。

## 3. 交付顯式安裝與一次性 cleanup

- [x] [P] 3.1 依「Installer is explicit stateless deployer」、「Stateless cross-project installer」、「Cash installation has no repair automation」、「Installer interface and failure behavior」與「Installer and cleanup enforce filesystem boundaries」實作 `install-cash-skills.fish --target <project> [--dry-run] [--force]`；先 canonicalize/preflight 24-file inventory、target、symlink/containment 與 conflicts，再處理 absent/identical/differing，並以 clean/identical/conflict/force/dry-run/root/unresolved/symlink-escape fixtures 驗證零 partial write。
- [x] [P] 3.2 依「Cleanup is one-time idempotent migration」、「One-time legacy repair cleanup」、「Cleanup interface and failure behavior」、「Installer and cleanup enforce filesystem boundaries」與「Cleanup unloads known labels independently of plist presence」實作 `uninstall-spectra-plus-repair.fish [--dry-run]`；安全驗證 HOME/boundaries 與 registry readable-regular-file preflight，normal/dry-run 先列 registered targets，normal mode 只接受 service-specific absent 訊息，對兩個 label 執行 print/必要 bootout 並確認 absent 後才刪 plist/registry/cache；以 isolated `HOME`、hostile startup config 和 stubbed `launchctl` 驗證 loaded-without-plist、installed、service-specific absent、generic not-found failure、registry read failure、unexpected print/bootout、unsafe HOME、symlink、dry-run 與 repeated-run。
- [x] 3.3 退役 legacy automatic repair contracts「Multi-project plus skill target registry」、「Repair all registered plus skill targets」、「LaunchAgent-based automatic plus skill repair」、「Auto-restore stripped commit guard source from git HEAD」、「Repair checks plus metadata freshness」、「Repair freshness check is three-state and fail-closed」與「Repair-all uses pinned commit inputs」；完成後 explicit installer 是唯一跨專案傳播路徑、cleanup 是唯一舊排程移除路徑，並以全 repo bounded `rg` 及 cleanup/installer fixtures 驗證無 registry writer、repair scheduler、fingerprint freshness 或 pinned-input runtime。

## 4. 建立 fail-loud contract tests

- [x] 4.1 依「Contract tests replace repair automation」、「Cash contract regression suite」、「Variant parity compares complete governed bodies」與「Acceptance criteria」建立 `scripts/cash-skills/tests/skill-checks.fish` 的 inventory、metadata、namespace、完整 quality contract、round schema、readable exact full-file parity manifests 與 legacy absence sections；在 compliant fixture 回傳 0，逐一刪改 full/micro/seeded/retry/disposition/accepted-risk/signal/ledger/abort/action-obligation 等 governed branch 時回傳非零並指出 project-relative path。
- [x] 4.2 為 installer 建立完整 branch matrix，至少覆蓋 invalid/root/unresolved target、missing source、clean install、identical no-op、single/multiple conflicts、`--force`、`--dry-run`、unrelated-file preservation、symlinked parent/destination、containment escape 與 preflight no-write；以 fixture checksum snapshots 驗證 target mutations精確符合 contract。
- [x] 4.3 為 cleanup 建立完整 branch matrix，使用 isolated `HOME`、temporary plist/registry/cache/log 與 stubbed `launchctl` 覆蓋 successful bootout、loaded-without-plist、service-specific absent、misleading generic not-found、unexpected print/bootout、unreadable/non-regular registry、unsafe HOME、symlink boundary、hostile Fish startup config、absent state、dry-run no-launchctl、log preservation與 idempotence；驗證不觸碰真實 `~/Library/LaunchAgents`。
- [x] 4.4 依「Spectra updates do not mutate cash skills」與「Project-owned cash guidance survives Spectra updates」在 isolated project fixture 對 24 個 cash files 記錄 checksums、執行 `spectra update --force`、再比對 byte identity；同時驗證 managed block 可更新、cash paths 不變、且 managed block 外的 effective cash override 仍存在並具 precedence。

## 5. 切換 ownership 並移除 legacy 系統

- [x] 5.1 依「Legacy plus implementation is retired」刪除四個 `spectra-*-plus` outputs、`scripts/spectra-plus/` 與 `install-spectra-plus.fish`，並將 `.agents/skills/spectra-commit/SKILL.md`、`.claude/skills/spectra-commit/SKILL.md` 一次性恢復成無 generated-plus exception 的 Spectra baseline；正式退役「Plus skills generation pipeline」、「Rules.yaml controls plus skill divergence」、「Content-derived plus skill fingerprints」、「Strict read-only fingerprint query」、「Plus skills are not hand-edited」、「Generated plus skill version metadata」、「Rules validation requires plus metadata」、「spectra-commit archive-first allowlist」、「plus generated skill deletion guard」、「plus installer updates spectra-commit guard」與「Structurally safe marked commit guard upgrade」，以 cash replacement fixtures、git path assertions、guard marker absence、generated metadata grep 與 regression suite 驗證舊 derived-artifact lifecycle 完全不存在。
- [x] 5.2 依「Project guidance survives Spectra-managed updates」與「Project-owned cash guidance survives Spectra updates」更新 `AGENTS.md`：把 project-owned override 放在 `<!-- SPECTRA:END -->` 後，明示 cash precedence，讓 discuss → propose → apply ⇄ ingest → archive/commit 指向 `$cash-*`；以 forced-update fixture 與 bounded `rg` 驗證 effective guidance。
- [x] 5.3 依「Live documentation and signals move with ownership」與「Live documentation reflects cash ownership and cleanup」將 `SPECTRA-PLUS.md` 改寫／更名為 `CASH-SKILLS.md`，更新 `openspec/signals/README.md` 的 current writer prose 並保留歷史 occurrences；以 live-doc bounded `rg` 與 fixture diff 驗證沒有 active generator/repair instructions。
- [x] 5.4 執行「Acceptance criteria」與「Scope boundaries」最終檢查：跑 `spectra validate fork-spectra-skills-to-cash`、`scripts/cash-skills/tests/skill-checks.fish`、artifact identifier/count cross-grep、legacy path/live-doc absence check 與 `git diff --check`；確認只修改 proposal `## Impact` 宣告的 repo paths，且不執行真實跨專案安裝、真實 LaunchAgent cleanup或任何 application implementation。
