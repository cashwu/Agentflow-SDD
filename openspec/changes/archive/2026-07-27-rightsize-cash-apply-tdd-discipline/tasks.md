## 1. 建立失敗中的 contract tests

- [x] [P] 1.1 為 `TDD discipline 以適用性判準表述` requirement 在 `scripts/cash-cli/tests/test_graph_instructions.py` 擴充 TDD resource contract：逐字驗證 `skill_payload("tdd")["instruction"] == DISCIPLINES["tdd"]`，並以獨立 assertions 覆蓋 observable executable behavior、目標失敗原因、unrelated earlier failure 排除、minimal green、green refactor、bug reproduction、pure-refactor evidence、remaining-task verification與四分支 precedence；另斷言沒有可行自動測試邊界的 bug fix，以及具有 checker 的文件／metadata task，都落入可使用命名 verification target但不強迫 red phase 的 catch-all 分支，並驗證 canonical instruction 不要求任何特定程式語言或 test framework。在修改 resource 前執行 `PYTHONPATH=.cash-skills/lib python3 scripts/cash-cli/tests/test_graph_instructions.py`，確認新增測試因 canonical instruction 缺少這些判準而失敗，而非因 import、syntax 或既有測試失敗。
- [x] [P] 1.2 為 `cash-apply 以條件式 canonical TDD discipline 驅動 task loop` requirement 在 `scripts/cash-skills/tests/skill-checks.fish` 新增 `assert_tdd_discipline` 與可獨立執行的 `tdd-discipline` group，並把相同函式納入 `all`；對 `.agents/skills/cash-apply/SKILL.md` 與 `.claude/skills/cash-apply/SKILL.md` 逐檔斷言唯一的條件式 `instructions --skill tdd` consumer、共同 verification-evidence gate、example-as-reference 與 reasoned-extra-case 判準，同時要求每檔的 `Red-Green-Refactor` literal count 恰為零，並拒絕舊的 per-task absolute fail-first 及 TDD 關閉仍強制更新測試的 literals。在修改兩個 skill 前執行 `fish scripts/cash-skills/tests/skill-checks.fish tdd-discipline`，確認新群組因現行 skill 仍含 `Red-Green-Refactor` literal、舊 literals 或缺少新 gate 而失敗。

## 2. 收斂 canonical discipline 與 consumers

- [x] 2.1 在 `.cash-skills/lib/cash_cli/resources.py` 將 `DISCIPLINES["tdd"]` 改為 design D2 有明確 precedence、每個 task 恰命中一項的四分支矩陣與有效 red 判準；在 `.agents/skills/cash-apply/SKILL.md` 及 `.claude/skills/cash-apply/SKILL.md` 移除完整 TDD sequence、bug-fix 重複規則與無條件 test-update 規則，只保留 `tdd` toggle、canonical consumer invocation、共同 verification-evidence gate及 example-reference 判準，且 consumer invocation 不含 `Red-Green-Refactor` literal。修改兩個變體時保持 invocation prefix 正規化後逐行一致，且不改動 audit、parallel task、blocker triage、Implementation Notes Protocol 或 review loop。同一 task 內遞增 `cash-skills.version` 並維持單行 LF；所有 replaceable runtime／skill bytes 完成後、呼叫本 task 的 `task done` 前，在 project root 執行 `./install-cash-skills.fish --self` 重建 source receipt。
- [x] 2.2 驗證 `python3 scripts/cash-skills/tests/test_bundle_version_history.py` 通過、`cash-skills.version` 相對 first-parent introduction commit 嚴格遞增，並確認 source receipt 對目前 runtime／skill bytes 有效；本 task 不延後或重做 2.1 首次 version bump 與 receipt 重建義務。

## 3. 驗證交付 contract

- [x] 3.1 執行 `PYTHONPATH=.cash-skills/lib python3 scripts/cash-cli/tests/test_graph_instructions.py`、`fish scripts/cash-skills/tests/skill-checks.fish tdd-discipline` 與 `fish scripts/cash-skills/tests/skill-checks.fish variant-parity`，確認 resource 八項行為語意、四分支 precedence、兩個 remaining-task boundary cases、instruction framework-neutral、CLI 逐字同源、兩個 conditional consumers、每個 skill 的 `Red-Green-Refactor` literal count 為零、舊絕對 literals 移除及完整變體對等全部通過。
- [x] 3.2 執行 `fish scripts/cash-skills/tests/skill-checks.fish`、`fish scripts/cash-cli/tests/cli-checks.fish` 與 `.cash-skills/bin/cash validate --all`；確認 full suites 涵蓋 `tdd-discipline` group，proposal、design、兩份 delta specs 與 tasks 的 requirements／Implementation Contract／驗證命令逐項相符，且工作樹未留下測試產生的 receipt 或 state residue。
