## 1. 先寫驗證（TDD 紅燈）

- [x] 1.1 [P] 在 `scripts/cash-cli/tests/test_sync_archive_transaction.py` 新增三個測試，沿用既有的 `make_workspace` helper：(a) `test_archive_manifest_records_touched_files` 先寫入一份合法的 `.cash-skills/state/touched/demo-change.json`（`version` 為 `1`、`change` 為 `demo-change`、`legacy_import` 為 `null`、`touched` 含一筆條目其 `files` 為兩個以 UTF-8 bytes 排序的來源檔路徑、頂層 `files` 恰為各條目 `files` 的排序聯集，以滿足 `_validate_touched` 的聯集檢查），封存後斷言 manifest 的 `touched_files` 逐字等於該 `files` 陣列、且 `touched_digest` 等於以封存前 touched 物件依既有公式計算的結果；(b) `test_archive_manifest_touched_files_is_empty_without_state` 在沒有任何 touched state 時斷言 manifest 含 `touched_files` 且其值為 `[]`；(c) `test_archive_manifest_other_fields_unchanged` 以固定期望值斷言 `version`、`change`、`destination`、`specs_synced`、`delta_digests`、`master_digests`、`legacy_cleanup` 的值不因新增 `touched_files` 而改變。驗證目標：執行 `scripts/cash-cli/tests/cli-checks.fish sync-archive-transaction`，(a) 與 (b) 因 manifest 缺少 `touched_files` 而失敗——(a) 的失敗原因必須是缺少 `touched_files`，不得是 `touched_invalid`——其餘既有測試維持通過。
- [x] 1.2 [P] 在 `scripts/cash-skills/tests/skill-checks.fish` 的 `assert_command_matrix` 內、既有 per-path 字面句斷言區塊之後，新增兩組 `assert_contains` 斷言，沿用既有三參數形式。對 `.agents/skills/cash-commit/SKILL.md` 與 `.claude/skills/cash-commit/SKILL.md` 斷言十一個字面句：`Detect a post-archive empty allowlist`、`parsed files array is empty`、`openspec/changes/.parked/<change-name>/`、`openspec/changes/archive/<date>-<change-name>/`、`keep the existing behavior and continue to step 3`、`except when step 2a establishes a post-archive recovery source`、`point-in-time snapshot taken at archive time`、`stop without committing`、`master_digests`、`` every `openspec/specs/` path stays in Unrelated Changes ``、`NEVER fall through to classifying every dirty source file as Unrelated`。對 `.agents/skills/cash-apply/SKILL.md` 與 `.claude/skills/cash-apply/SKILL.md` 斷言兩個字面句：`Archive first, then commit together`、`deletes the touched state that`。驗證目標：執行 `scripts/cash-skills/tests/skill-checks.fish codex-command-matrix`，因四個 SKILL 檔皆尚未修改而失敗。

## 2. CLI manifest 實作

- [x] 2.1 在 `.cash-skills/lib/cash_cli/commands/archive.py` 的 `archive_change` 內，於組裝 `archive_manifest` 時新增鍵 `touched_files`，值為 `list(touched["files"])`，插入位置在 `touched_digest` 之後、`legacy_cleanup` 之前；不得改動 `touched_digest` 的計算輸入或方式，也不得改動其餘欄位的值。**本任務的收尾步驟**：在標記本任務完成之前，MUST 先於 project root 執行 `./install-cash-skills.fish --self` 重建 `.cash-skills/receipt.tsv`。理由：`archive.py` 為 replaceable runtime record，launcher 在任何 dispatch 之前逐檔比對 receipt 記錄的 runtime digest；未重建時所有 `.cash-skills/bin/cash` 指令會以 `receipt_invalid: runtime record drift` 失敗，而標記本任務完成所用的 `task done` 正是下一個要執行的 CLI 指令，因此重建不能排成後續獨立任務。`.cash-skills/receipt.tsv` 為 gitignore 檔，不列入 affected code。驗證目標：執行 `scripts/cash-cli/tests/cli-checks.fish sync-archive-transaction` 與 `scripts/cash-cli/tests/cli-checks.fish positive-lifecycle` 全部通過，且重建後 `.cash-skills/bin/cash validate --all` exit 0。

## 3. cash-commit 封存後空允許清單偵測

- [x] 3.1 在 `.claude/skills/cash-commit/SKILL.md` 的 step 2 之後、step 3 之前新增子步驟 `2a`，標題為 `**Detect a post-archive empty allowlist**`，內容涵蓋 design.md 的 C2 所列全部要素與全部字面句；同時依 C2 改寫 step 2 的 `Cash state is the only allowlist authority after this point.`，並為 step 3、step 4、step 5、step 6、step 7 各加一句指向 `2a` 的說明（step 3 改用 `2a` 產出的 artifact 集合、step 4 的 `the tracking file` 改指 `2a` 產出的來源允許清單且 Unrelated 判定同時排除 artifact 集合與 spec sync 集合、step 5 的 STOP 判定輸入為 artifact 集合、來源允許清單的 dirty 子集與 spec sync 集合三者並新增獨立的 `### Spec Sync Changes` 區段、step 6 不提供 archive-first 選項、step 7 改讀封存目錄下的 proposal 與 tasks），以及 step 5 Source Files 區段在來源為 archive manifest 時的單一未分組呈現方式與時點快照標示。spec sync 判定的敘述 MUST 含「僅在 manifest 的 `specs_synced` 為 true 時適用、為 false 時所有 `openspec/specs/` 路徑一律留在 Unrelated」以及「目前 digest 為該檔案內容的 sha256 hexdigest」兩項。驗證目標：對該單一檔案逐一執行 `rg -F -- '<literal>' .claude/skills/cash-commit/SKILL.md`，任務 1.2 所列的十一個 cash-commit 字面句全部命中。
- [x] 3.2 將 3.1 的同一段內容寫入 `.agents/skills/cash-commit/SKILL.md`，使兩個變體在 invocation 前綴（`/cash-` 與 `$cash-`）正規化後逐字相同。驗證目標：對該單一檔案逐一執行 `rg -F -- '<literal>' .agents/skills/cash-commit/SKILL.md`，十一個 cash-commit 字面句全部命中；並執行 `scripts/cash-skills/tests/skill-checks.fish variant-parity` 通過。（整組 `codex-command-matrix` 尚含未修改的 cash-apply 斷言，其通過由任務 4.2 承接。）

## 4. cash-apply 封存指引改為提交優先

- [x] 4.1 在 `.claude/skills/cash-apply/SKILL.md` step 10 的 `**Archive guidance timing**` 區塊中，修改 `decision: passed` 那一條：保留「可以建議封存」的既有語意，並加上「先執行 `/cash-commit`，或改用 `cash-commit` 的 `Archive first, then commit together` 子流程」的指引，以及逐字包含 `deletes the touched state that` 的說明句；`decision: aborted` 那一條不得改動。驗證目標：對該單一檔案執行 `rg -F -- 'deletes the touched state that' .claude/skills/cash-apply/SKILL.md` 命中，且 `scripts/cash-skills/tests/skill-checks.fish grader-immutability` 通過，確認修改位於 grader 區塊之外。
- [x] 4.2 將 4.1 的同一段內容寫入 `.agents/skills/cash-apply/SKILL.md`（invocation 前綴改為 `$cash-`），使兩個變體正規化後逐字相同。驗證目標：執行 `scripts/cash-skills/tests/skill-checks.fish variant-parity`、`scripts/cash-skills/tests/skill-checks.fish codex-command-matrix` 與 `scripts/cash-skills/tests/skill-checks.fish grader-immutability`，三者皆通過。

## 5. Bundle 關卡與全量驗證

- [x] 5.1 讀取當下的 `cash-skills.version` 與 `git show HEAD:cash-skills.version`，將 `cash-skills.version` 寫為嚴格大於 HEAD 值的下一個 minor 版本（自現行 `2.3.1` 起為 `2.4.0`）。不得寫死常數而不比對 HEAD：同一 workspace 另一個進行中的 change `tolerate-versioned-legacy-guidance-marker` 也宣告要提升該檔，先落地時 HEAD 值會改變。理由：`archive.py` 與四個 `SKILL.md` 皆為 replaceable 檔案，`scripts/cash-skills/tests/test_bundle_version_history.py` 的 `check_history` 在 `cash-skills.version` 與 HEAD 相同時會逐檔比對其引入 commit 的 bytes，未提升即必然失敗。驗證目標：執行 `python3 scripts/cash-skills/tests/test_bundle_version_history.py` 通過。
- [x] 5.2 依 design.md C4 的 receipt 常規再執行一次 `./install-cash-skills.fish --self`，使 receipt 反映版本提升後的狀態。此為本變更的最後一次重建，MUST 在任務 5.1 的版本提升之後、任務 5.3 的回歸執行之前。驗證目標：`.cash-skills/bin/cash validate --all` exit 0。
- [x] 5.3 執行 `scripts/cash-cli/tests/cli-checks.fish all`、`scripts/cash-skills/tests/skill-checks.fish all`，以及 `.cash-skills/bin/cash validate guard-post-archive-commit-allowlist`，全部通過。
