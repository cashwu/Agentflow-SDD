## 1. Shim 與安裝入口

- [x] 1.1 [P] 實作 scripts/cash-shim/cash-shim.sh（POSIX sh）：互斥雙分支路由（C1）、dispatch 逐字透傳 `exec` absolute launcher（C2）、非 git／launcher 缺失 fail-closed 與 `cash-shim:` 單行 stderr 含 `cash init` 提示（C3）、init 的固定操作順序（旗標解析 → source 驗證 → git 初始化 → 印 target → exec installer）、git 目錄內部／bare repo 防護與 `--dry-run` 不觸發 git 初始化（C4）、installer 委派與 `--vendor`/`--target`/`--dry-run`/`--force` 旗標映射及未知旗標拒絕（C5）、`CASH_SOURCE_ROOT` 定位與兩個 source-layout 標記驗證（C6）、結果語意委派 installer（C7）、零環境 export（C9）。驗收：以 `sh -n` 通過語法檢查，且逐條對照 design Implementation Contract C1–C7、C9。
- [x] 1.2 [P] 實作 install-cash-shim.fish：realpath 自我定位、建立 `$HOME/.local/bin`、內容與 mode 一致時回報 `current` 零寫入、同目錄 temp file 加 atomic rename 寫入後回報 `installed` 並 chmod `0755`（結束前清除殘留 temp file）、非 regular file 目的地 fail closed、寫入範圍限定 `$HOME/.local/bin/` 之內（含建立該目錄與其必要父目錄）、兩個 informational 警告（`PATH` 缺目錄；`command -v cash` 解析非 `$HOME/.local/bin/cash` 的遮蔽警告）皆不改 exit code（C8）。驗收：連續執行兩次，第二次回報 `current` 且目的地 mtime 不變。

## 2. 測試

- [x] 2.1 實作 scripts/cash-shim/tests/shim-checks.fish，以暫存目錄 fixture 與 stub launcher／installer 覆蓋 C10 全部案例：dispatch 成功透傳（含帶空白引數與 `--json`）、零引數透傳、非 git fail-closed、launcher 缺失 fail-closed、init 於 fresh 目錄的 git 初始化與 installer 呼叫參數斷言、init 於 worktree 子目錄解析到 top-level、init 未知旗標拒絕（斷言未產生 `.git/`）、source-layout 標記驗證失敗（錯誤訊息含 `CASH_SOURCE_ROOT`，斷言未產生 `.git/`）、init 於 git 目錄內部／bare repo fail-closed、`cash init --dry-run` 於非 worktree 目錄 fail-closed 且零寫入、fresh `git init` fixture 以 `PYTHONPATH=.cash-skills/lib python3 -s -P` 直接呼叫真實 `cash_cli.installer.validate_target_prerequisites`（`allow_missing_config=True`）斷言不 raise、刪除 shim 後 fixture 專案的 `.cash-skills/bin/cash` 直接呼叫行為不變（deletion test）、install-cash-shim.fish 的 installed／current／fail-closed 三態。install-cash-shim.fish 案例 MUST 以覆寫 `HOME` 指向暫存 fixture 執行，並斷言真實 `$HOME/.local/bin/cash` 全程未被觸及。驗收：套件全綠，且任一案例失敗時整體非零 exit。

## 3. 文件

- [x] 3.1 [P] 在 CASH-SKILLS.md 新增全域 shim 章節：install-cash-shim.fish 安裝方式、`cash init` 流程（含 git 初始化 fallback、`--vendor` 預設與 `--dry-run` 需既有 worktree 的限制）、`CASH_SOURCE_ROOT` 設定、更新模型（source repo 更新後明確重跑，shim 無版本無背景行為）、shim 非 trust-bearing 的邊界說明（不進 manifest／receipt、不觸發 cash-skills.version 調升）、submodule 視為獨立專案的行為記載。驗收：章節涵蓋上述六點，且不修改文件中既有受管段落的語意。

## 4. 整合驗證

- [x] 4.1 端到端驗證：執行 fish scripts/cash-shim/tests/shim-checks.fish 全綠；建立暫存資料夾並先執行 git init，再以 `CASH_SOURCE_ROOT` 指向本 repo 執行 `cash init --dry-run`，確認回報 `Result: update` 預覽且該 worktree 除 `.git/` 外零寫入；於本 repo 內執行 `cash list --json` 確認透傳到 project launcher；暫時移開 `$HOME/.local/bin/cash` 後於本 repo 執行 `.cash-skills/bin/cash list --json` 確認 deletion test 成立，再還原 shim；執行 fish scripts/cash-skills/tests/skill-checks.fish 與 fish scripts/cash-cli/tests/cli-checks.fish 確認既有套件不受影響；確認 git status 顯示 `.cash-skills/manifest.tsv` 與 cash-skills.version 無變更。

## 5. Apply review remediation

- [x] 5.1 [P] 新增 `scripts/cash-shim/install_shim.py` 並將 `install-cash-shim.fish` 收斂為 realpath／Python 3.11+ probe／safe-path exec wrapper；helper 依 design D7／C11 以 held directory FD、`(st_dev, st_ino)` identity、no-follow `dir_fd` 相對操作實作 directory 建立、destination shape／content 檢查、owned temp create／cleanup 與 atomic replace。更新 `CASH-SKILLS.md` 說明 Python 3.11+ 前提。驗收：installed／current／non-regular fail-closed 維持通過；HOME leaf-swap fault injection 證明初始 `lstat` 與 opened FD identity mismatch 時 fail closed且原 HOME／外部零寫入；parent-swap fault injection 證明 identity mismatch fail closed、外部 sentinel／destination 零變更、owned temp 清除。
- [x] 5.2 [P] 修正 `scripts/cash-shim/cash-shim.sh` 的 inherited export 污染：所有 internal variables 改用 `_cash_shim_` reserved prefix，第一次賦值前清除同名 inherited export attribute，不改寫 caller 一般變數。於 `scripts/cash-shim/tests/shim-checks.fish` 新增 dispatch 與 init hostile inherited environment fixtures，斷言 caller 一般值維持不變且 exec 環境不含 shim 寫入的 reserved internal values。驗收：`sh -n` 與新增 fixtures 通過，逐條對照 C9。
- [x] 5.3 擴充 `scripts/cash-shim/tests/shim-checks.fish`，逐列執行 spec 的「引數逐字透傳」與「旗標映射」Examples：dispatch 包含 `search "portable manifest" --limit 10 --json`，init 分別驗證裸 `init`、裸 `--target`、`--dry-run`、`--target --force` 的完整 installer argv 與順序。將 task 4.1 已完成的 deletion-test deviation 固化為後續驗證 contract：真實 shim 存在時可暫移／還原，不存在時以隔離 `HOME` fixture 安裝、暫移、direct-launcher 驗證與還原，兩者驗證相同 deletion property 且真實 HOME 零變更。驗收：所有 exact-argv 與 deletion fixtures 通過。
- [x] 5.4 重新執行 `fish scripts/cash-shim/tests/shim-checks.fish`、`fish scripts/cash-skills/tests/skill-checks.fish`、`fish scripts/cash-cli/tests/cli-checks.fish` 與真實 installer dry-run E2E；逐項驗證 requirements「全域 shim 的指令路由」、「dispatch 分支的 fail-closed 行為」、「cash init 的 target 解析與 git 初始化」、「cash init 的 installer 委派與旗標映射」、「source repo 定位與驗證」、「shim 安裝入口的冪等安裝」、「shim 與 bundle inventory 的邊界」。確認唯一 machine-local destination 為 `~/.local/bin/cash`、必要父目錄寫入不超出 `~/.local`、不建立否決方案 `~/.config/fish/functions/`，且 `.cash-skills/manifest.tsv`、`cash-skills.version` 與 canonical `SKILL.md` 無變更；最後以 seeded apply review 驗證 apply-r1 bucket-1 findings 全部 resolved。
