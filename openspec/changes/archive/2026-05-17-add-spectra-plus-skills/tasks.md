<!--
parallel_tasks: true（取自 .spectra.yaml）。同組內無相依、不動同檔的任務標 [P]。
-->

## 1. Generator 骨架與 rules schema

- [x] 1.1 建立 `scripts/spectra-plus/` 目錄與 `generate.fish` 主程式骨架，落實 design 中「Generator 拆檔：script / template / rules」決策——主程式 generate.fish 僅做執行器、不嵌入業務文字（fish + yq 為實作語言，遵循 design 中「Generator 實作語言」決策）。完成時：執行 `scripts/spectra-plus/generate.fish --help` 印出用法摘要且 exit code 0，且 generate.fish 內無 review/rating 段落字串（grep 應為空）。驗證：手動執行該指令觀察輸出 + grep 檢查業務文字未洩漏到 script。
- [x] 1.2 [P] 撰寫 `scripts/spectra-plus/rules.yaml` 的初版內容，包含 `spectra-propose-plus` 與 `spectra-apply-plus` 兩個 entry，各含 `source` / `output` / `metadata` / `transformations`（遵循 design「rules.yaml schema」契約，落實「Rules.yaml controls plus skill divergence」需求）。完成時：對該檔執行 `yq '.' rules.yaml` 可正常 parse 且輸出非空。驗證：CLI parse 通過。
- [x] 1.3 [P] 在 `scripts/spectra-plus/template/` 下建立 `review-loop-block.md`（描述 sub-agent reviewer + rater 兩段、max 6 round、quality_score > 9、no critical gap 通過條件）與 `no-park-end-block.md`（描述 propose-plus 不執行 spectra park）兩份範本（對應 design「注入策略：marker-based section replacement」與「`spectra-propose-plus` 不 park」決策）。完成時：兩檔均存在且為非空 markdown。驗證：`ls scripts/spectra-plus/template/` 列出兩檔且 `wc -l` 皆大於 20。

## 2. Generator 行為實作

- [x] 2.1 在 `generate.fish` 內實作 rules.yaml 解析與欄位驗證，缺欄位時以 exit code 2 與明確錯誤退出（落實「Rules.yaml controls plus skill divergence」需求中 parse failure scenario）。完成時：故意刪除 rules.yaml 中 `source` 欄位重跑，stderr 印出對應欄位名稱且 exit code 為 2。驗證：故障注入測試。
- [x] 2.2 在 `generate.fish` 內實作 marker-based section 定位與 replace / append / remove 三種 operation（落實 design「注入策略：marker-based section replacement」決策）。完成時：對一份測試用 spectra skill 跑 generator 後，target_section 被預期的 template 取代。驗證：對輸出檔 diff 與預期內容比對。
- [x] 2.3 在 `generate.fish` 內實作「target_section 找不到」的 fail-fast 行為，落實「Plus skills generation pipeline」需求中 Target section not found scenario。完成時：故意改動 rules.yaml 中某 target_section 為不存在字串、重跑，generator 以 exit code 1 退出且 stderr 列出該 section 名稱與 source 路徑，目標 plus 檔未被部份覆寫。驗證：故障注入後檢查 exit code 與 stderr。
- [x] 2.4 在 `generate.fish` 內實作「template 檔不存在」的 fail-fast 行為，落實「Plus skills generation pipeline」需求中 Missing template file scenario。完成時：故意把 rules.yaml 指向不存在的 template 名稱、重跑，generator 以 exit code 3 退出並列出缺檔名稱。驗證：故障注入後檢查 exit code 與 stderr。
- [x] 2.5 在 `generate.fish` 內實作 frontmatter metadata 覆寫（落實「Generator-driven 而非 fork-and-sync」決策中 plus 與 spectra 的差異集中在 rules 的設計）。完成時：產出的 plus SKILL.md frontmatter 中 `name` / `description` 與 rules.yaml 內 `metadata` 區段一致。驗證：對輸出 SKILL.md 取 frontmatter 解析比對。
- [x] 2.6 在 `generate.fish` 內為每份輸出檔加上 `<!-- Generated by scripts/spectra-plus/generate.fish — DO NOT EDIT MANUALLY -->` 首行 marker（落實「Plus skills are not hand-edited」需求）。完成時：兩份 plus SKILL.md 開頭即為該 marker。驗證：對兩檔取首行比對字串相等。
- [x] 2.7 在 `generate.fish` 內實作「single-skill regeneration」CLI（傳入 plus skill 名稱只重產該檔）（落實「Plus skills generation pipeline」需求中 Single-skill regeneration scenario）。完成時：執行 `scripts/spectra-plus/generate.fish spectra-propose-plus` 僅 propose-plus 被改寫、apply-plus 檔案 mtime 不變。驗證：mtime 前後比對。
- [x] 2.8 驗證 generator 的 idempotency：連續兩次無變動執行應產生位元組相同的 plus SKILL.md（落實「Plus skills generation pipeline」需求中 Idempotent regeneration scenario）。完成時：兩次執行後 `diff` 輸出空。驗證：自動 diff 比對。

## 3. spectra-propose-plus skill 內容

- [x] 3.1 在 `scripts/spectra-plus/template/review-loop-block.md` 中明確描述「per-change 粒度、max 6 round、quality_score > 9、critical_gap == false 為通過條件、超過 6 round 寫 decision: aborted」並為 propose 路徑命名 propose-r<N>.md（落實「spectra-propose-plus quality gate」需求、「Review/rating/fix loop 粒度：per-change、max 6 round」與「Round md 檔輸出契約」決策）。完成時：模板 markdown 含上述條件且明確區分 propose 與 apply 命名。驗證：對模板做 grep "max 6"、"quality_score > 9"、"propose-r" 三項皆有命中。
- [x] 3.2 跑 generator 產出 `.claude/skills/spectra-propose-plus/SKILL.md`，確認其 spectra-propose-plus 行為符合 design 中「spectra-propose-plus 行為」契約——流程末段已移除原 inline self-review 與 analyze-fix loop、且**不**包含 `spectra park` 指令（落實「spectra-propose-plus quality gate」需求中 Park is never invoked scenario）。完成時：對輸出 SKILL.md `grep "spectra park"` 結果為空，且含 sub-agent review loop 段落。驗證：CLI grep 比對。

## 4. spectra-apply-plus skill 內容

- [x] 4.1 在 `scripts/spectra-plus/template/review-loop-block.md`（或為 apply 另建專屬 template，視 rules 設計需要）描述「所有 task 完成（tasks.md 全 [x]）才進入 review loop」、命名為 apply-r<N>.md（落實「spectra-apply-plus quality gate」需求中 Review loop runs after tasks complete scenario）。完成時：模板含此前置條件描述且 apply 命名規則明確。驗證：grep "tasks.md 全" 或對應英文與 "apply-r" 兩項命中。
- [x] 4.2 跑 generator 產出 `.claude/skills/spectra-apply-plus/SKILL.md`，確認流程在所有 task 完成後追加 review/rating/fix loop（落實「spectra-apply-plus 行為」決策）。完成時：對輸出 SKILL.md `grep "review/rating/fix"` 命中且該段落出現在 task 實作段落之後。驗證：CLI grep + 位置比對。

## 5. Sub-agent 契約與失敗處理

- [x] 5.1 在 review-loop-block 範本中明確要求每 round spawn 兩隻獨立 sub-agent（reviewer 一隻、rater 一隻），且 round 間不重用 sub-agent（落實「Fresh sub-agent per round」需求與「Sub-agent 分工：reviewer 與 rater 分離」決策）。完成時：模板描述含「fresh sub-agent」、「reviewer」、「rater」三個關鍵字且明確要求兩段獨立呼叫。驗證：grep 三個關鍵字皆命中。
- [x] 5.2 在 review-loop-block 範本中描述 sub-agent 失敗重試一次後仍失敗即 abort 整個 plus 流程的契約（落實「Sub-agent failure handling」需求）。完成時：模板明確描述「retry once / 連續兩次失敗 / abort」三個情境。驗證：grep 三組關鍵字皆命中。

## 6. Round md 檔格式

- [x] 6.1 在 review-loop-block 範本中固定 round md 檔 schema：路徑 `openspec/changes/<change>/reviews/<skill>-r<N>.md`、五個固定 section（Round heading H1、Reviewer Findings、Rating、Fix Actions、Decision），decision 值限 passed / next_round / aborted（落實「Round file output contract」需求與「Round md 檔格式（兩支共用）」決策）。完成時：模板明確列出五個 section 標題與 decision 三個合法值。驗證：grep 五個 section 標題與三個 decision 值皆命中。

## 7. End-to-end 與故障注入驗證

- [x] 7.1 對 generator 執行完整端對端驗證：清空 plus skill 目錄、跑 `scripts/spectra-plus/generate.fish`、確認兩份 plus SKILL.md 被產生且各自含 generator marker、frontmatter name 正確、不含 `spectra park`（綜合驗證 design「Generator script」契約所有 acceptance criteria）。完成時：自動化或半自動化測試 script 全綠。驗證：執行該測試 script。
- [x] 7.2 [P] 對 generator 執行故障注入回歸：分別注入 target_section 缺失、template 缺檔、rules 欄位缺失三種故障，驗證對應 exit code（1 / 3 / 2）與 stderr 內容（落實「Plus skills generation pipeline」與「Rules.yaml controls plus skill divergence」中所有 failure scenario）。完成時：三個案例皆以預期 exit code 退出且 stderr 含預期關鍵字。驗證：執行故障注入測試 script。
- [x] 7.3 [P] 驗證手改 plus skill 後重跑 generator 會覆寫（落實「Plus skills are not hand-edited」需求中 Generator overwrites hand edits scenario）。完成時：手動在 plus SKILL.md 加入一行特定字串、重跑 generator、`grep` 該字串應無命中。驗證：手動或 script 自動 grep。
