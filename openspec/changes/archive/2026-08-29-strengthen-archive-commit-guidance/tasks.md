## 1. 版本序位

- [x] 1.1 以 git show HEAD:cash-skills.version 推導新版本值（patch 位 +1，不寫死常數）並寫入 `cash-skills.version`，此 task 必須先於任何受守衛 SKILL.md 的修改（design D5 序位約束）；delivery: cash-skills.version；verification: manual assertion——比較工作樹 cash-skills.version 與 git show HEAD:cash-skills.version 的輸出，確認工作樹值嚴格領先且僅 patch 位遞增；regression: scripts/cash-skills/tests/skill-checks.fish（bundle-version history gate 於 task 3.2 全套執行）；success: 工作樹 cash-skills.version 內容為自 HEAD 值 patch +1 的單行版本號；red: N/A——本 change 記錄 tdd: false，且 history gate 的失敗態在受守衛檔案尚未修改前不可達（remaining-task：gate 由 task 3.2 的全套執行涵蓋）

## 2. Skill 內容（MUST 循序執行，不得平行——design D5 fail-closed 窗口約束）

- [x] 2.1 在 `.claude/skills/cash-apply/SKILL.md` 的「Archive guidance timing」段落內嵌 fenced 固定文案模板與「MUST 逐字輸出、唯二代換（change 名稱、invocation 前綴）」規範句，並加上模板語言 precedence 句（模板本體屬逐字保留內容，語言切換規則不適用於模板本體），保留既有 passed／aborted 分支與 deletes the touched state that 因果說明（design D1；對應 requirement：`cash-apply 封存指引須指引提交優先`），完成修改後、任何下一次 Cash CLI 呼叫（含本 task 結尾的 task done）之前，於 project root 執行 ./install-cash-skills.fish --self 重建 manifest 關閉 fail-closed 窗口（design D5）；delivery: .claude/skills/cash-apply/SKILL.md, .cash-skills/manifest.tsv；verification: manual assertion——grep 該檔確認模板 fenced 區塊、兩條路徑文句、唯二代換規範句與語言 precedence 句同時存在，且 --self 回報 Result: bootstrap；regression: scripts/cash-skills/tests/skill-checks.fish（task 3.2 全套執行，含 task 2.3 新增的 literal assertion）；success: 該段落含完整模板且兩條路徑與 touched state 警告逐字可 grep；red: N/A——tdd: false，無 red phase（分類：remaining-task——本 task 改變的是 skill workflow 的可觀察行為而非執行程式路徑，其驗證由本 task verification 的 manual assertion 與 task 3.2 的具名 literal assertion 全套執行承載）
- [x] 2.2 在 `.claude/skills/cash-archive/SKILL.md` 新增步驟 4b Uncommitted source guard，實作 design D2 的觸發條件（current 檔 top-level files 欄位／legacy 檔 touched 條目 files 聯集，與逐字完整指令 git status --porcelain=v1 -z --untracked-files=all 依 NUL-delimited 解析出的 dirty 路徑交集，rename／copy 條目新舊路徑皆計入）、legacy 路徑 `.spectra/touched/` 涵蓋、以「無法安全取得合法 path set」為判準且保留 CLI 實際 diagnostic（state_invalid／touched_invalid／legacy_touched_invalid）的 malformed fallback、git 指令 non-zero 或 NUL 解析失敗時的停止分支（MUST NOT 視同交集為空、報告原始錯誤、MUST NOT 呼叫 archive）、兩個互斥出口、靜默通過條件與 touched state 唯讀約束，不重編既有步驟編號（design D2；對應 requirement：`cash-archive 單獨封存前的未提交來源守門`），完成修改後、任何下一次 Cash CLI 呼叫之前執行 ./install-cash-skills.fish --self 重建 manifest（design D5）；delivery: .claude/skills/cash-archive/SKILL.md, .cash-skills/manifest.tsv；verification: manual assertion——grep 該檔確認 4b 步驟存在、含完整 git 指令與 NUL-delimited 解析句、legacy 聯集讀法、malformed 放行句（含三個 diagnostic 名）、偵測失敗停止句、Archive first, then commit together 導向與 MUST NOT 修改或刪除 touched state 的約束；regression: scripts/cash-skills/tests/skill-checks.fish（task 3.2 全套執行）；success: 步驟 4b 位於步驟 4 與步驟 5 之間且守門要素（觸發條件、legacy 聯集、malformed diagnostic、偵測失敗停止、兩出口、唯讀）逐字可 grep；red: N/A——tdd: false，無 red phase（分類：remaining-task——本 task 改變的是 skill workflow 的可觀察行為而非執行程式路徑，其驗證由本 task verification 的 manual assertion 與 task 3.2 的具名 literal assertion 全套執行承載）
- [x] 2.3 在 `scripts/cash-skills/tests/skill-checks.fish` 擴充 commit-before-archive guidance 區塊的 cash-apply 模板 literal（模板三段各取一個不含前綴的中文固定片段），並新增對兩個 cash-archive 變體的守門 literal assertion 區塊（步驟名 Uncommitted source guard、完整 git 指令 git status --porcelain=v1 -z --untracked-files=all、兩個選項標籤文句、靜默通過句、唯讀約束句、legacy 聯集讀法句、malformed 放行句含 state_invalid／touched_invalid／legacy_touched_invalid 三個 diagnostic 名、偵測失敗停止句、NUL 解析語意句含 rename／copy 新舊兩路徑皆計入），cash-apply 側另補模板語言 precedence 句 literal，所有 literal 不含 /cash- 或 $cash- 前綴且為新內容獨有、不得選既有內容已出現的字串（design D4）；delivery: scripts/cash-skills/tests/skill-checks.fish；verification: fish -n scripts/cash-skills/tests/skill-checks.fish 語法檢查通過；regression: scripts/cash-skills/tests/skill-checks.fish（task 3.2 全套執行使新 assertion 實跑）；success: fish -n 以 exit 0 結束、模板三段 literal 已入既有 commit-before-archive guidance 區塊、且守門專屬的新 assertion 區塊存在；red: N/A——tdd: false（分類：remaining-task——新 assertion 的實際失敗／通過由 task 3.2 的全套執行觀察）

## 3. 再生與 manifest（MUST 循序執行，不得平行——design D5 fail-closed 窗口約束）

- [x] 3.1 執行 scripts/cash-skills/generate.fish 自 `.claude` 來源再生 `.agents` 兩個變體，不手動編輯 .agents 檔（design D6），再生完成後、任何下一次 Cash CLI 呼叫之前執行 ./install-cash-skills.fish --self 重建 manifest（design D5）；delivery: .agents/skills/cash-apply/SKILL.md, .agents/skills/cash-archive/SKILL.md, .cash-skills/manifest.tsv；verification: manual assertion——generate.fish 以 exit 0 結束後，git diff 顯示兩個 .agents 檔更新且新內容（模板與 4b 守門）以 $cash- 前綴出現；regression: scripts/cash-skills/tests/skill-checks.fish（task 3.2 全套執行，含變體對等比較）；success: 兩個 .agents 檔含再生後的新段落且無其他非預期差異；red: N/A——tdd: false（分類：pure-refactor——再生為決定性轉換，無新行為）
- [x] 3.2 在 project root 執行 ./install-cash-skills.fish --self 作最終冪等確認（回報 Result: current，manifest 的 bundle_version 與四個 skill record digest 與工作樹一致），完成後執行全套 ./scripts/cash-skills/tests/skill-checks.fish；為使全套測試可通過，同步 `.cash-skills/lib/cash_cli/installer.py` 的 BUNDLE_VERSION 常數至新版本值，並把兩個 cash-archive SKILL.md 加入 `scripts/cash-skills/tests/test_live_namespace.py` 的 touched_allow（兩筆 deviation 詳見 implementation-notes 與 design D2／D5）；delivery: .cash-skills/manifest.tsv, .cash-skills/lib/cash_cli/installer.py, scripts/cash-skills/tests/test_live_namespace.py；verification: ./scripts/cash-skills/tests/skill-checks.fish 全套執行；regression: scripts/cash-cli/tests/cli-checks.fish；success: skill-checks.fish 全數通過（含 bundle-version history gate、變體對等比較與 task 2.3 新增的 assertion）；red: N/A——tdd: false（分類：remaining-task——此 task 即本 change 的最終驗證匯聚點，無更後續 red 載體）

## 4. Sol review 回饋（MUST 循序執行，不得平行——design D5 fail-closed 窗口約束）

- [x] 4.1 在 `.claude/skills/cash-archive/SKILL.md` 步驟 4b「取得 dirty 路徑」bullet 明寫：rename／copy 條目僅第一段帶狀態欄前綴，第二個 NUL field 是裸 old path，MUST NOT 對其剝除前綴（design D2；逐 field 套剝除規則會截掉舊路徑前三個字元，使 touched 檔被 rename 走時交集靜默漏判）；在 `scripts/cash-skills/tests/skill-checks.fish` 的 uncommitted-source guard 區塊加入對應的新內容獨有 literal（不含 /cash-／$cash- 前綴，design D4）；以 scripts/cash-skills/generate.fish 再生 `.agents/skills/cash-archive/SKILL.md`，完成修改後、任何下一次 Cash CLI 呼叫之前，於 project root 執行 ./install-cash-skills.fish --self 重建 manifest（design D5）；delivery: .claude/skills/cash-archive/SKILL.md, .agents/skills/cash-archive/SKILL.md, scripts/cash-skills/tests/skill-checks.fish, .cash-skills/manifest.tsv；verification: manual assertion——grep 兩個變體確認裸 old path 句與 MUST NOT 剝除句存在，且新 literal 已入 assertion 區塊並經 fish -n 語法檢查；regression: scripts/cash-skills/tests/skill-checks.fish 全套執行（含新 literal 實跑與變體對等比較）；success: 全套 skill-checks 通過且兩變體正規化後對等，裸 old path 規則逐字可 grep；red: N/A——tdd: false（分類：remaining-task——本 task 改變 skill workflow 文字的可觀察行為，其驗證由 manual assertion 與全套 skill-checks 承載）
