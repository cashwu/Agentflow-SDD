## 1. Test-first 契約 fixtures

- [x] 1.1 在 `scripts/cash-skills/tests/skill-checks.fish` 先建立會失敗的「Cash skill bundle version and target receipt」與「Stateless cross-project installer」fixtures，覆蓋 leading-zero rejection、major/minor/patch 與任意長度 numeric ordering、changed version 對 `HEAD` 與 same-version introduction commit（含後續 unrelated commit）治理、完整／損壞 receipt、equal-version source/receipt mismatch failure、首次安裝、無 receipt 接管、mixed legacy conflict、clean upgrade、equal、newer、drift、validation-safe `--force`、不降版、result/exit protocol、prior-receipt partial write、first-install persisted partial write 與 zero-write clean retry；以對未實作 source copy 執行 suite 時這些具名分支確實 fail，證明「使用單一來源版本與目標 receipt」、「版本優先序與首次接管」及「失敗模式」已有 regression oracle。
- [x] 1.2 在 `scripts/cash-skills/tests/skill-checks.fish` 接續建立會失敗的「Manual cash project registry」、「Version-aware cash skill batch updater」、「Cash installation has no repair automation」與「Installer and cleanup enforce filesystem boundaries」fixtures，使用 isolated `HOME`／targets 覆蓋 absent config/registry 的四種模式、register dedupe、existing/stale unregister、read-only list、所有模式的 malformed/read-error registry 零寫入、mutation input LF/control-character injection、persisted record retained-control-character rejection、unsafe HOME、symlink/containment、duplicate/missing targets、updated/current/newer/conflict/failed、equal-version source/receipt mismatch 必須為 failed、fail-and-continue、aggregate exit、dry-run parity、force 與無排程副作用；以 suite 在 updater 尚未存在時具名分支 fail，鎖定「使用者 registry 是手動維護的資料，不是排程」與「所有可寫路徑共用 fail-closed 邊界模型」。

## 2. 核心實作

- [x] [P] 2.1 在 `cash-skills.version` 建立初始 `1.0.0` bundle version，並擴充 `install-cash-skills.fish` 使 24-file inventory、任意長度 SemVer ordering、target-local `.cash-skills/receipt.tsv`、equal-version source integrity guard、version/drift precedence、atomic receipt publish、dry-run 與 `Result:`/exit semantics 符合「Cash skill bundle version and target receipt」及「Stateless cross-project installer」；以 1.1 全部 runtime fixtures 通過驗證「可觀察行為與介面」及 execution error 不被誤判。
- [x] 2.2 建立 `update-cash-skills.fish`，依「以穩定結果列連接 installer 與 batch updater」實作互斥 `--register`、`--unregister`、`--list`、`--all [--dry-run] [--force]`、first-use empty state、existing registry 全模式 validation、control-character rejection、atomic registry mutation、installer result delegation、逐 target statuses、fail-and-continue 與 aggregate exit，使「Manual cash project registry」、「Version-aware cash skill batch updater」及「Cash installation has no repair automation」成立；在 2.1 的 stable installer protocol 完成後，以 1.2 registry/updater fixtures 全部通過驗證，並確認沒有 LaunchAgent、background process、cache 或 lock。
- [x] 2.3 對 `install-cash-skills.fish` 與 `update-cash-skills.fish` 執行安全邊界整合，確保 no-config Fish、command-qualified tools、source repository rejection、receipt/registry temporary siblings、read/hash/child tri-state errors與所有 preflight 零寫入條件共同滿足「Installer and cleanup enforce filesystem boundaries」；以 hostile startup overrides、symlink escape、unreadable input、unexpected child result 與 dry-run fixtures 通過驗證「失敗模式」和「範圍邊界」。

## 3. 文件與完整驗證

- [x] [P] 3.1 更新 `CASH-SKILLS.md` 讓「Live documentation reflects cash ownership and cleanup」列出 direct installer、bundle bump 責任、receipt、registry 四種模式、batch statuses/exit、dry-run、force、無 receipt migration、one-time cleanup 順序與無背景排程保證；以文件內容 assertions 及人工 content review 驗證「使用者 registry 是手動維護的資料，不是排程」沒有被描述為 repair。
- [x] 3.2 擴充 `scripts/cash-skills/tests/skill-checks.fish` 的 final inventory/assertion matrix，使「Cash contract regression suite」對每個 spec scenario 與 design「驗收條件」至少有一個可獨立 fail-loud 的 fixture；加入 changed working version 對 `HEAD`、same-version current inventory 對該 version first-parent introduction commit 的 governance，並以包含後續 unrelated commit 的 isolated Git history 證明忘記 bump 持續 fail，再以 repository-wide grep 盤點 `1.0.0`、`cash-skills.version`、`.cash-skills/receipt.tsv`、registry path、status 與 result literals，驗證 version assertion inventory、完整具名 branch coverage 及 proposal 五個 affected-code paths 未漂移。
- [x] 3.3 執行 `fish scripts/cash-skills/tests/skill-checks.fish`、`spectra validate add-versioned-cash-skill-batch-update` 與變更檔案安全審查，確認全部 regression contracts 通過、dry-run 沒有 persistent effects、inventory 外 files 不受 `--force` 影響、既有 cleanup 契約未回歸，且「範圍邊界」只包含 `cash-skills.version`、`update-cash-skills.fish`、`install-cash-skills.fish`、`scripts/cash-skills/tests/skill-checks.fish`、`CASH-SKILLS.md`。

## 4. 單一 installer 入口整併

- [x] 4.1 在 `scripts/cash-skills/tests/skill-checks.fish` 先將「Manual cash project registry」、「Version-aware cash skill batch installation」、「Stateless cross-project installer」與「Installer and cleanup enforce filesystem boundaries」fixtures 改為只呼叫 `install-cash-skills.fish`，並新增 `update-cash-skills.fish` 不得存在與五種 mode 互斥的 fail-loud assertions；以現有雙 script 實作執行 suite 必須失敗，建立「以單一 installer 入口承載直接安裝與批次更新」的 regression oracle。
- [x] 4.2 將 registry parsing、atomic mutation 與 batch orchestration 合併進 `install-cash-skills.fish`，使 exactly one of `--target`、`--register`、`--unregister`、`--list`、`--all` 成立，`--dry-run`／`--force` 只可搭配 `--target` 或 `--all`，且 `--all` 重用單一專案安裝流程；移除 `update-cash-skills.fish`，並以 4.1 fixtures 驗證 direct、registry、batch、result/exit、fail-and-continue 與無背景排程行為。
- [x] 4.3 更新 `CASH-SKILLS.md` 的所有操作範例與 script inventory，使使用者只需記住 `install-cash-skills.fish`；執行 `fish scripts/cash-skills/tests/skill-checks.fish`、`spectra analyze add-versioned-cash-skill-batch-update --json` 與 `spectra validate add-versioned-cash-skill-batch-update`，驗證「可觀察行為與介面」、「驗收條件」及「範圍邊界」只交付單一 installer 入口。

## 5. 安裝時退役舊 plus skills

- [x] 5.1 在 `scripts/cash-skills/tests/skill-checks.fish` 先加入會失敗的 isolated target fixtures：四個 exact `.agents`／`.claude` `spectra-propose-plus`／`spectra-apply-plus` 目錄在首次安裝、upgrade、adoption 與 equal/current target 上都被列入 plan 並於正常執行後移除；equal/current-with-plus 回報 `Result: update`；`--dry-run`、newer、conflict 與 malformed/extra-entry/name-mismatch/symlink candidates 保持零移除、零 cash/receipt write；non-plus Spectra skills 與 legacy 目錄外 sentinel 維持 byte-identical。以尚未具備 cleanup 的 installer 執行 suite 必須在具名 plus removal assertion fail，建立 destructive-boundary regression oracle。
- [x] 5.2 依 design「成功安裝會清除可辨識的 retired plus skills」擴充 `install-cash-skills.fish` 的 target preflight 與 update plan，盤點四個 exact retired plus paths，只接受「non-symlink directory + 唯一 non-symlink regular readable/writable `SKILL.md` + matching frontmatter name」；在所有 cash、receipt、legacy boundary 與 write permissions 通過後，讓 install/adopt/upgrade/repair/current-with-plus 使用 non-recursive `rm SKILL.md` + `rmdir` 完成 cleanup，dry-run 只列 plan，newer/conflict 不移除，並以 5.1 fixtures 驗證 fail-closed 與 batch `would-update`／`updated` propagation。
- [x] 5.3 更新 `CASH-SKILLS.md` 說明 installer 會移除的四個 retired plus 目錄、未知內容拒絕規則及與 `uninstall-spectra-plus-repair.fish` 排程清理的分工；執行 `fish scripts/cash-skills/tests/skill-checks.fish`、`spectra analyze add-versioned-cash-skill-batch-update --json`、`spectra validate add-versioned-cash-skill-batch-update` 與 `git diff --check`，確認文件、單一 target、batch、dry-run、版本優先序、安全邊界及 legacy cleanup 契約一致。
