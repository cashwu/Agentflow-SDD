## 1. Implementation

- [x] 1.1 依 D7 把版本調升排為第一個動作：將 `cash-skills.version` 與 installer 的 `BUNDLE_VERSION` 常數同步調升為嚴格較大版本（現值均為 `2.21.0`），在任何受 guard 的 runtime 編輯之前完成；並在同一個 task 內執行 source-only 發佈——`installer.py` 是 portable manifest 的 runtime record 並綁定 digest，只調升 `BUNDLE_VERSION` 而不發佈會使整支 CLI 立即以 `manifest_invalid` 失效，連 `cash task done` 都無法執行；delivery: cash-skills.version, .cash-skills/lib/cash_cli/installer.py, .cash-skills/manifest.tsv；verification: `python3 scripts/cash-skills/tests/test_bundle_version_history.py`；regression: 發佈後執行一次 `.cash-skills/bin/cash list --json`，確認 manifest 重新發佈後 launcher 的 portable gate 通過、CLI 可用——`installer.py` 是 manifest 的 runtime record，本 task 改動其 `BUNDLE_VERSION` 常數並重新發佈，該路徑不在 primary target 的涵蓋範圍內；success: 該測試通過且回報的目前版本嚴格大於 first-parent history 中的引入版本；red: N/A，本 task 是發佈前置的版本調升，不交付可觀察行為，且現況兩處版本值一致、primary target 通過，不存在先行失敗狀態，屬 remaining-task 分類
- [x] 1.2 為結構類 gate 與唯讀性寫下 failing tests，涵蓋 single-change 有／無 `--json` 時 stdout 形狀、stderr 為空、pass exit `0`／gate fail exit `2`，以及：只有檔名符合 `<skill>-r<N>.md` 者納入判定而 `loop-ledger.tsv` 與 `accepted-risks.md` 不使其失敗、四 section 齊全、`## Decision` 值域、`propose` 與 `apply` 兩序列各自從 1 起算、序列缺少起始編號 `r1` 判 `fail`、中間編號缺號判 `fail`、`## Decision` 值之後接 rationale 段落仍判 `pass`、`` - `round_type`：`micro` `` 這種 backtick 加全形冒號的形狀可被解析、檔名符合樣式但內容不可解析時 `round_file_schema` 判 `fail` 且其餘 gate 仍回報、`## Rating` 缺 `round_type` 或值域外判 `fail`、run 第一輪為 `full` 而第一輪之後當且僅當第四輪為 `full`、執行前後排除 `.git/` 的工作區逐位元組不變且不建立目錄、在 portable-manifest target 上不產生 `__pycache__`、在 receipt-based target 上比較範圍另排除 `__pycache__/` 與 `*.pyc` 後仍逐位元組不變且新產生的 `__pycache__` 不使判定失敗、額外位置參數以 `unknown_command` 或 `invalid_arguments` 失敗而非靜默忽略；實作當時 `openspec/changes/add-host-derived-round-lint/reviews/` 下全部符合 `<skill>-r<N>.md` 樣式的 round file MUST 複製為 `scripts/cash-cli/tests/fixtures/lint_round/` 下的靜態 fixture 並列為必須判 `pass` 的 case——測試 MUST 讀取該靜態 fixture 目錄而 MUST NOT 讀取 `openspec/changes/` 下的 live 路徑（`cash archive` 會移走該目錄），且 fixture 集合為空時測試 MUST 失敗而非 vacuously pass；清單以實作當時的目錄內容為準，MUST NOT 釘住特定編號，否則每次新增 round file 都會使該清單過期並漏掉最新的合規檔案；另涵蓋 `## Rating` 內符合 `round_type` 擷取條件的 bullet 多於一筆時判 `fail`、前一輪 `## Decision` 不可解析時其後各輪 `round_type_position` 判 `fail`；delivery: scripts/cash-cli/tests/test_lint_round.py, scripts/cash-cli/tests/fixtures/lint_round/；verification: `python3 -m pytest scripts/cash-cli/tests/test_lint_round.py`；regression: N/A，本 task 只新增測試檔案且不改動任何 runtime 或既有測試，primary target 已涵蓋新增內容的完整相關範圍；success: pytest 蒐集到全部新增 case 且無 collection error；red: 全部新增 case 因 `lint-round` 尚未註冊而以 `unknown_command` 失敗
- [x] 1.3 實作 single-change mode 的結構類三個 gate、唯讀性保證及 D8 的 single-change 輸出與 exit code，於 `COMMANDS` 與 `commands/execute` 註冊 `lint-round`，並依 D7 的既定順序擴充 installer runtime inventory、新增 runtime 檔案，最後執行 source-only 發佈以更新 portable manifest；delivery: .cash-skills/lib/cash_cli/commands/lint_round.py, .cash-skills/lib/cash_cli/main.py, .cash-skills/lib/cash_cli/commands/__init__.py, .cash-skills/lib/cash_cli/installer.py, .cash-skills/manifest.tsv；verification: `python3 -m pytest scripts/cash-cli/tests/test_lint_round.py`；regression: `fish scripts/cash-cli/tests/cli-checks.fish`，因為 dispatch table 與 help 表面的既有契約由該套件治理；success: 1.2 的全部 case 通過；red: 1.2 的全部 case 在 `lint_round.py` 存在之前以 `unknown_command` 失敗
- [x] 1.4 為活動判定與 grader_immutability 寫下 failing tests，涵蓋：逐 skill 取最高編號、`propose-r3` 為 `passed` 而 `apply-r1` 為 `next_round` 時 MUST NOT `skip`、全部 skill 皆終結時 `skip`、無 round file 時全部 `skip` 且整體通過、列舉排除 `archive` 與 `.parked` 保留目錄並涵蓋 `.parked/` 下的 parked change、`openspec/changes/` 下存在非目錄項目（如 `.DS_Store`）或名稱不符 `[a-z][a-z0-9-]*` 的項目時不被列舉且不進入 fail-open、single-change mode 對只被另一個 active change 宣告的受保護路徑判 `pass`（與 `--hook` 同取聯集）、未宣告即修改受保護路徑判 `fail`、已 `git add` 但未 commit 的受保護路徑仍判 `fail`、已在 proposal `## Impact` 宣告者判 `pass`、只出現在 `tasks.md` delivery target 而未出現在 `## Impact` 者亦判 `pass`、parked change 納入列舉但不計入 active 判定且其 `grader_immutability` 為 `skip`、A 已 `passed` 或 `aborted` 但尚未封存或 parked、修改尚未提交且 B 仍 active 時，A 的宣告仍解除 B 對該路徑的判定；只被已封存或 parked change 宣告者仍判 `fail`、只有 `proposal.md` 時仍解析其 `## Impact`，只有 `tasks.md` 時仍解析其 delivery target，兩份皆缺少時才貢獻空聯集，皆不因缺檔進入 fail-open、僅出現在 `design.md` 散文中的受保護路徑不構成宣告、`openspec/specs/` 目錄型宣告涵蓋其下 master spec 檔案、未被任何被列舉 change 宣告的 master spec 改動判 `fail`、某受保護路徑只被 change A 宣告時 change B 的判定 MUST NOT fail；delivery: scripts/cash-cli/tests/test_lint_round.py；verification: `python3 -m pytest scripts/cash-cli/tests/test_lint_round.py -k immutability`；regression: `python3 -m pytest scripts/cash-cli/tests/test_lint_round.py`，因為新增 case 與 1.2 的結構類 case 共用同一組 fixture 與 command 進入點；success: 該過濾條件下的全部新增 case 被蒐集且無 collection error；red: 全部新增 case 因 `grader_immutability` gate 尚未實作而失敗
- [x] 1.5 實作依 D3 的逐 skill 活動判定與列舉規則、依 D4 的三方比對（受保護集合只取路徑成分且 `openspec/specs/` 為目錄型、變更集合涵蓋 staged 與 untracked、structured scope declarations 只取自 proposal `## Impact` 與 tasks.md delivery target、兩種 mode 的涵蓋判定都取未封存且未 parked 的被列舉 change 宣告聯集，來源 change 即使已 `passed` 或 `aborted` 仍保留宣告，逐檔解析 proposal/tasks 且缺檔僅使該份貢獻空集合、列舉限目錄型且名稱符合 `[a-z][a-z0-9-]*`、parked change 納入列舉但不計入 active），以本 change 自身的宣告與散文複製為 `scripts/cash-cli/tests/fixtures/lint_round/` 下的靜態 fixture 作為 R4 指定的雙向 fixture（MUST NOT 讀取 live 路徑），並在 runtime 位元組改變後重新發佈 manifest；delivery: .cash-skills/lib/cash_cli/commands/lint_round.py, .cash-skills/manifest.tsv；verification: `python3 -m pytest scripts/cash-cli/tests/test_lint_round.py -k immutability`；regression: `python3 -m pytest scripts/cash-cli/tests/test_lint_round.py`，因為活動判定改變了結構類 gate 之外的共用判定路徑；success: 1.4 的全部 case 通過；red: 1.4 的全部 immutability case 在 D3 的活動判定與 D4 的三方比對實作之前失敗
- [x] 1.6 為 `--hook` mode 寫下 failing tests，依 D8 涵蓋有／無 `--json` × 首次／重入 × 通過／gate 失敗的 stdout、stderr、exit code 矩陣，重入失敗的 JSON 必須保留 `ok: false`，另涵蓋有／無 `--json` 的 fail-open 均 stdout 為空、exit `1` 且 stderr 含 `gate_unavailable`；以下靜默預期僅適用於無 `--json` 的呼叫：判定失敗時 exit `2` 且 stderr 含失敗 gate 的 `id`、無 round file 時 exit `0` 且無輸出、standard input 的 JSON payload 無法解析時 exit `1` 且 stderr 含 `gate_unavailable`、workspace 解析失敗時 exit `1` 且 stderr 含 `gate_unavailable`、任何 fail-open 分支都不以 exit `0` 結束、`stop_hook_active` 為真且有未解決失敗項時仍執行判定並以 exit `1` 於 stderr 列出該等失敗項、`stop_hook_active` 為真且無失敗項時 exit `0` 且無輸出；CLI 缺席、launcher 信任 gate 失敗與 Python 版本不足依 R9 不在測試範圍；delivery: scripts/cash-cli/tests/test_lint_round.py；verification: `python3 -m pytest scripts/cash-cli/tests/test_lint_round.py -k hook`；regression: `python3 -m pytest scripts/cash-cli/tests/test_lint_round.py`，因為 `--hook` mode 與 single-change mode 共用同一組 gate 實作與活動判定；success: 該過濾條件下的全部新增 case 被蒐集且無 collection error；red: 全部新增 case 因 `--hook` mode 尚未實作而失敗
- [x] 1.7 實作 `--hook` mode 與 D8 輸出矩陣（JSON 在正常完成判定時一定輸出，且不改變重入 exit `1` 的規則）：自 standard input 讀取 host 供應的 Stop hook JSON payload、依 `stop_hook_active` 放行但仍執行判定並在有未解決失敗項時以 exit `1` 輸出、依 D3 列舉檢查對象（限目錄型且名稱符合樣式）、依 D6 分離阻擋（exit `2`）與 fail-open（exit `1`）兩層語意且進入點之後的每個 fail-open 分支以 exit `1` 輸出 `gate_unavailable`、MUST NOT 有任何分支以 exit `0` 搭配 stderr 診斷結束，並在 runtime 位元組改變後重新發佈 manifest；delivery: .cash-skills/lib/cash_cli/commands/lint_round.py, .cash-skills/manifest.tsv；verification: `python3 -m pytest scripts/cash-cli/tests/test_lint_round.py -k hook`；regression: 依 R2 對 `openspec/changes/` 下每個既有 change 執行 `.cash-skills/bin/cash lint-round <change> --json` 並確認 `ok` 皆為 `true`，避免掛上 hook 後既有歷史 round files 阻擋無關的 turn；success: 1.6 的全部 case 通過；red: 1.6 的全部 hook case 在 `--hook` mode 實作之前失敗
- [x] 1.8 在 `.claude/settings.json` 的 `Stop` 事件新增一筆 command hook，其 command 為 `"$CLAUDE_PROJECT_DIR"/.cash-skills/bin/cash lint-round --hook`，並依 R9 宣告 host 層的 `timeout`，既有設定鍵逐位元組保留；delivery: .claude/settings.json；verification: 以 `python3 -c` 解析 `.claude/settings.json` 並斷言其為合法 JSON、既有鍵保留、`Stop` 事件下存在該 command hook 條目且該條目宣告 `timeout`；regression: `python3 -m pytest scripts/cash-cli/tests/test_lint_round.py -k hook`，因為 settings.json 指向的正是該 mode 的進入點；success: 該斷言全部成立；red: 該 `Stop` hook 條目新增之前，`.claude/settings.json` 不含該條目，斷言失敗
- [x] 1.9 依 `lint-round 輸出與結束碼` requirement，將 `lint-round` 的 dispatch、shared-lock 分類、help 清單涵蓋與兩種 mode 的 JSON shape、single-change gate fail exit `2` 與 hook 首次／重入及 JSON／文字輸出矩陣納入 CLI 契約套件治理；delivery: scripts/cash-cli/tests/cli-checks.fish；verification: `fish scripts/cash-cli/tests/cli-checks.fish`；regression: `fish scripts/cash-skills/tests/skill-checks.fish`，因為新增 runtime 檔案與版本調升改變了 manifest 與 installer 的 runtime inventory，該套件治理 bundle 版本與 installer 分支；success: cli-checks 全數通過且輸出含 `lint-round` 相關檢查；red: N/A，本 task 為 1.3 至 1.8 已通過的行為補上契約層斷言，不存在先行失敗狀態，屬 remaining-task 分類
