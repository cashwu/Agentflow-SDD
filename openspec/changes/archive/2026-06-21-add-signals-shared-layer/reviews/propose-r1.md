# Propose Plus Review — Round 1

## Reviewer Findings

### Critical

（無）

### Warning

- **severity**: Warning / **confidence**: 100 / **location**: tasks.md §1–§5 vs. `specs/signals-shared-layer/spec.md` Requirement: "Signals shared layer location and file schema" / 來源：A
  - **summary**: `signals-shared-layer` 的「location and file schema」requirement 沒有任何 task 將其列為具名驗證目標；1.1 只引用 README 與 status-lifecycle requirement，2.1 只引用 `spectra-plus-skills` 的寫入 requirement。
  - **recommendation**: 在實作該 schema 的 task（1.1 文件化 schema、2.1 寫入符合 schema 的檔案）上補上對該 requirement 的具名驗證引用，使每條 spec requirement 都有具名 backing task。

- **severity**: Warning / **confidence**: 100 / **location**: tasks.md §4.1 vs. design.md Implementation Contract「Interface」 / 來源：A
  - **summary**: task 4.1 本體要求「頂端 `DO NOT EDIT MANUALLY` 標記不變」，但其驗證只 grep 步驟存在與否，未斷言該 marker 仍存在。
  - **recommendation**: 擴充 4.1 驗證，grep 四個重新生成檔含 `<!-- Generated by scripts/spectra-plus/generate.fish — DO NOT EDIT MANUALLY -->`。

- **severity**: Warning / **confidence**: 80 / **location**: design.md「Signal 寫入的觸發條件與去重」+ `specs/signals-shared-layer` schema requirement + `specs/spectra-plus-skills` 寫入 requirement / 來源：B
  - **summary**: `<slug>` 如何從 finding 推導從未定義，但 slug 是 condition (a)/(b) 建檔與比對的去重鍵；缺此契約則 create-vs-update 非決定性。
  - **recommendation**: 在 design、spec 與寫入模板定義可重現的 slug 推導規則（normalized `location + summary` 類別轉 kebab-case），作為去重識別。

### Suggestion

- **severity**: Suggestion / **confidence**: 75 / **location**: design.md「Signal 寫入的觸發條件與去重」condition (a) / 來源：B（原 Warning 70 降級）
  - **summary**: 「跨 ≥2 rounds 反覆出現」在健康 loop 幾乎不觸發（被修好的 finding 不會再現），使 signal 幾乎只在 aborted/卡住的 loop 累積；又因 condition (b) 依賴既有 signal，整個跨 change 複利機制可能形同失效。
  - **recommendation**: 改為「loop 中任一 round 出現過的 post-filter Critical/Warning，依 finding class 去重後皆記錄」，讓被修好的 finding 也成為 signal，使 condition (b) 跨 change 比對得以真正運作。

- **severity**: Suggestion / **confidence**: 75 / **location**: `specs/spectra-plus-skills` Scenario「apply-plus does not gain the read step」+ tasks.md §4.2 / 來源：B（原 Warning 降級）
  - **summary**: 「apply-plus 不含讀取步驟」與「含寫入步驟」的斷言缺少明確唯一 sentinel 字串，測試可能寫成脆弱 substring 而 vacuous pass。
  - **recommendation**: 在 `signals-read-block.md` 與寫入步驟各定義唯一標記字串，要求 `generator-checks.fish` 對 propose-plus `assert_contains`、對 apply-plus `assert_not_contains` 讀取標記。

- **severity**: Suggestion / **confidence**: 75 / **location**: design.md Implementation Contract「Acceptance criteria」vs. tasks.md / 來源：A（conf 75 降級）
  - **summary**: contract 定義的手動端到端驗收（跑含反覆 finding 的 loop 產生 signal、另一 change 比對遞增 occurrences）無對應 task。
  - **recommendation**: 於 §5.1 補一條手動驗收檢查，或在 design 註明該手動驗收為 advisory 非 gating。

- **severity**: Suggestion / **confidence**: 60 / **location**: design.md「比對既有 signal」risk row + spec condition (b) / 來源：B
  - **summary**: design 承諾模板會有「明確比對 rubric」，但無任何 rubric 內容，去重品質完全延後到實作且無驗收檢查。
  - **recommendation**: 在 design 勾勒比對 rubric，或加 task 驗證寫入模板含具體比對規則。

- **severity**: Suggestion / **confidence**: 55 / **location**: design.md Risks「跨 change 並發寫同一 signal」 / 來源：B
  - **summary**: last-writer-wins 被描述為僅 `occurrences` 計數誤差，實際全檔覆寫會丟失整筆 `## Occurrences` 與 `links`，create-create race 還可能丟掉整個檔。
  - **recommendation**: 據實改寫並發 trade-off 描述（會丟失整筆紀錄而非僅計數），維持不加鎖（Simplicity First）。

- **severity**: Suggestion / **confidence**: 50 / **location**: tasks.md §3.2 / §4.2 排序 / 來源：A
  - **summary**: 3.2 的驗證依賴 generator-checks 斷言，但該斷言於 4.2 才加入，3.2 驗證在斷言存在前未被實際執行。
  - **recommendation**: 將 4.2 排序至 3.2 之前或合併，或把 3.2 驗證改為手動 grep 直到斷言存在。

- **severity**: Suggestion / **confidence**: 50 / **location**: `specs/spectra-plus-skills` 讀取 requirement vs. 既有 master spec「Rules schema valid」 / 來源：A
  - **summary**: 新 append transformation 與既有 rules.yaml `append`+`template` schema 相容，屬 near-miss 非違規。
  - **recommendation**: 無須行動；可選在 design 加一行註明新 transformation 符合既有 schema 場景。

## Rating

- 存活 `Critical` 數（post-filter）：0
- 存活 `Warning` 數（post-filter）：3
- `critical_gap`: false
- rationale：套用 confidence filter 後存活三條 `confidence >= 80` 的 Warning（A1=100、A2=100、B1=80），均為真實且廉價可修的 artifact 缺口（schema requirement 無具名 task、4.1 驗證漏 marker 檢查、slug 推導未定義）。依機械規則：無 Critical、但有存活 Warning → `next_round`。另有多條 Suggestion，其中 B 提出的觸發條件邏輯死結（降級後 conf 75）雖不強制 next_round，但屬會使功能失效的設計問題，於本輪一併修復。

## Fix Actions

本輪將於 next_round 前修復下列項目（含三條 Warning 與數條高價值 Suggestion）：

1. （A1）tasks.md：於 1.1 與 2.1 補上對 `signals-shared-layer`「Signals shared layer location and file schema」requirement 的具名驗證引用。
2. （A2）tasks.md §4.1：驗證加入對 `DO NOT EDIT MANUALLY` marker 的 grep。
3. （B1）design.md / 兩份 spec / tasks：定義 `<slug>` 推導規則（normalized `location + summary` → kebab-case）作為去重鍵。
4. （B3，高價值）proposal.md / design.md / 兩份 spec / tasks 2.1：改寫觸發條件為「loop 中任一 round 出現過的 post-filter Critical/Warning，依 finding class 去重後皆記錄」，修正跨 change 複利死結。
5. （B2）tasks 3.1/4.2 與 spec 場景：定義 read/write 唯一 sentinel 標記並要求 generator-checks 具名斷言。
6. （B4）design.md：勾勒比對 rubric。
7. （B5）design.md Risks：據實改寫並發 trade-off。
8. （A3/A5）tasks.md：§5.1 補手動驗收；調整 3.2/4.2 排序敘述。

## Decision

next_round
