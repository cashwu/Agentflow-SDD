## 1. Signals 層基礎與 schema 文件

- [x] 1.1 [P] 建立 `openspec/signals/README.md`，使其作為 signals 共享層的 contract：說明收錄/不收錄規則、frontmatter schema（`id`、`type`、`status`、`occurrences`、`first_seen`、`last_seen`、`links`）、`<slug>` 為 writer 指派的語意 issue-class 識別碼（ASCII kebab-case，符合 `^[a-z0-9]+(-[a-z0-9]+)*$`，非 finding 文字機械轉換）、新增/更新流程（含 coin 新 slug 前先列舉既有 `openspec/signals/*.md` 挑未存在 slug、建檔不覆寫既有檔），並明示 `addressed` / `dismissed` status 轉換為人工操作、以及並發全檔覆寫可能遺失整筆 occurrence/links 或整個新建 signal 的 lost-entry 風險與「occurrence 描述明顯不相關時人工拆分」指引。驗證：content review 確認 README 含收錄/schema/slug 指派與列舉不覆寫/新增流程內容、明文敘述 status 由人維護、且註明 lost-entry 風險與拆分指引（對應 spec `Signals shared layer location and file schema`、`Signals directory README contract`、`Signal status lifecycle is human-maintained`）。

## 2. Review loop 寫 signal（共用模板）

- [x] 2.1 [P] 在 `scripts/spectra-plus/template/review-loop-block.md` 新增帶唯一 sentinel `<!-- SIGNALS-WRITE-STEP -->` 的「寫 signal」步驟，行為為：loop 結束（`decision` 為 `passed` 或 `aborted`）且機械決策已寫入後，對**任一 round** 通過 confidence filter 的 `Critical` / `Warning` finding，先依 issue class 去重（此寫入集去重採 issue class，獨立於模板既有 line-22 per-round `location + summary` 聚合去重，不可混用）；對每個 class，main agent 讀 `openspec/signals/` 既有 signals 並以 rubric（同 capability/domain + 同 rule/anti-pattern 為同 class，不同根因為不同 class，不確定時偏向建新）比對：命中既有 `open` 同 class signal 則沿用其 slug 就地更新（遞增 `occurrences`、更新 `last_seen`、append `## Occurrences` 與 `links`、不改 `status`），否則先列舉 `openspec/signals/*.md` coin 一個未被佔用的語意 slug 建新 signal（`status: open`、`occurrences: 1`）、建檔不覆寫既有檔；僅命中 addressed/dismissed 同 class 時亦 coin 未被佔用新 slug 建新 open signal、不覆寫人工 status；`Suggestion` 與 `confidence < 80` 不寫入；寫入不改變 round file 的 `decision`；寫入失敗只警告不使 workflow 失敗。驗證：content review 確認模板含 `<!-- SIGNALS-WRITE-STEP -->` 標記、涵蓋「任一 round」觸發、issue-class 去重（獨立於 line-22 聚合去重）與比對 rubric、coin slug 前列舉避免覆寫、addressed/dismissed 不覆寫、status 不變、Suggestion/低信心排除與失敗處理，且明示對 `signals-shared-layer` schema（含 slug 指派）的符合（對應 spec `Plus review loop writes signals after the loop ends` 與 `Signals shared layer location and file schema`）。

## 3. propose-plus 讀 signal（專屬模板與 transformation）

- [x] 3.1 [P] 新增 `scripts/spectra-plus/template/signals-read-block.md`，以唯一 sentinel `<!-- SIGNALS-READ-STEP -->` 開頭並自成一個獨立的 bolded 步驟標題（不與前一步「Scan existing specs for relevance」的 IMPORTANT 區塊融成連續散文）。行為為：`spectra-propose-plus` 在該步驟後讀取 `openspec/signals/` 的 open signals，列出相關者作為資訊性排序摘要；不阻擋流程、不要求確認、不修改 signal；資料夾不存在或無 open signal 時靜默繼續。驗證：content review 確認模板含 `<!-- SIGNALS-READ-STEP -->` 標記、為獨立步驟、敘述為 informational 且涵蓋無 signal 時靜默繼續（對應 spec `spectra-propose-plus reads open signals for prioritization`）。
- [x] 3.2 在 `scripts/spectra-plus/rules.yaml` 的 `spectra-propose-plus` 項下新增一筆 transformation，將 `signals-read-block.md` 以 `append` 接到 `target_section` 為 `3. **Scan existing specs for relevance**` 之後；不在 `spectra-apply-plus` 加入此讀取步驟。驗證：手動 grep base 確認 `target_section` 字串於 `.claude/skills/spectra-propose/SKILL.md` 與 `.agents/skills/spectra-propose/SKILL.md` 逐字存在；待 4.2 斷言加入後，`fish scripts/spectra-plus/tests/generator-checks.fish` 通過（對應 spec `spectra-propose-plus reads open signals for prioritization`）。

## 4. 重新生成與測試斷言

- [x] 4.1 更新 `scripts/spectra-plus/tests/generator-checks.fish`，以 sentinel 標記新增斷言：生成後兩個 plus skill 皆含 `<!-- SIGNALS-WRITE-STEP -->`、propose-plus 兩變體含 `<!-- SIGNALS-READ-STEP -->`、apply-plus 兩變體不含 `<!-- SIGNALS-READ-STEP -->`、且四檔頂端 `<!-- Generated by scripts/spectra-plus/generate.fish — DO NOT EDIT MANUALLY -->` 標記仍在。驗證：`fish scripts/spectra-plus/tests/generator-checks.fish` 通過。
- [x] 4.2 執行 `scripts/spectra-plus/generate.fish` 重新生成四個 plus skill，使 propose-plus 兩變體含讀取與寫入步驟、apply-plus 兩變體含寫入步驟但不含讀取步驟，且頂端 `DO NOT EDIT MANUALLY` 標記不變。驗證：grep 四個生成檔確認 `<!-- SIGNALS-WRITE-STEP -->`、`<!-- SIGNALS-READ-STEP -->`（依各檔應含與否）與 `DO NOT EDIT MANUALLY` 標記，並重跑 `fish scripts/spectra-plus/tests/generator-checks.fish` 通過。
- [x] 4.3 更新 `SPECTRA-PLUS.md`，記錄 signals 共享層的用途、frontmatter schema、`<slug>` 推導、read/write 時機、人工 status 規則與 lost-entry 並發風險。驗證：content review 確認文件涵蓋上述項目且與 `openspec/signals/README.md` 一致。

## 5. 最終驗證

- [x] 5.1 跑 `fish scripts/spectra-plus/tests/generator-checks.fish` 與 `spectra validate add-signals-shared-layer` 全數通過。驗證：兩個命令 exit code 0。手動端到端驗收（advisory，非 gating）：跑一次含早期 round 即被修好之 finding 的 plus loop 產生 signal 檔，之後另一含同 class finding 的 loop 使同 slug signal 的 `occurrences` 遞增。
