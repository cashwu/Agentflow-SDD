## 1. Runtime foundation

- [x] 1.1 在 `.cash-skills/bin/cash`、`.cash-skills/lib/cash_cli/__init__.py`、`.cash-skills/lib/cash_cli/main.py` 與 `.cash-skills/lib/cash_cli/errors.py` 實作 **Project-local Python standard-library runtime**、**Project-local Cash CLI runtime**、**Cash CLI 只實作被 workflow 消費的 command surface**、**Cash workflow command surface**及**統一 JSON 與錯誤契約**，交付Python 3.11+自定位launcher、明確dispatch、stable JSON與exit 0/1/2分類；以`cli-checks.fish runtime-and-errors`驗證supported/unknown commands、Unicode JSON與Spectra binary零呼叫。
- [x] 1.2 新增空的`0644` `.cash-workspace.lock`，並在stable `.cash-skills/bin/cash` bootstrap、`.cash-skills/lib/cash_cli/workspace.py`與`config.py`實作**單一 workspace adapter 擁有 artifact state**及**Workspace 與設定邊界**，交付launcher import前由argv直接選定shared/exclusive lock、receipt `st_dev/st_ino`與canonical runtime-generation驗證、Git-root bootstrap、兩個精確受限YAML subsets、all-read no-follow containment、temp ownership、snapshot revalidation、journal與recovery；以`cli-checks.fish workspace-config-boundaries`驗證nested cwd、missing/drifted lock零寫入、stable identity、generation record stream、duplicate/unknown config keys、所有拒絕YAML features、read/mutation symlinks、crash lock release、unfinished-journal reader、concurrent read及runtime generation不混用。
- [x] 1.3 在 `.cash-skills/lib/cash_cli/resources.py` 實作**Cash-owned artifact graph 與 instructions resources**及**Artifact graph 與 instructions 使用單一來源**，交付`spec-driven` DAG、templates、locale、apply/TDD/audit instructions的唯一資料來源；以`cli-checks.fish graph-instructions`逐byte比對`status`與`instructions`的dependencies/outputPath並覆蓋所有artifact readiness states。

## 2. Read 與 lifecycle commands

- [x] 2.1 在 `.cash-skills/lib/cash_cli/commands/discovery.py` 實作**Command interfaces and data shapes**中的`list`、`status`、artifact instructions、`instructions apply`與`instructions --skill <tdd|audit>`完整JSON contracts，交付排序固定的list entries、status `Artifact` element/empty rules、artifact instructions全部top-level/dependency/unlock fields與always-present `context/rules`，以及apply的`changeName/schemaName/changeDir` strings、`contextFiles` object、`progress.{total,complete,remaining}` integers、`tasks[].{id,description,done,parallel}`、always-present `missingArtifacts`、`state`、always-present `preflight.{status,missingFiles[].{path,source},driftedFiles[],staleness.{daysOld,isStale}}`、`locale/instruction`；以`cli-checks.fish discovery-contracts`對non-empty/empty context/rules、empty、single、multiple、parked、blocked、warning、critical與all_done snapshots逐byte驗證type、stable order、field presence、null prohibition及invariants。
- [x] 2.2 在 `.cash-skills/lib/cash_cli/commands/create.py`、`.cash-skills/lib/cash_cli/commands/tasks.py`與`.cash-skills/state/`實作**Change 與 artifact lifecycle**及**Cash-owned touched-state 追蹤 source allowlist**，交付identity互斥、dependency gate、stdin validation、含`legacy_import.{path,sha256,st_dev,st_ino}` provenance的versioned snapshot/touched schemas、first-entry/resume state machine、per-task attribution/aggregate union、唯一task mutation與`touched ensure` single-source cutover；legacy只在Cash state缺失時匯入現行`.spectra/touched/<name>.json`，成功後不再作為workflow輸入，且明確不讀歷史`.spectra/snapshots/`。以`cli-checks.fish creation-task-lifecycle`驗證resume不重設未完成baseline、同task idempotence、多task累積、無變更、staged-only/unstaged/add/delete/rename-copy雙端/typechange/unmerged/untracked、pre-existing dirty排除、legacy valid/malformed/direct commit/direct archive migration、`ensure→task done→ensure/commit/archive`、matching cleanup、same-path/same-bytes/different-inode preserve、pathname swap、digest drift diagnostic、native Cash旁legacy不刪除、duplicate task及archive cleanup。
- [x] 2.3 在 `.cash-skills/lib/cash_cli/commands/lifecycle.py` 實作`park`與`unpark`，交付active/parked identity preflight及collision-safe directory move；以`cli-checks.fish park-unpark`驗證round trip、destination collision、symlink parent與failure後source保留。

## 3. Gates、retrieval 與 archive

- [x] 3.1 在 `.cash-skills/lib/cash_cli/validation.py` 與 `.cash-skills/lib/cash_cli/commands/validate.py` 實作**Validation、analysis 與 drift 是可區分的 deterministic gates**中的validation部分、**Deterministic validation、analysis 與 drift**、**Spec 檔案語言政策**及**Requirement 標題是合併身分鍵**，交付DAG/Markdown/delta/title/task/config validators與`validate <name|--all>`；以`cli-checks.fish validation-matrix`覆蓋每個delta operation、missing scenario、title mismatch、malformed config與all-project aggregation。
- [x] 3.2 在 `.cash-skills/lib/cash_cli/commands/analyze.py` 與 `.cash-skills/lib/cash_cli/commands/drift.py` 完成**Deterministic validation、analysis 與 drift**的完整consumer shapes，交付analyze的`dimensions[].{dimension,status,finding_count}`、`findings[].{id,dimension,severity,location,summary,recommendation}`與artifact arrays；drift交付nullable `last_commit`、`dimensions[].{kind,status,score,contributes_to_total}`、`broken_anchors[].{anchor,category,reason}`、`tasks_maybe_resolved[].{id,description,matching_commits}`、`tasks_blocked_external[].{id,description,paths}`、score/severity、Cash recommendation及由同一object render的non-JSON report，所有empty/null行為逐欄固定；以`cli-checks.fish analyze-drift`逐byte驗證canonical skill讀取欄位、完整/不足artifacts、zero-result arrays、dirty Git與Git啟動失敗。
- [x] 3.3 在 `.cash-skills/lib/cash_cli/commands/search.py` 實作**Lexical search 取代向量搜尋**、**Deterministic lexical search**及**Cash-owned 設定與無向量模型 fallback**，交付Unicode ranking、stable tie-break、bounded excerpts、zero-result及all-read no-follow containment；以`cli-checks.fish lexical-search`驗證中英文query、排序、limit、symlink file/directory、parent swap、root外sentinel、unreadable file與沒有model/index依賴。
- [x] 3.4 在 `.cash-skills/lib/cash_cli/spec_merge.py` 與 `.cash-skills/lib/cash_cli/commands/archive.py` 實作**Archive 與 sync 使用同一個 spec merge transaction**及**Atomic park、sync 與 archive**，交付四種delta的MODIFIED/REMOVED→ADDED→RENAMED固定phase、合法MODIFIED+RENAMED與非法跨operation collision、由proposal/tasks導出的`@trace`、archive identity、sync digest manifest、repeated-sync idempotency、explicit no-sync、`--no-validate`不可略過preflight、`--mark-tasks-complete`transactional checkbox、journal rollback/recovery與collision-safe move；以`cli-checks.fish sync-archive-transaction`逐byte驗證本change等價combined operation、rename destination collision、sync→archive、already-synced、兩archive flags交叉矩陣、`--skip-specs`、不存在`spectra-sync-specs`委派、第N個checkbox/replace/move/rollback failure及active/touched state保留或清理。

## 4. Cash workflow cutover

- [x] 4.1 修改 `.agents/skills/` 下十二個canonical Cash `SKILL.md`，將**Cash namespace 負責 workflow 路由，Spectra 仍是 artifact 引擎**重新命名並交付Cash-owned artifact engine、**Cash 提案品質關卡**、**cash-propose 品質關卡**、**cash-apply 品質關卡**、**cash-commit 的 archive-first 允許清單**與guidance內**cash-apply 任務迴圈的阻塞分類**，所有commands先解析Git root再呼叫absolute Cash launcher，propose/apply/verify消費artifact `context/rules`與always-present `missingArtifacts/preflight`完整schema並處理三種state，commit在建立allowlist前呼叫`touched ensure`，archive使用內部ensure與Cash-only touched/sync state、對legacy cleanup diagnostic只回報不重讀allowlist，並遵守`--skip-specs`與transaction flags；以nested-cwd Codex command matrix、blocked/ready/all_done consumer snapshots、direct-upgrade及import後演進的source allowlist、sentinel與full-body parity baseline驗證。
- [x] 4.2 修改 `.claude/skills/` 下十二個canonical Cash `SKILL.md`及 `scripts/cash-skills/variant-parity/` manifests，交付與Codex相同的**Cash CLI cutover 覆蓋全部 live workflow surfaces**並只保留合法tool/invocation差異；以`skill-checks.fish variant-parity`對24個完整files執行exact diff與每個command shape覆蓋。
- [x] 4.3 修改 `.agents/skills/cash-propose/SKILL.md`、`.agents/skills/cash-apply/SKILL.md`、`.claude/skills/cash-propose/SKILL.md`與`.claude/skills/cash-apply/SKILL.md`的**審查迴圈的 grader 不可變性**，將protected config path改為`.cash.yaml`並維持signals `check`與master specs保護；以sentinel/content assertions驗證四檔的protected set與結構化範圍例外逐字一致。
- [x] 4.4 新增 `.cash.yaml`、移除 `.spectra.yaml`，並修改 `AGENTS.md`、`CLAUDE.md`與 `CASH-SKILLS.md`，交付**Behavior**、**Cash 指引提供無向量模型替代流程**、**現行文件反映 cash 所有權與清理**與Cash-only guidance：canonical Cash block最上方逐byte包含全域繁體中文回覆規則（`本專案所有面向使用者的回覆一律以繁體中文撰寫，除非使用者明確要求其他語言。`起始，位於阻塞分類requirement與Cash-owned fallback之前，兩個variant一致）、known change直接讀artifact、nested-cwd parked fallback使用Cash CLI、search不要求vector model、文件不再要求保留標準Spectra skills，且歷史資料邊界明確；以balanced-fence exact block render tests（含全域回覆語言規則位置斷言）與source namespace scan驗證project-owned文字、config keys、legacy-removal說明及無runtime fallback。

## 5. Installer、bundle 與 legacy removal

- [x] 5.1 擴充 `install-cash-skills.fish` 與 `.cash-skills/receipt.tsv` schema（並在 `.gitignore` 忽略 `.cash-skills/receipt.tsv` 與 `.cash-skills/state/`，使 target-specific `st_dev/st_ino` receipt 與 per-target state 不進版控），實作**Installer transaction 同時治理 runtime、skills 與 legacy removal**、**Bundle 安裝與 runtime receipt**、**Cash guidance deployment**、**Installer 與 legacy cleanup filesystem boundaries**、MODIFIED**手動的 cash 專案 registry**及MODIFIED**版本感知的 cash skill 批次安裝**，完整承接移除的**無狀態的跨專案安裝器**、**Cash 安裝不含修復自動化**、**安裝器與清理落實檔案系統邊界**、**Cash skill bundle 版本與 target receipt**及**Cash project guidance migration**；fresh、receipt-less相同24-skill adoption與known `version + 24 sha256/digest/path` receipt migration使用monotonic stable bootstrap，lock/launcher發布後rollback永不unlink，existing/current/upgrade/force/batch在讀managed state前鎖同一no-follow inode且永不replace bootstrap，再以精確record-stream generation、target `st_dev/st_ino`、24 skills、mode-aware receipt、config/guidance與legacy removals共用transaction。以fixtures驗證register的Git/config prerequisites與registry零寫入、receipt-less 0/24/partial/different/unsafe inventory、install-vs-running/new-launch/legacy-bootstrap concurrency、lock-only/lock+launcher recovery、rollback後same-inode與concurrent loser reclassification、bootstrap source drift、runtime generation receipt、known-old/unknown-old receipt與rollback、target Git/config prerequisites、incoming parser與newer target ordering、existing Cash config完整YAML拒絕矩陣、legacy unknown scalar/map/list/default config、fresh/full-inventory-adopt/current/update/newer/conflict/dry-run/force、batch summary、SemVer/history、invalid receipt、0755 launcher及rollback。
- [x] 5.2 新增 `scripts/cash-skills/legacy-spectra-digests.tsv`並在 `install-cash-skills.fish` 實作安全config migration與標準`spectra-*` exact-baseline removal，交付**Spectra 更新不改動 cash skills**、**專案擁有的 cash 指引在 Spectra 更新後存續**及移轉後installer requirements的Cash治理；以fixtures驗證既有`.cash.yaml`由同一parser驗證後逐byte保留、受支援keys轉換、non-default`spec_dir`拒絕、caller-owned config不刪除，以及同名body drift/mode/hard-link/unknown version/symlink/extra content在首次write前fail closed。
- [x] 5.3 移除 `.agents/skills/spectra-analyze/`、`.agents/skills/spectra-apply/`、`.agents/skills/spectra-archive/`、`.agents/skills/spectra-ask/`、`.agents/skills/spectra-audit/`、`.agents/skills/spectra-commit/`、`.agents/skills/spectra-debug/`、`.agents/skills/spectra-discuss/`、`.agents/skills/spectra-drift/`、`.agents/skills/spectra-ingest/`、`.agents/skills/spectra-propose/`、`.agents/skills/spectra-verify/`及對應 `.claude/skills/` directories，並提升 `cash-skills.version`，完成**Legacy plus 實作已除役**後續的全Spectra canonical retirement；以`git ls-files` inventory assertion與installer removal fixture驗證恰好24個Cash skills。

## 6. Contract regression 與 final cutover

- [x] 6.1 在 `scripts/cash-cli/fixtures/` 與 `scripts/cash-cli/tests/cli-checks.fish` 建立**Acceptance criteria**與**Cash 合約測試套件**中`cli-checks.fish`治理範圍要求的positive lifecycle suite，交付new → artifacts → task done → validate → analyze/drift → sync/archive smoke path及JSON snapshots；以PATH排除Spectra binary後執行整套suite並逐byte驗證outputs、master specs與archive tree。
- [x] 6.2 在 `scripts/cash-cli/fixtures/` 與 `scripts/cash-cli/tests/cli-checks.fish` 建立**Error contract**negative/atomicity suite，覆蓋unsafe root、read/mutation symlinks、hard link、post-preflight edit、parent/destination swap、malformed config、unknown command、invalid title、collision、第N個replace/move/rollback failure、crash lock release、reader-during-publication、Git error與temporary ownership；逐case驗證exit 1/2、diagnostic、mode、journal/recovery、root外sentinel與可觀測state。
- [x] 6.3 修改 `scripts/cash-skills/tests/skill-checks.fish` 與 `CASH-SKILLS.md`，實作**Namespace 完成條件區分 live surface 與歷史資料**、**Live namespace 與歷史邊界**、**Scope boundaries**及**Cash 合約測試套件**中`skill-checks.fish`治理範圍，移除`update --force` regression並以精確include roots掃描canonical Cash skills、variant manifests、installer/runtime/tests、live docs/config及master specs；active migration artifacts/reviews、archive、signals history與窄化legacy detector/importer/digest literals分別治理，不使用模糊all-non-archive豁免；另驗證24-skill consumer matrix、`.spectra/`state allowlist、runtime receipt/mode與installer contracts。
- [x] 6.4 執行`.cash-skills/bin/cash validate --all`、`fish scripts/cash-cli/tests/cli-checks.fish`、`fish scripts/cash-skills/tests/skill-checks.fish`及精確live include-root namespace scan，完成**Cash CLI cutover 覆蓋全部 live workflow surfaces**的最終驗收；確認proposal、design、specs、tasks的**Command interfaces and data shapes**逐項有test backing、所有removed paths不存在、Git worktree沒有test residue，且不修改active migration provenance、`openspec/changes/archive/`與signal occurrence history。

## 7. Source repository bootstrap 修正

- [x] 7.1 在`install-cash-skills.fish`、`.cash-skills/lib/cash_cli/installer.py`與stable `.cash-skills/bin/cash`實作source-only `--self [--dry-run]`及actionable `bootstrap_invalid`診斷：一般target modes仍拒絕source repository，self模式持有既有stable lock的exclusive FD並只原子建立同schema target receipt，不發布其他managed content；在`scripts/cash-skills/tests/test_installer_runtime.py`與`skill-checks.fish`先新增無receipt source fixture的失敗測試，再驗證dry-run零寫入、real/current idempotence、mode互斥、unsafe boundary、JSON/non-JSON提示、source `validate --all`成功及測試後零receipt/state residue。
- [x] 7.2 在`.cash-skills/lib/cash_cli/installer.py`修正registry parser，使LF分隔的精確空行被忽略、包含space/tab/CR或其他ASCII控制字元的非空record仍以含行號的diagnostic fail closed，且`--register`、`--unregister`、`--list`與`--all`共用相同行為；在`scripts/cash-skills/tests/test_installer_runtime.py`先新增前導／中間／尾隨空行、CRLF、embedded CR與whitespace-only fixtures的失敗測試，再驗證空行不改寫registry、invalid records零registry/target寫入及完整installer/skill suites通過。
- [x] 7.3 在`.cash-skills/lib/cash_cli/spec_merge.py`與`.cash-skills/lib/cash_cli/validation.py`封閉RENAMED destination碰撞缺口：同一份delta中多個RENAMED指向相同destination MUST在publication前以`requirement_collision`失敗，`validate` MUST另以`operation_collision` finding獨立攔截duplicate destination、destination與ADDED title collision及destination已存在於master spec三種情形，形成validation gate與merge phase雙重防線；在`scripts/cash-cli/tests/test_validation_matrix.py`與`test_sync_archive_transaction.py`先新增雙rename指向同一title的失敗測試，再驗證master spec逐byte不變且requirement數不減。
- [x] 7.4 在`.cash-skills/lib/cash_cli/commands/lifecycle.py`與`.cash-skills/lib/cash_cli/commands/archive.py`於移動前確保destination parent directory存在，使尚無`openspec/changes/.parked/`或`openspec/changes/archive/`的workspace首次`park`與`archive`成功而非以缺少目錄的execution error失敗；在`scripts/cash-cli/tests/test_park_unpark.py`與`test_sync_archive_transaction.py`先新增移除該父目錄的失敗測試，再驗證transaction完成、來源change消失且suites全數通過。
- [x] 7.5 在`.cash-skills/lib/cash_cli/installer.py`修正legacy config migration，使full-line `#` comment被略過而非拒絕（Spectra預設模板通篇為註解，原行為使真實target幾乎必然失敗），且縮排行、被註解的key與其他active unsupported key仍fail closed；在`scripts/cash-skills/tests/test_installer_runtime.py`先新增真實註解模板與四組反向fixtures的失敗測試，再驗證遷移輸出等同去註解後結果。
- [x] 7.6 在`.cash-skills/lib/cash_cli/installer.py`把legacy skill removal的「無法證明為已知baseline」與「邊界不安全形狀」分離：新增`classify_legacy_candidate`，使unknown version與mode drift改為保留、回報並繼續安裝，symlink／hard link／額外內容維持首次write前fail closed；legacy receipt migration不再以舊schema未記載的mode為gate；安裝輸出逐target列出被保留path。更新`CASH-SKILLS.md`與既有fail-closed測試至新契約，並驗證被保留檔案的bytes與mode逐一不變。
- [x] 7.7 在`.cash-skills/lib/cash_cli/config.py`收斂`_reject_yaml_features`：quotes、anchors、aliases、tags與flow collection字元只在scalar或rule item起始位置拒絕，中段出現視為合法plain scalar文字（原行為會誤擋`Mark independent tasks with [P]`與含引號的散文，使真實專案的`openspec/config.yaml`無法通過target prerequisite）；` #` trailing comment維持任意位置拒絕。在`scripts/cash-cli/tests/test_workspace_config_boundaries.py`先新增中段indicator應接受、起始indicator與trailing comment應拒絕的失敗測試，再驗證真實rules內容原封不動可解析。
