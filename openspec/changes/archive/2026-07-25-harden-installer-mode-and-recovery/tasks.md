## 1. 測試先行

- [x] 1.1 在 `scripts/cash-skills/tests/test_installer_runtime.py` 新增 **空字串 mode 參數不得被重新解讀**、**空字串 mode 參數的診斷優先於 registry 錯誤**、**空字串 mode 參數與相容性 flag 併用的診斷** 與 **Boolean mode flag 的相容性守衛不受影響** 的失敗測試：以至少註冊一個 project 的隔離 HOME registry，分別執行帶空字串的 `--target`、`--register`、`--unregister`，斷言三者皆以確切 exit code 2 失敗、registry 內 project 完全未被寫入（receipt、runtime、skills、guidance 與版控排除設定皆不存在或逐 byte 不變）；另以刻意不合法（無法通過 canonical 檢查）的 registry 重跑同組指令，斷言 diagnostic 指出 mode 參數值無效而非 registry 或 HOME 錯誤，藉此把「未讀取 registry」變成可觀察條件；斷言三個空字串 mode 參數各自配合 `--dry-run` 時 diagnostic 皆指出該值無效而非缺少 mode 參數（`--register ""` 與 `--unregister ""` 不在相容性檢查的運算元之列，若空值守衛未排在該檢查之前就會被「缺少 mode 參數」訊息遮蔽，此案例即為該順序的觀察點）；斷言三個空字串 mode 參數各自配合 `--force` 時 diagnostic 同樣指出該值無效而非缺少 mode 參數（`--force` 守衛的運算元同樣不含 `--register`／`--unregister`，是同一順序約束的第二個觀察點，與 `--dry-run` 併為同一個 scenario 的兩個 flag 變體）；斷言 `--list --dry-run` 與 `--register <project> --force` 維持 exit 2 且零寫入。以 `scripts/cash-skills/tests/test_installer_runtime.py` 驗證這些測試在實作前失敗。
- [x] 1.2 在 `scripts/cash-skills/tests/test_installer_runtime.py` 新增 **Crash 後首次執行即完成恢復**、**版控排除設定 plan 取自 recovery 之後**、**Receipt-less 與 legacy 崩潰同樣先恢復**、**Newer target 帶未完成 journal 仍零寫入**、**Journal 存在而 stable lock 不存在則 fail closed** 與 **Dry run 遇未完成 journal 不恢復但明示** 的失敗測試。publishing 階段的 journal 無法由任何既有 hook 產生（失敗注入會走 rollback 並清除 journal，崩潰型 hook 都在 committed 之後），因此 fixture 以直接構造的方式建立：以 `0600` 手工寫入 schema v2 的 journal（`version` 為 `2`、`phase` 為 `publishing`、`published` 為已發布筆數、`operations` 內每筆 write 記錄其 `before` 的存在旗標、base64 內容與 mode，且至少含一筆版控排除設定的 write 與一筆 receipt-managed runtime path 的 write），並同步把對應 managed paths 置為半發布 bytes。receipt-managed path 是關鍵案例：恢復點若晚於 conflict 判定，該情形會先以 `managed target drift` 回報 `Result: conflict` 並 exit 2 而永遠到不了 recovery。斷言下一次非 dry-run 執行在單次 invocation 內完成 rollback、回報 `update`、清除 journal，且不出現 `Result: conflict`、不出現 `installation inputs changed after lock acquisition`、不需 `--force`；斷言版控排除設定內容由 recovery 之後的狀態導出、未被 recovery 之前的 snapshot 覆寫；除已安裝 target 之外，另建立兩個會命中提前返回分支的 fixture：其一為「無 receipt、24 個 skill 中已發布 1–23 個」（命中 `receipt-less Cash skill inventory is partial`），其二為 legacy receipt 尚未被替換（命中 `legacy receipt drift`），各配一份 publishing 階段 journal。兩個 fixture 皆必須同時具備既存的 `.cash-workspace.lock` 與已發布的 launcher（canonical `lock+launcher` prefix）——journal 只可能在 `acquire_lock` 與 `publish_launcher` 之後才被寫出，因此真實崩潰狀態必然帶該 prefix；若照 receipt-less／legacy 的 canonical「無 lock」形狀構造，會先命中「journal 存在而 stable lock 不存在則 fail closed」而使本斷言不可達。斷言兩者皆先完成 recovery 再重新分類、不以該分支提前返回；斷言帶 journal 的 newer target（receipt 版本高於 incoming bundle）維持零寫入、回報 `newer`、不執行 recovery，且通用 diagnostic 之外另有 newer 專屬補充句；斷言 journal 的 schema version 不被本 bundle 辨識時以 execution error fail closed 且 diagnostic 指出需版本相符或更新的 installer；斷言 `JOURNAL_PATH` 為 symlink（含 dangling）時以 execution error fail closed 而非被視為無 journal；斷言 journal 存在而 `.cash-workspace.lock` 不存在時以 execution error fail closed 且未建立新的 lock inode（比對 device/inode）；斷言同一 fixture 下的 `--dry-run` 維持零寫入、不執行 recovery，且 diagnostic 指出存在未完成的 journal——並額外覆蓋最終分類為 `conflict`、`update`、`newer` 與 `current` 四種結局，確認該 diagnostic 在四者皆出現、且在 dry-run 與 real run 皆出現，藉此把「偵測點早於 `newer` early return」變成可觀察條件；另斷言 recovery 之後仍存在與該 journal 無關的 drift 時，installer 回報 `Result: conflict`、exit 2，且自重新分類起零寫入（recovery 本身的 rollback 寫入不計入該零寫入契約）——此案例與「回報 `update`」的案例互斥，兩者的差別只在 recovery 之後是否仍有無關 drift。以 `scripts/cash-skills/tests/test_installer_runtime.py` 驗證這些測試在實作前失敗。
- [x] 1.3 在 `scripts/cash-skills/tests/test_installer_runtime.py` 新增 **開關關閉時 hooks 完全無效**、**Preflight 可判定的 hold 設定錯誤 fail closed**、**等待點才可判定的 hold 形狀在等待點中止**、**後續 installation attempt 不因自身 hold 檔而失敗**、**兩個 hold hook 各自記帳** 與 **失敗注入序號無法解析** 的失敗測試：斷言 `CASH_INSTALL_TEST_HOOKS` 未設定或值不為 `1` 時，即使其餘 `CASH_INSTALL_*` 變數皆設為會改變行為的值，分類、發布與 exit code 仍與這些變數不存在時完全相同且無 ready 檔產生；斷言開關開啟後，preflight 可判定的六種形狀——hold path 為 relative path、parent 不存在、parent 為 symlink、ready 檔已存在、release 檔在 preflight 已存在、兩個 hook 的 hold path 相同——各自在首次 target write 前以 execution error 失敗（含 fresh target 未建立 workspace lock）；另斷言等待點才可判定的三種形狀——ready 檔在 preflight 之後才出現、release 檔在 preflight 當下不存在而於等待點前才出現、該後出現的 release 檔為 symlink——在該 hook 的等待點以 execution error 中止、不被當成解除訊號、且未提交任何 transaction operation（這兩種不得斷言「首次 target write 之前」，因為等待點在 `acquire_lock` 之後）；兩組皆斷言既有檔案內容與 symlink 目標未被寫入或截斷；斷言兩個 hook 設在互異路徑時，`CASH_INSTALL_HOLD_FILE` 的 hook 已等待過一次後 `CASH_INSTALL_PUBLICATION_HOLD_FILE` 的 hook 仍正常等待，證明記帳以 hook 為鍵而非以路徑為鍵（同路徑的 fail-closed 屬前述 preflight 六種形狀之一，不在此案例）；斷言 hold 已等待過一次後，重新分類的重新進入與 batch 迴圈的後續 target 皆完全跳過該 hook（含 preflight 的 ready 檔與 release 檔存在性檢查），不因本 process 前一輪建立的 ready 檔或解除等待所用的 release 檔而失敗，且路徑形狀檢查仍逐次執行；斷言失敗注入序號為非整數時在首次 target write 前以 execution error 失敗而非未捕捉例外。以 `scripts/cash-skills/tests/test_installer_runtime.py` 驗證這些測試在實作前失敗。
- [x] 1.4 在 `scripts/cash-skills/tests/test_installer_runtime.py` 新增 **系統預設 interpreter 過舊但存在合格版本化 interpreter**、**泛用名稱合格時選擇結果不改變**、**無合格 interpreter** 與 **交棒不保留 shell parent 且停用 user site** 的失敗測試：以自建的 shim 目錄與受控 PATH，模擬泛用名稱皆過舊或不存在而版本化名稱合格的情形，斷言進入點仍完成該次 invocation；模擬泛用名稱合格且另有更新版本化名稱可用的情形，斷言選中的是泛用名稱解析到的 interpreter；斷言候選清單全數不合格時以 execution error 失敗且零寫入；斷言交棒後的 Python process 其 parent 為啟動進入點的測試 process 而非進入點自身；以 hold hook 持住 installer 的期間檢查 `Popen` 回傳的 pid 之下不存在子行程（或該 pid 的執行檔已成為 python）來觀察，因為 `exec` 之後 pid 不變、無法單靠 pid 比對驗證。user site 停用以可觀察的方式驗證：以受控 `PYTHONUSERBASE` 在 user site 目錄植入會產生外部可見副作用的 `usercustomize.py`，斷言交棒後該副作用未發生，並以 installer 正常完成安裝間接證明既有 library path 注入仍生效。以 `scripts/cash-skills/tests/test_installer_runtime.py` 驗證這些測試在實作前失敗。

## 2. Installer runtime 實作

- [x] 2.1 在 `.cash-skills/lib/cash_cli/installer.py` 實作 **Installer 與 legacy cleanup filesystem boundaries** 新增的 mode 分派規則：`--target`、`--register`、`--unregister` 三個分支條件改依「參數是否被提供」判定而非參數值的 truthiness；並在解析參數之後、且在 `read_registry()` 與 `--dry-run`／`--force` 相容性檢查兩者之前，對這三個帶值參數各自加一道空值守衛，以 `InstallerError` 搭配 `exit_code=2` 失敗。順序必須同時早於這兩個關卡：早於 registry 讀取，空字串才不會被 registry 或 HOME 自身的錯誤遮蔽診斷；早於相容性檢查，`--register "" --dry-run` 才不會先命中「`--dry-run` 需要 `--target`、`--all` 或 `--self`」而回報缺少 mode 參數。`install_target` 的 `target must be a safe existing directory` 與 `canonical_target` 的 `project path is invalid` 兩個既有守衛的退出碼不得改動，因為它們同時服務既有的 boundary execution error 情境。`--dry-run` 與 `--force` 的相容性前置檢查只對帶值參數改用存在性判準，`--all`、`--self` 等 `store_true` 布林 flag 維持真值判斷，避免 `False is not None` 恆真使守衛整組失效。以 `scripts/cash-skills/tests/test_installer_runtime.py` 驗證 1.1 的測試轉為通過，且既有的 mode 互斥、`--list`、`--all` 與 registry 行為未改變，特別確認 `--list --dry-run` 與 `--register <project> --force` 仍為 exit 2。（本任務改動 replaceable runtime 檔 `.cash-skills/lib/cash_cli/installer.py`，完成後須立即依任務 2.4 重建 receipt，否則後續任何 `.cash-skills/bin/cash` 指令——包含 cash-apply 用來標記 checkbox 的 `task done`、`touched ensure` 與 `instructions apply`——皆會以 `receipt_invalid: runtime record drift` 與 exit 1 失敗。）
- [x] 2.2 在 `.cash-skills/lib/cash_cli/installer.py` 實作 **Bundle 安裝與 runtime receipt** 新增的 recovery 順序規則：在 read-only preflight 內、且早於版本比較的 `newer` early return 加入 journal 存在偵測（純讀取、不持鎖、不解讀 target config，並以 no-follow `lstat` 判定形狀，非 regular file 含 symlink 時 fail closed），通用 diagnostic 由此點發出；`newer` 專屬補充句於 `newer` early return 之前另外輸出，不得併入通用句。恢復本身緊接在 `newer` early return 之後，且必須早於全部三個提前返回的分類分支（`legacy receipt drift`、`receipt-less Cash skill inventory is partial`、`managed target drift`）——只擋 `managed target drift` 會讓 fresh install 崩潰與 legacy migration 崩潰的 journal 仍永遠到不了 recovery；恢復只在非 dry-run 且 target 未分類為 `newer` 時執行——newer target 必須維持零寫入返回，其 journal 由較新的 installer 處理。恢復的順序為：先執行既有的 `launcher exists without stable workspace lock` 檢查，再取得既存 stable lock，再呼叫 recovery；該次取鎖不得建立不存在的 lock，journal 存在而 lock 不存在時 fail closed，不得以 `O_CREAT|O_EXCL` 建立新 lock inode 而把該 unknown partial state 靜默修復。journal recovery 回傳它是否實際處理並清除了 journal，回傳為偽表示持鎖後 journal 已不存在，使 pre-lock 偵測在持鎖時被重新驗證。兩個回傳分支都必須關閉 lock descriptor：回傳為真時關閉後依 recovery 之後的狀態重新進入（不重複輸出版本控制診斷），回傳為偽時關閉後沿原流程繼續。若回傳偽時不關閉而續走主流程，主流程稍後會對同一 lock 檔開第二個 fd 並取 exclusive lock，同一 process 的兩個 open file description 互為獨立持有者而無限期阻塞；必須維持「同一 process 任一時刻至多持有一個 stable lock descriptor」的不變量。恢復點必須早於三個提前返回分支，否則半發布 bytes 落在 receipt-managed path 時會先以 `managed target drift` 回報 `conflict` 並 exit 2，而 receipt-less 與 legacy 崩潰更會先命中另外兩個分支，皆永遠到不了 recovery。journal 的 schema version 不被本 bundle 辨識時以 execution error fail closed 並在 diagnostic 指出需版本相符或更新的 installer。重新進入後該份 journal 已不存在，因此同一份 journal 不再觸發第二次重新分類；釋放 lock 的時間窗內由外部併發產生的新 journal 可再觸發一次，屬既有併發語意，不需額外的 journal identity 追蹤。target snapshot、legacy candidate plan 與版控排除設定 plan 皆取自 recovery 之後的狀態。`--dry-run` 維持不執行 recovery 並維持零寫入，並在同一個「早於 `newer` early return」的偵測點輸出 diagnostic 指出 target 存在未完成的 journal，使該 diagnostic 與 dry-run 的最終分類無關而一律出現（含分類為 `newer` 的情形）。以 `scripts/cash-skills/tests/test_installer_runtime.py` 驗證 1.2 的測試轉為通過，且既有的兩處 post-lock 重新分類與 rollback 契約未改變；特別確認 publication 前 revalidation 對外部併發修改仍 fail closed（既有測試釘住 `installation inputs changed after lock acquisition` 的那條路徑不得被放寬），本次只免除 recovery 自身造成的變更。（本任務改動 replaceable runtime 檔 `.cash-skills/lib/cash_cli/installer.py`，完成後須立即依任務 2.4 重建 receipt，否則後續任何 `.cash-skills/bin/cash` 指令——包含 cash-apply 用來標記 checkbox 的 `task done`、`touched ensure` 與 `instructions apply`——皆會以 `receipt_invalid: runtime record drift` 與 exit 1 失敗。）
- [x] 2.3 在 `.cash-skills/lib/cash_cli/installer.py` 實作 **Installer fault-injection hooks 治理**：所有 `CASH_INSTALL_*` fault-injection hooks 只在 `CASH_INSTALL_TEST_HOOKS` 的值恰為 `1` 時被讀取，開關關閉時完全不查詢其餘變數；把已啟用 hooks 的設定驗證（hold 路徑形狀、兩個 hook 的 hold 路徑互異、release 檔當下不存在、失敗注入序號可解析性）提前到任何 `acquire_lock` 呼叫之前的 preflight，使不合法設定在建立 workspace lock 這第一次 target write 之前就 fail closed，hold 的等待點維持原位；hold 路徑要求為 absolute path 且其 parent 為既存非 symlink 目錄，ready 檔改以 exclusive、no-follow 的建立語意產生，release 檔的不存在性在 preflight 與各 hook 自身的等待點進入時各驗證一次（等待點進入時已存在即以 execution error 中止，不得當成解除訊號），且只在其為非 symlink 的 regular file 時視為解除訊號，既有檔案或 symlink 一律 fail closed；at-most-once 的記帳以 hook 為鍵而非以 hold 路徑為鍵，`CASH_INSTALL_HOLD_FILE` 與 `CASH_INSTALL_PUBLICATION_HOLD_FILE` 各自記帳互不影響；每個 hold hook 在單一 process 內至多等待一次，且已等待過之後同一 process 內任何後續的 `install_target` 呼叫（含重新分類的重新進入與 `--all` batch 迴圈的後續 target）皆完全跳過該 hook，包含其等待與 preflight 的全部 hold 檔存在性檢查（ready 檔與 release 檔皆在免除範圍內，因為 `wait_for_test_hold` 從不刪除 release 檔），路徑形狀檢查則不在免除範圍內；失敗注入序號的整數轉換以 `InstallerError` 包覆。本 task 不得在 `.cash-skills/lib/cash_cli/` 新增模組檔案：新增一個 runtime record 會新增一筆 transaction operation 並位移既有測試硬編的失敗注入序號，使該測試仍然通過卻不再落在原本階段。刪除零引用的 commit 後崩潰 hook `CASH_INSTALL_CRASH_AFTER_COMMIT`，並確認它不再於實作、測試或使用者文件中作為可生效的環境變數名稱被讀取或設定（本 change 的 delta spec、design 與 tasks 對該名稱的敘述性引用不在此限，archive 後 master spec 亦會保有該敘述，因此驗收不得以全域字串搜尋為準）；`CASH_INSTALL_CRASH_AFTER_QUARANTINE` 保留不動，僅受新開關約束。同步在 `scripts/cash-skills/tests/test_installer_runtime.py` 為既有使用 fault-injection hooks 的六個測試加上該開關。它們分屬三條注入路徑，必須逐一處理，不得只涵蓋其中一部分：(a) 兩個經 `install` helper 既有的 `TEST_` 前綴轉譯層（設定 `TEST_CASH_INSTALL_FAIL_AFTER_PATH` 與 `TEST_CASH_INSTALL_FAIL_AFTER`）；(b) 兩個把真名寫進 `os.environ` 再經 `install_from` 繼承（`CASH_INSTALL_FAIL_AFTER`、`CASH_INSTALL_CRASH_AFTER_QUARANTINE`）；(c) 兩個自行以 `os.environ.copy()` 組 env 交給 `subprocess.Popen`（`CASH_INSTALL_PUBLICATION_HOLD_FILE`、`CASH_INSTALL_HOLD_FILE`）。(a) 這一組特別容易被漏掉——它們確實經過 `TEST_` 轉譯層，開關導入後若未一併轉譯新開關，注入變數不再被讀取、安裝會成功，其 rollback 斷言會靜默失效。`install`、`install_from` 兩個實際承載 hook 注入的 helper，以及目前無 hook 測試使用但同屬安裝入口的 `run_installer`，統一改由 per-call 的 env 參數注入 hook 變數並先剝除全部 `CASH_INSTALL_*`（含新開關），`TEST_` 轉譯清單一併涵蓋新開關；不得把 `CASH_INSTALL_*` 真名寫入 parent process 的 `os.environ`，否則同一測試期間所有子行程都會繼承而使「開關關閉」的測試失去意義。六個測試需逐一列舉並確認注入後階段不變。以 `scripts/cash-skills/tests/test_installer_runtime.py` 驗證 1.3 的測試轉為通過，且既有的失敗注入、hold 與 quarantine 崩潰測試仍落在原本階段。（本任務改動 replaceable runtime 檔 `.cash-skills/lib/cash_cli/installer.py`，完成後須立即依任務 2.4 重建 receipt，否則後續任何 `.cash-skills/bin/cash` 指令——包含 cash-apply 用來標記 checkbox 的 `task done`、`touched ensure` 與 `instructions apply`——皆會以 `receipt_invalid: runtime record drift` 與 exit 1 失敗。）
- [x] 2.4 每一次改動任一 replaceable runtime 檔（本 change 為 `.cash-skills/lib/cash_cli/installer.py`）之後、下一次執行任何 `.cash-skills/bin/cash` 指令之前，於 project root 執行 `./install-cash-skills.fish --self` 重建 `.cash-skills/receipt.tsv`。此步驟可重複執行，且最後一次重建必須在任務 4.1 的版本提升之後、任何回歸執行之前。理由：launcher 每次執行都逐檔比對 receipt 記錄的 runtime digest，任一 runtime 檔改動而未重建 receipt，`.cash-skills/bin/cash` 即全面以 `receipt_invalid: runtime record drift` 與 exit 1 失敗——因此重建不能只排在版本提升之後：任務 2.1 一落地就必須先重建一次，否則 2.1 至 4.1 之間的任務都無法標記完成。版本提升不是重建的前提（launcher 的 receipt 驗證只檢查版本字串格式、不比對 `cash-skills.version`）。`.cash-skills/receipt.tsv` 為 gitignore 檔，不列入 affected code。驗收：每次重建後 `.cash-skills/bin/cash validate --all` 須 exit 0。

## 3. 進入點實作

- [x] 3.1 [P] 在 `install-cash-skills.fish` 實作 **Installer 進入點 interpreter 解析與 process 邊界**：interpreter 候選清單依序改為 `python3`、`python`、`python3.14`、`python3.13`、`python3.12`、`python3.11`，逐一套用既有的最低版本探測並取第一個通過者。泛用名稱必須排在版本化名稱之前：實測本機 `python3` 解析到 mise 管理的 3.13.0 而 `python3.14` 解析到 Homebrew 的 3.14.6，版本化優先會讓 installer 繞過既有 toolchain shim，而本次只需要在泛用名稱不合格時提供備援；移除版本探測中從未被讀取的輸出變數；改以 `exec` 交棒給選定的 interpreter，使 Python process 不保留由該進入點建立的 shell parent 且 exit status 直接傳遞；交棒時停用 user site directory，並維持既有的 library path 注入與 cwd 隔離。以 `scripts/cash-skills/tests/test_installer_runtime.py` 驗證 1.4 的測試轉為通過，且既有的 source root 解析與「找不到合格 interpreter 時失敗」行為未改變。

## 4. 版本與驗收

- [x] 4.1 提升 `cash-skills.version` 使其嚴格遞增，並同步更新 `scripts/cash-skills/tests/skill-checks.fish` 中以字面值釘住 bundle version 的斷言（該檔以 `test (string trim <"$root_dir/cash-skills.version") = <version>` 比對，不更新則 4.2 的「合約測試套件全數通過」與本 task 互相矛盾；該檔已在 proposal `## Impact` 宣告為交付目標）。執行 `--self` 重新發布 source receipt，使 replaceable runtime generation 與新的 `.cash-skills/lib/cash_cli/installer.py` bytes 一致。以 `scripts/cash-skills/tests/test_bundle_version_history.py` 驗證版本嚴格遞增與 bundle 內容綁定成立，確認 `.cash-skills/bin/cash` 逐 byte 未變，並確認 `.cash-skills/lib/cash_cli/` 底下（含子目錄，即 `library.rglob("*.py")` 排除 `__pycache__` 後的結果，變更前為 19 個）的 `.py` 檔數與變更前相同——非遞迴清點只會數到 9 個而漏掉 `commands/` 下的 10 個，是無效的判準。
- [x] 4.2 執行 `.cash-skills/bin/cash validate --all`、`fish scripts/cash-cli/tests/cli-checks.fish` 與 `fish scripts/cash-skills/tests/skill-checks.fish` 全數通過；並以隔離 fixture target 確認 `current`、`update`、`newer`、`conflict`、`bootstrap` 五種分類語意、receipt schema、lock 的建立與 identity 重驗機制（`O_CREAT|O_EXCL`、`flock`、`fstat` device/inode 比對）與 lock inode 的持久性、legacy 遷移判準與 guidance 區塊渲染皆未改變（兩項例外：recovery 觸發的釋放-重入沿用既有 post-lock 重新分類的同一路徑；恢復前置階段使用一條不走建立分支的取鎖路徑。兩者皆不改變建立語意與 identity 重驗步驟本身），且測試後無 receipt、journal、hold 或 quarantine 殘留。
