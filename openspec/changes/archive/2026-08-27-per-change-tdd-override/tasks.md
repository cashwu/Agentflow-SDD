## 0. Bundle version contract 修復

- [x] 0.1 對應既有bundle version history contract與design C6；先讀取工作樹及`git show HEAD:cash-skills.version`的合法版本，取較大者後將`cash-skills.version`嚴格提升為更大的版本並維持單行LF，再將`.cash-skills/lib/cash_cli/installer.py`的`BUNDLE_VERSION`同步為相同值，執行`./install-cash-skills.fish --self`重建manifest；此task必須在任何後續受守衛skill編輯或Cash CLI呼叫前完成，且不得寫死artifacts建立時的版本；delivery: `cash-skills.version`、`.cash-skills/lib/cash_cli/installer.py`、`.cash-skills/manifest.tsv`；verification: `python3 scripts/cash-skills/tests/test_bundle_version_history.py`；regression: `python3 scripts/cash-skills/tests/test_installer_runtime.py`；success: primary target輸出`PASS: bundle version history`且以exit 0結束；red: 已於task 1.1部分實作後執行全量skill suite，primary所治理的同一checker因`.agents/skills/cash-apply/SKILL.md changed without a strictly greater cash-skills.version`具名失敗

## 1. cash-apply 生效值解析與 ledger 摘要

- [x] 1.1 對應requirement `cash-apply 以條件式 canonical TDD discipline 驅動 task loop`；依design C3與C5，先在`scripts/cash-skills/tests/skill-checks.fish`的`assert_tdd_discipline`更新／新增斷言（change-level解析、global fallback、非法值警告、來源宣告文字），執行群組觀察具名失敗後，再修改`.claude/skills/cash-apply/SKILL.md` Step 5為生效值解析（change目錄`.openspec.yaml`的`tdd:`行優先、重複行取第一行、`.cash.yaml` fallback、非法值印警告後fallback）並於Step 6進度輸出宣告生效值與來源，以`fish scripts/cash-skills/generate.fish`重生成`.agents`變體，最後在任何下一次Cash CLI呼叫（含`task done`）前執行`./install-cash-skills.fish --self`重建manifest消除fail-closed窗口；delivery: `scripts/cash-skills/tests/skill-checks.fish`、`.claude/skills/cash-apply/SKILL.md`、`.agents/skills/cash-apply/SKILL.md`、`.cash-skills/manifest.tsv`；verification: `fish scripts/cash-skills/tests/skill-checks.fish tdd-discipline`；regression: `fish scripts/cash-skills/tests/skill-checks.fish`；success: primary target輸出`PASS: tdd-discipline`且以exit 0結束；red: 斷言更新後、SKILL.md修改前執行primary target，因兩個cash-apply變體缺少change-level解析與非法值警告文字而具名失敗

- [x] 1.2 對應requirement `cash-apply 品質關卡結束回報 loop-ledger 摘要`；依design C4與C5，先在`assert_tdd_discipline`新增斷言（兩個cash-apply變體含ledger摘要步驟與本次run輪數達4以上的設計劣化警訊文字、且該文字不出現在兩個cash-propose變體；正向與負向斷言依design C5鎖定摘要步驟特有文字如「apply 迴圈：本次」，不得使用`loop-ledger.tsv`／`fixed_files`等shared review-gate block既有字串，並避開`/cash-`／`$cash-` prefix-variant token），執行群組觀察具名失敗後，再於`.claude/skills/cash-apply/SKILL.md`的review-gate region之外新增品質關卡結束後的loop-ledger摘要步驟（N與M以主agent本次run寫入的round files與ledger列為權威來源、N以run內位置計數、讀`loop-ledger.tsv`核對尾端apply列、N達4以上附建議檢視design.md或以cash-ingest workflow更新設計的警訊、ledger缺檔或不一致印警告仍以run紀錄回報摘要並繼續、read-only不改任何decision），以`fish scripts/cash-skills/generate.fish`重生成`.agents`變體，最後在任何下一次Cash CLI呼叫前執行`./install-cash-skills.fish --self`重建manifest；delivery: `scripts/cash-skills/tests/skill-checks.fish`、`.claude/skills/cash-apply/SKILL.md`、`.agents/skills/cash-apply/SKILL.md`、`.cash-skills/manifest.tsv`；verification: `fish scripts/cash-skills/tests/skill-checks.fish tdd-discipline`；regression: `fish scripts/cash-skills/tests/skill-checks.fish`；success: primary target輸出`PASS: tdd-discipline`且以exit 0結束；red: 斷言更新後、SKILL.md修改前執行primary target，因兩個cash-apply變體缺少ledger摘要步驟文字而具名失敗

## 2. cash-propose 詢問與記錄

- [x] 2.1 對應requirement `cash-propose 記錄 change-level TDD 選擇`；依design C2與C5，先在`assert_tdd_discipline`新增斷言（兩個cash-propose變體含TDD詢問步驟、`tdd: true`／`tdd: false` append語意、「已有`tdd:`行跳過詢問」語意與寫入失敗停止語意），執行群組觀察具名失敗後，再修改`.claude/skills/cash-propose/SKILL.md`：在step 4（new change成功）之後、step 5（proposal撰寫）之前新增TDD詢問步驟——先檢查該change的`.openspec.yaml`，已有unindented`tdd:`行則跳過詢問（continue既有change的路徑亦同），缺行才以AskUserQuestion詢問本change是否套用TDD（兩選項附規模建議、工具不可用時依既有fallback純文字詢問）、將答案以unindented單行append至該change的`.openspec.yaml`且不改動既有行也不修改`.cash.yaml`、append失敗報告錯誤並停止——以`fish scripts/cash-skills/generate.fish`重生成`.agents`變體，最後在任何下一次Cash CLI呼叫前執行`./install-cash-skills.fish --self`重建manifest；delivery: `scripts/cash-skills/tests/skill-checks.fish`、`.claude/skills/cash-propose/SKILL.md`、`.agents/skills/cash-propose/SKILL.md`、`.cash-skills/manifest.tsv`；verification: `fish scripts/cash-skills/tests/skill-checks.fish tdd-discipline`；regression: `fish scripts/cash-skills/tests/skill-checks.fish`；success: primary target輸出`PASS: tdd-discipline`且以exit 0結束；red: 斷言更新後、SKILL.md修改前執行primary target，因兩個cash-propose變體缺少TDD詢問與記錄步驟文字而具名失敗

## 3. 欄位 dogfood 與收尾驗證

- [x] 3.1 對應requirement `cash-apply 以條件式 canonical TDD discipline 驅動 task loop`的change-level欄位契約；依design C1，將`tdd: true`以unindented單行append至`openspec/changes/per-change-tdd-override/.openspec.yaml`（本change為行為變更、規模中等，套用TDD），並確認CLI對追加行容忍：validate無新findings、status與preflight正常；delivery: `openspec/changes/per-change-tdd-override/.openspec.yaml`；verification: `.cash-skills/bin/cash validate per-change-tdd-override`；regression: `.cash-skills/bin/cash status --change per-change-tdd-override --json`與`.cash-skills/bin/cash instructions apply --change per-change-tdd-override --json`皆以exit 0回傳合法JSON；success: primary target回報validation通過且無`artifact_missing`或`heading_missing` finding；red: N/A，屬remaining-task的metadata append，CLI容忍性由primary與regression targets直接驗證，無可先失敗的目標行為測試邊界

- [x] 3.2 對應requirement `cash-apply 以條件式 canonical TDD discipline 驅動 task loop`的manifest同步收尾；依design C5，tasks 1.1／1.2／2.1已各自於SKILL.md修改後立即重簽manifest，本task執行最終一次`./install-cash-skills.fish --self`確認manifest與全部skill檔案一致且重簽冪等；delivery: `.cash-skills/manifest.tsv`；verification: `.cash-skills/bin/cash list --json`；regression: 再次執行`./install-cash-skills.fish --self`並以manual assertion確認第二次執行後`git diff --stat .cash-skills/manifest.tsv`無進一步變動（冪等）；success: primary target以exit 0回傳合法JSON且未以`bootstrap_invalid`或manifest fail-closed失敗；red: N/A，屬remaining-task的產物重簽，不修改production behavior

- [x] 3.3 對應requirement `cash-apply 以條件式 canonical TDD discipline 驅動 task loop`的全量回歸；執行完整測試套件確認本change未破壞既有skill與CLI契約；delivery: 無新增檔案（驗證性task，change-scoped diff為空）；verification: `fish scripts/cash-skills/tests/skill-checks.fish`；regression: `fish scripts/cash-cli/tests/cli-checks.fish`；success: primary target全部群組回報PASS且以exit 0結束；red: N/A，屬remaining-task的全量驗證，不修改任何檔案
