## 1. 測試先行（TDD）

- [x] 1.1 在 scripts/spectra-plus/tests/generator-checks.fish 比照既有 `<!-- MECHANICAL-SELF-CHECK -->` 斷言，為四個生成的 plus skill 檔各新增 `<!-- GRADER-IMMUTABILITY -->` 與 `<!-- LOOP-LEDGER-STEP -->` 的 assert_contains 斷言（涵蓋 spec「Review loop grader immutability」與「Review loop ledger output」的 sentinel 場景）。驗證：在模板尚未修改前執行 scripts/spectra-plus/tests/generator-checks.fish，最先遇到的新斷言失敗（紅燈）；既有斷言以修改前的 HEAD 執行一次確認全綠作為基準。

## 2. 共享 review-loop 模板

- [x] 2.1 在 scripts/spectra-plus/template/review-loop-block.md 的 Fix actions 段落新增 `<!-- GRADER-IMMUTABILITY -->` 區塊，交付 spec「Review loop grader immutability」的契約：loop 進行中主 agent（含 fix action 與 mechanical self-check 修復）禁改保護路徑集（依 design.md「Decision 1: GRADER-IMMUTABILITY 規則以路徑集加範圍例外定義」逐檔列舉，含 openspec/specs/ 下的 master spec 檔案）、「明文列入」判定規則（專案根相對路徑逐字出現；目錄視同其下所有檔案；模板檔視同其重生成產物）、進行中 loop 依啟動時規則版本執行、任何 signal 的 `check` 欄位一律禁改不受範圍例外影響、「未修復：裁判面保護」的 `## Fix Actions` 記錄義務與 finding 續存規則、以及完成摘要列出全部輪次裁判保護記錄的義務。此區塊同時落實 delta 對母 spec「spectra-propose-plus quality gate」與「spectra-apply-plus quality gate」MODIFIED 的優先序句與「fixes the … findings … except any finding withheld under the grader-immutability rule」例外場景。驗證：content review 模板區塊逐項對照 delta spec 的五個 scenario 與兩個 MODIFIED 場景。
- [x] 2.2 在 review-loop-block.md 新增 `<!-- LOOP-LEDGER-STEP -->` 區塊，交付 spec「Review loop ledger output」的契約：依 design.md「Decision 2: LOOP-LEDGER-STEP 在每輪 round file 定稿後追加一行 TSV」定義的確定性時點（next_round 輪以 spawn 下一輪 reviewers 之前為最終錨點，fix actions、fix 後 self-check 記錄、validation 重跑修復記錄與 re-derivation 註記均須先完成；最終輪在 loop 結束、signals write step 之前）向 openspec/changes/<change>/reviews/loop-ledger.tsv 追加一行、首次建立時寫入釘死的表頭（七個欄位名依序 tab 分隔）、七欄位順序與語意（含 failure-abort 輪計數為 0、fixed_files 為相異檔案數且註記行不計）、append-only 事件日誌跨迴圈與重跑累加（`(skill, round)` 非唯一鍵）、round file 為權威、寫入失敗僅警告不中斷。驗證：content review 模板區塊逐項對照 delta spec 的六個 scenario 與 Example 表。
- [x] 2.3 改寫 review-loop-block.md 中 mechanical self-check 的「Signal-derived checks」項目為兩層規則，交付 spec「Deterministic signal-derived self-checks」的契約：帶 `check` 欄位的 open signal 一律（不經 relevance 篩選）從專案根目錄把值作為 `sh -c` 的單一參數執行、exit 0 為通過、exit 1 且實例位於本 change 的 artifacts 或修改檔案內為須在 spawn reviewers 前修復的失敗、exit 1 但實例為既有問題或修復落在範圍外／裁判保護路徑內則不修復並記錄「範圍外 check 失敗」註記且將結果納入該輪 reviewers context 後照常 spawn（不死鎖）、其他 exit code 為執行錯誤退回 best-effort 並記錄退回註記（各註記行與「None; pass condition met.」共存、不計入 fixed_files）、無 `check` 欄位者行為完全不變（依 design.md「Decision 3: signal frontmatter 新增選填 check 欄位，self-check 確定性執行」）。驗證：content review 模板文字逐項對照 delta spec 的四個 scenario。
- [x] 2.4 修訂 review-loop-block.md 的 `<!-- SIGNALS-WRITE-STEP -->` 區塊，交付 spec「Signal status lifecycle is human-maintained」在寫入面的契約：Signal file schema 敘述行提及選填 `check` 欄位，且 on-match update-in-place 規則明文要求不增改刪 `check`、既有人工撰寫的 `check` 保留逐字節不動、新建 signal 不得含自動鑄造的 `check`。驗證：content review 模板區塊對照 delta spec「Automated writer never authors a check command」scenario 的全部 THEN/AND 條目。

## 3. Signals 文件

- [x] 3.1 [P] 更新 openspec/signals/README.md，交付 spec「Signals directory README contract」、「Signals shared layer location and file schema」與「Signal status lifecycle is human-maintained」的文件契約：記載選填 `check` frontmatter 欄位的單行 shell 命令形式、值作為 `sh -c` 單一參數的執行形式、exit code 慣例（0 = anti-pattern 不存在、1 = 存在、其他 = 執行錯誤）、`check` 為人工撰寫且自動化寫入者（含 signals write step 與 fix action）不得增改刪、撰寫規則（唯讀、快速、離線、非互動；偵測結果只以 0 或 1 回報、可預見的執行錯誤須以其他 exit code 浮現不得盲目反轉折疊、YAML 引號與 `#` 截斷陷阱），且既有七個必要欄位、slug 規則與人工 status 維護說明維持不變。驗證：content review README 對照 delta spec 的 README scenario 全部 THEN/AND 條目與 lifecycle 新 scenario。

## 4. 重生成與驗證

- [x] 4.1 收斂 scripts/spectra-plus/rules.yaml 中 Codex variant 的既有 `/spectra-` substitution，使其只匹配 backtick command 形式 `` `/spectra-``，避免新 protected path literals 被誤改；接著執行 scripts/spectra-plus/generate.fish 全量重生成，交付契約：.claude/skills/spectra-propose-plus/SKILL.md、.claude/skills/spectra-apply-plus/SKILL.md、.agents/skills/spectra-propose-plus/SKILL.md、.agents/skills/spectra-apply-plus/SKILL.md 四檔各包含 `<!-- GRADER-IMMUTABILITY -->` 與 `<!-- LOOP-LEDGER-STEP -->` sentinel 及 literal protected path set。驗證：scripts/spectra-plus/tests/generator-checks.fish 全數通過（含 1.1 新斷言轉綠、literal protected path 斷言、corrupted `$` path 反向斷言），且連續執行兩次重生成後以 git diff 確認產物 byte-identical（冪等性契約不破壞）。
- [x] 4.2 執行 spectra validate "add-review-loop-discipline"，交付契約：change 的全部 artifacts 通過驗證無錯誤。驗證：命令 exit 0 且輸出無 error。
