## 1. 測試先行（TDD red）

- [x] 1.1 更新 scripts/spectra-plus/tests/generator-checks.fish：移除六條與舊輪型推導及 re-derivation 綁定的斷言（'the next round is `micro` if and only if the current round is `full`, no Critical findings survive, and every surviving Warning has `layer == text`'、"MUST NOT be followed by another micro round"、'Re-derivation MUST only change `micro` to `full`'、"cannot determine whether a post-decision modification only synchronizes text" 附近斷言、re-derivation note 斷言、"(or, for apply-plus, any implementation-file modification)" 斷言），檢查 'surviving Critical'/'surviving Warning' 兩條字串斷言並按 blocking 措辭更新，並更新 scripts/spectra-plus/tests/generator-checks.fish 與 scripts/spectra-plus/tests/repair-all-checks.fish 兩檔內的 plus_version 為 "1.5.0"、plus_updated 為實作日（repair-all-checks.fish 以共用變數 plus_version/plus_updated 供 frontmatter 斷言與 staleness fixture 取用，更新變數即可）；同步檢查「Round-1 claim verification」斷言並改為 run 首輪措辭；新增斷言涵蓋「Graded convergence and micro-verification round」新規則：run 內位置推導（第 4 輪 checkpoint 唯一 full 複掃）、disposition 與 blocking finding 定義、cumulative blocking set、連續 micro 合法。行為：測試對現行生成輸出轉紅、對新輸出轉綠。驗證：fish scripts/spectra-plus/tests/generator-checks.fish 於模板改動前失敗於新斷言。
- [x] 1.2 [P] 在 generator-checks.fish 新增斷言涵蓋新增機制字串：「Accepted-risks ledger」context 附帶與 finding 級匹配降分、「Fix-loop design circuit breaker」needs-design note、「Abort triage」三桶分流與 seeded re-run 首輪 cumulative set pass 條件與續號、per-member resolved/unresolved 判定（V、checkpoint A/B 與 seeded re-run 首輪）與判定分歧任一 unresolved 即保留、new 標記的 fix-touched location 更正義務、不回升規則的 fix-introduced 例外、round file 不可回改、摘錄 fallback 註記、全保護短路（含 fix 階段 aborted 覆寫、spawn 前短路輪 round_type: full 且不 spawn reviewer 的 carve-out）、「Apply-plus introduced-by evidence」introduced_by 降分、「Review round action obligation」無動作輪禁止（blocking 成員動作限三種）、「Round file output contract」Rating 的 blocking/non-blocking 計數與 Decision record requirements 的 blocking 版 pass 禁令、「Propose-plus impact granularity advisory」僅出現在 propose-plus 輸出且 apply-plus 輸出不含。行為：斷言鎖定新模板內容的關鍵句。驗證：同 1.1 測試指令。

## 2. 模板實作：review-loop-block.md

- [x] 2.1 依 design「反轉輪型推導：micro 為預設、full 只在 round 1 與 round 4 checkpoint」改寫 scripts/spectra-plus/template/review-loop-block.md 的 Round limit and pass condition 與 Fresh sub-agent calls 段落：run 首輪之後預設 micro（Reviewer V）、第 4 輪未 pass 時強制 full、刪除「micro 不得連續」與 behavior 觸發的 re-derivation 規則，並同步改寫模板 GRADER-IMMUTABILITY 段、LOOP-LEDGER-STEP 段與 Fresh sub-agent calls 段殘留的 re-derivation／決策推導引用句（對應 MODIFIED「Review loop grader immutability」「Review loop ledger output」「Fresh sub-agent per round」——決策推導改引 cumulative blocking set、Reviewer V 改為 delta verification），並將「Round-1 claim verification (Reviewer A)」措辭改為 run 首輪（re-run 首輪同樣執行 design claim 驗證）。行為：生成的 SKILL.md 依 run 內位置機械推導輪型且無懸空 re-derivation 引用。驗證：generator-checks.fish 輪型斷言轉綠且全檔 grep 無 re-derivation 字樣。
- [x] 2.2 依 design「Delta pass 語意與 blocking set 定義」在模板加入 disposition 欄位（`unresolved-prior`/`fix-introduced`/`new`，同檔＋同機制匹配、行號 advisory；re-report 即證明 fix 無效、new 桶排除匹配前輪 blocking finding 者；dedupe 分歧取 blocking 值；主 agent 更正留痕且 blocking→non-blocking 更正列入完成輸出）、cumulative blocking set 規則（出口僅「下一輪驗證解決」與「同意接受」，出口 (a) 移除留痕且 V 與第 4 輪 A/B 均回報 per-member resolved/unresolved 判定；全裁判面保護且無同意出口即短路 abort，fix 階段才成立時當輪直接記 aborted；已完成輪次 round file 不可回改；re-run 首輪採 seeded cumulative set pass 條件；critical_gap 重定義為 cumulative set 含 Critical；摘錄 fallback 使用時留一行註記）與 non-blocking triage note 規則（首輪之後只有 blocking finding 擋 pass；non-blocking 記入 `## Fix Actions` triage note、進 signals、完成輸出醒目列出且 Critical 附後續 change 建議；最近狀態為分流者不回升），首輪之後每輪 reviewer context 必附全部 prior round files（超量改附每輪摘錄）與當前 cumulative blocking set 成員清單，主 agent 對 new 標記負「檢查 location 是否被本 loop——seeded re-run 含前 run——fix actions 改過」的更正義務、不回升規則含 fix-introduced 證據例外、prior-triage re-report 的動作為指向原 note 的交叉引用 note、判定分歧任一 unresolved 即保留；同步改寫 Rating／ledger criticals 與 warnings 為 post-filter cumulative blocking set 計數、Rating 增記分流數、Decision record requirements 的 pass 禁令改 blocking 版（對應 MODIFIED「Round file output contract」），Round file language 的 keep-verbatim 清單加入 `disposition` 三值。行為：pass 判定 = cumulative blocking set 為空。驗證：generator-checks.fish blocking／disposition／Rating 斷言轉綠。
- [x] 2.3 [P] 依 design「Accepted-risks ledger 採 finding 級匹配」在模板加入 accepted-risks 規則：openspec/changes/<change>/reviews/accepted-risks.md 的條目格式（severity、location、機制、理由、日期）、增修刪均需使用者明確同意且 loop 中不得以 fix action 編輯、每輪 reviewer context 必附、同 location＋同機制的 finding 於 confidence filter 一律 ≤ 25（行號 advisory）、僅同 subsystem 不降分、每次降分記錄於 `## Fix Actions` 且完成輸出列出、寫入失敗警告不中斷；並於 Confidence scoring rubric 段補「accepted-risks 與 introduced_by 降分優先於 direct artifact violation 必為 100 不變式」句（對應 MODIFIED「Confidence-scored findings and filter」）。驗證：generator-checks.fish accepted-risks 斷言轉綠。
- [x] 2.4 [P] 依 design「設計級斷路器走 aborted，不新增 decision 值」在模板 Fix actions 段加入 Fix-loop design circuit breaker 規則（限 apply-plus）：修復需引入 design.md 未定義的同步原語／身份世代型別／狀態機時不得實作，記 needs-design note、該輪 decision: aborted、導向 /spectra-ingest；明文排除 propose-plus（於自身 design.md 補定義為正常修復）。驗證：generator-checks.fish 斷路器斷言轉綠。
- [x] 2.5 [P] 依 design「Abort 強制 triage 三桶分流」在模板加入 Abort triage 規則：round cap、斷路器或全裁判面保護短路造成的 aborted 必須將未解 findings 分流三桶並同時記錄於最終 round file `## Fix Actions` 與完成輸出（桶 1＝每個未經同意接受的 cumulative blocking set 成員，留本 change／桶 2＝從未 blocking 的新發現寫 signals 且 Critical 建議後續 change／桶 3＝取捨經同意進 accepted-risks），不得建議原樣重跑；取捨桶無法取得同意時退回桶 1（仍屬本 change 義務、seed 進 re-run）並註記待同意，桶 2 絕對不收曾 blocking 的 finding；re-run round file 自最後既有編號續號不覆寫、首輪 full 且 context 必附前 run 全部 round files（或摘錄）、cumulative blocking set 以前 run bucket-1 findings 為種子、首輪 reviewer 同樣回報 per-member 判定、cap 與輪型以 re-run 自身位置起算；seeded set 全為裁判面保護且無同意出口時於 spawn 前短路——寫一個續號 round file（無 reviewer findings、decision: aborted、含 triage）＋一列 ledger，完成輸出導向取得同意或經 /spectra-ingest 擴充 structured scope；sub-agent 失敗 abort 與 proposal-level scope-error abort 豁免（後者預期重新提案而非 re-run）。驗證：generator-checks.fish triage 斷言轉綠。
- [x] 2.6 [P] 依 design「introduced_by 證據硬規則（僅 apply-plus）」在模板 Reviewer output requirements 與 Confidence filter 段加入 Apply-plus introduced-by evidence 規則：apply-plus Reviewer B 的 Critical/Warning 必附 introduced_by（change diff 位置、一或多筆 fix action、或某輪 fix actions 全集以涵蓋交互回歸），缺失或無法驗證者主 agent 一律 ≤ 25 且降分留痕於 `## Fix Actions` 並列入完成輸出；propose-plus 不適用；Round file language 的 keep-verbatim 清單加入 `introduced_by`。驗證：generator-checks.fish introduced_by 斷言轉綠。
- [x] 2.7 [P] 依 design「無動作輪禁止」在模板 Fix actions 段加入 Review round action obligation 規則：decision: next_round 時每個 surviving finding 與每個計入決策的 cumulative set 成員（含 carryover）必須有動作記錄——blocking finding／成員限三種（修復記錄、裁判面保護 note、經同意的 accepted-risks 條目；對 blocking 成員記 triage note 無效且不改變身分）、non-blocking（new）finding 的動作即其分流 note、僅匹配前輪分流 note 的 re-report 之動作為指向原 note 的一行交叉引用 note（非重複 note、不產生新 signal）、needs-design note 依斷路器強制 aborted 與 next_round 互斥——否則不得 spawn 下一輪 reviewer。驗證：generator-checks.fish 無動作輪斷言轉綠。

## 3. 新模板與掛載

- [x] 3.1 依 design「Impact 粒度提醒（propose-plus，資訊性）」新增 scripts/spectra-plus/template/impact-granularity-block.md 並在 scripts/spectra-plus/rules.yaml 的 spectra-propose-plus transformations 掛載於 proposal 撰寫步驟之後：Impact affected-code 路徑條目（Modified＋New＋Removed，排除 (none) 佔位行，目錄計 1 為下界）> 15 時輸出含條目數與按 capability 拆分建議的資訊性警告，≤ 15 靜默，不阻斷、不要求確認（Propose-plus impact granularity advisory）。行為：僅 propose-plus 生成輸出含此段。驗證：generator-checks.fish granularity 斷言轉綠。
- [x] 3.2 依 design「版本與再生成」更新 scripts/spectra-plus/rules.yaml：兩個 skill 的 spectraPlusVersion bump 至 1.5.0、spectraPlusUpdated 更新為實作日。行為：再生成的四份 SKILL.md frontmatter 含 spectraPlusVersion: 1.5.0。驗證：grep spectraPlusVersion 於四份生成 SKILL.md 均為 1.5.0，且 generator-checks.fish 版本斷言（更新後的 plus_version/plus_updated）通過。

## 4. 再生成與整體驗證

- [x] 4.1 執行 fish scripts/spectra-plus/generate.fish 再生成 .claude/skills/spectra-propose-plus/SKILL.md、.claude/skills/spectra-apply-plus/SKILL.md、.agents/skills/spectra-propose-plus/SKILL.md、.agents/skills/spectra-apply-plus/SKILL.md。行為：四份輸出含全部新規則且帶生成標記。驗證：fish scripts/spectra-plus/tests/generator-checks.fish 全綠（TDD green，涵蓋 spectra-propose-plus quality gate 與 spectra-apply-plus quality gate 的新 pass 條件字串）。
- [x] 4.2 執行其餘測試套件確認無回歸：fish scripts/spectra-plus/tests/repair-all-checks.fish（含 task 1.1 更新後的版本變數）、auto-restore-checks.fish、installer-commit-guard-checks.fish。行為：既有 repair／restore／guard 契約不受影響。驗證：全部測試指令 exit 0。
- [x] 4.3 執行 spectra validate "converge-plus-review-loop" 確認 artifacts 一致。驗證：validate 通過無錯誤。
