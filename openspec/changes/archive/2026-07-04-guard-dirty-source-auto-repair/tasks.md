## 1. Dirty source detection contract

- [x] 1.1 實作 clean source fixture test harness：`repair-all-checks.fish` 的 normal repair-all cases 必須從 clean temporary source fixture 執行 installer/entrypoint，dirty-source cases 則只在該 fixture 中刻意製造 dirty state，避免目前開發中的 working tree 讓 normal repair tests 被 dirty guard 提早 skip；驗證：fixture clean 時既有 repair-all normal/stale/current tests 仍執行 target current-state 或 repair assertions，而不是只看 dirty-source skip。
- [x] 1.2 實作 `Guard repair-all with source-sensitive dirty detection`：在 `install-spectra-plus.fish` 建立 source-sensitive dirty 檢查，讓 `Repair-all protects registered targets from dirty source checkout` 可針對包含 `install-spectra-plus.fish` 的 git work tree root 比對 `install-spectra-plus.fish`、`scripts/spectra-plus/**`、`.agents/skills/spectra-*/**`、`.claude/skills/spectra-*/**` 的任何 matching `git status --porcelain` entry，且 path matching 必須使用目錄邊界而非 substring；驗證：以 fixture repo 或 test setup 建立 path matrix，至少覆蓋 `install-spectra-plus.fish`、`scripts/spectra-plus/rules.yaml` 或 template、`.agents/skills/spectra-commit/SKILL.md`、`.claude/skills/spectra-commit/SKILL.md`、`.agents/skills/spectra-propose/SKILL.md`、`.claude/skills/spectra-apply/SKILL.md`、一個非 output 直接輸入的 Spectra skill 如 `.agents/skills/spectra-ask/SKILL.md` 或 `.claude/skills/spectra-audit/SKILL.md`、nested untracked file、deleted source-sensitive file、renamed source-sensitive file、copied/typechange source-sensitive entry，以及 unmerged porcelain parser fixture，確認 helper 回報 dirty，且 unrelated prefix path/rename 不回報 dirty。
- [x] 1.3 實作 `Guard repair-all with source-sensitive dirty detection` 的 `--repair-all` 入口行為：source-sensitive paths dirty 時，`install-spectra-plus.fish --repair-all` 必須在 dependency preflight、lock/throttle state、local plus metadata validation、registry target processing、讀取 target current state、或呼叫 per-target repair 前輸出 dirty-source skip、exit 0、且不修改任何 registered target；驗證：`fish scripts/spectra-plus/tests/repair-all-checks.fish` 覆蓋 dirty source normal repair-all、dirty invalid `scripts/spectra-plus/rules.yaml` precedence、invalid registry target skip precedence、dirty-source skip 訊息至少命名一個 offending path、既有 throttle state 不產生 throttle decision 且不被改寫、lock/throttle 不寫入，並用 checksum/fingerprint 證明 target plus outputs 與 `spectra-commit` guard 不變。
- [x] 1.4 實作 `Auto-restore stripped commit guard source from git HEAD` 與 dirty-source guard 的 precedence：automatic `--repair-all` 在 source `spectra-commit/SKILL.md` dirty 且 guard stripped 時必須 dirty-source skip，不得 restore from `HEAD`；manual `--target` / `--target --dry-run` 仍保留既有 auto-restore 行為；驗證：`fish scripts/spectra-plus/tests/repair-all-checks.fish` 覆蓋 dirty stripped source skip、manual `--target` restore、以及 manual `--target --dry-run` restore report。

## 2. Manual and dry-run behavior

- [x] 2.1 實作 `Make dry-run report the same guard decision`：source-sensitive paths dirty 時，`install-spectra-plus.fish --repair-all --dry-run` 必須輸出 dirty-source skip，且不得輸出 `+ repair target ...`；驗證：`fish scripts/spectra-plus/tests/repair-all-checks.fish` 覆蓋 dry-run dirty source，確認 stdout/stderr 與 target fingerprint。
- [x] 2.2 實作 `Keep manual single-target repair explicit and permissive`：`install-spectra-plus.fish --target <project>` 不受 dirty-source automatic repair guard 阻擋，仍走既有單一 target 產生與驗證流程；驗證：`fish scripts/spectra-plus/tests/repair-all-checks.fish` 在 source-sensitive dirty fixture 下執行 `--target`，確認 command 不因 dirty-source guard 失敗。
- [x] 2.3 實作 unrelated dirty file boundary：dirty files 僅落在 source-sensitive path set 外時，`install-spectra-plus.fish --repair-all` 必須依既有 current-state/repair 行為處理 registered target，且不得輸出 dirty-source skip；驗證：`fish scripts/spectra-plus/tests/repair-all-checks.fish` 建立 unrelated dirty file fixture，確認 registered target 仍被 skipped/current 或 repaired。
- [x] 2.4 實作 staged-only dirty boundary：source-sensitive path 只有 staged/index change 且沒有 unstaged working tree change 時，`install-spectra-plus.fish --repair-all` 仍必須輸出 dirty-source skip 並不修改 registered target；驗證：`fish scripts/spectra-plus/tests/repair-all-checks.fish` 建立 staged modified 與 staged added fixture，確認 skip output 與 target fingerprint。
- [x] 2.5 實作 LaunchAgent entrypoint ordering：`scripts/spectra-plus/repair-all.fish` 必須是 thin handoff wrapper，在呼叫 `install-spectra-plus.fish --repair-all` 前不得做 registry read、lock/throttle state、target current-state check、或非必要 dependency preflight；dirty source-sensitive path 存在且 entrypoint PATH 找不到 `yq` 時，仍必須輸出 dirty-source skip、exit 0，且不修改 target；驗證：`fish scripts/spectra-plus/tests/repair-all-checks.fish` 用 restricted PATH 執行 entrypoint fixture，確認 missing `yq` 不遮蔽 dirty-source skip。
- [x] 2.6 實作 parseable dirty entrypoint handoff：`scripts/spectra-plus/repair-all.fish` 本身是 parseable dirty WIP 時，LaunchAgent entrypoint 仍必須 hand off 到 installer dirty-source guard，輸出 dirty-source skip、exit 0，且不讀 registry、不建立 lock、不寫 throttle、不修改 target；驗證：`fish scripts/spectra-plus/tests/repair-all-checks.fish` 建立 parseable dirty `repair-all.fish` fixture，搭配 registered target fingerprint，確認 entrypoint 沒有 target/lock/throttle side effect。
- [x] 2.7 實作 source clean state unavailable fail-closed：automatic `--repair-all` 無法確認 source-sensitive paths clean 時必須輸出 source clean state unavailable skip、exit 0、且不處理 registry targets；驗證：`fish scripts/spectra-plus/tests/repair-all-checks.fish` 建立非 git source fixture 或 git status failure fixture，確認不執行 target current/repair 且 target fingerprint 不變。

## 3. Final validation

- [x] 3.1 更新 Spectra artifacts 與完整驗證：確保 tasks 覆蓋 `Repair-all protects registered targets from dirty source checkout` 的所有 scenarios，並明確連結 `Repair all registered plus skill targets`、`LaunchAgent-based automatic plus skill repair`、`Repair checks plus metadata freshness` 的本次變更範圍，且 design headings 都有 backing task；驗證：`spectra validate guard-dirty-source-auto-repair` 通過。
