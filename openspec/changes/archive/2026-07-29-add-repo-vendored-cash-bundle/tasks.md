## 1. 先建立失敗中的契約測試

- [x] [P] 1.1 在 `scripts/cash-skills/tests/test_bundle_version_history.py` 新增 portable manifest canonicality／version binding、exact launcher transition與永久 workspace-lock immutability案例；先執行 `python3 scripts/cash-skills/tests/test_bundle_version_history.py`，確認新案例在 production變更前因缺少契約而失敗。
- [x] [P] 1.2 在 `scripts/cash-skills/tests/test_installer_runtime.py` 新增manifest-presence優先序與stale receipt cutover、portable help／concurrent generation、可被一般import接受但payload不同的 `.pyc`必須被忽略、manifest pre-open FIFO／broken symlink／directory／hard link及executable manifest、schema與受治理inventory、runtime `.py` missing／extra、umask `022`／`002`及bytecode完整filesystem零寫入、全planned paths Git exclude、receiptless exact／partial／force adoption、vendor fresh明確回報`Result: update`、current／update、receipt conversion、direct-mode拒絕manifest、skipped-version launcher migration、schema v2／v3與pre／post-cutover fault recovery案例；執行 `python3 scripts/cash-skills/tests/test_installer_runtime.py`並確認新案例先失敗。
- [x] [P] 1.3 在 `scripts/cash-skills/tests/test_init_receipt.py` 新增 `init_vendored_bundle`拒絕、manifest初次preflight為missing但在exclusive lock前由併發publisher建立時必須post-lock重驗並零寫入拒絕、canonical source `--self`同步manifest並清除receipt residue、dry-run／current零寫入及既有receipt-only mode回歸案例；執行 `python3 scripts/cash-skills/tests/test_init_receipt.py`並確認新案例先失敗。
- [x] [P] 1.4 在 `scripts/cash-skills/tests/skill-checks.fish` 新增雙guidance分流、`CASH-SKILLS.md`與 `CASH-INIT-RECEIPT.md`的vendored／receipt-only操作說明、`init_vendored_bundle`、manifest不進receipt inventory與雙variant完整性斷言；執行 `fish scripts/cash-skills/tests/skill-checks.fish`並確認新斷言先失敗。

## 2. 建立版本與 bootstrap migration guard

- [x] 2.1 在改動任何 launcher、runtime、skill或portable manifest production bytes之前，將 `cash-skills.version`從目前 HEAD值調升為嚴格較大的版本；執行 `git diff -- cash-skills.version .cash-skills/bin/cash .cash-skills/lib/cash_cli/installer.py .agents/skills/ .claude/skills/`，確認此時受 guard的production檔案尚未改動。
- [x] 2.2 完成 `scripts/cash-skills/tests/test_bundle_version_history.py` 的 history gate：`.cash-workspace.lock`維持first-introduction bytes／Git mode、launcher只接受 `APPROVED_LAUNCHER_TRANSITIONS`的 exact old/new digest與版本、`.cash-skills/manifest.tsv`納入 version-bound inventory且逐 byte符合canonical serializer；執行 `python3 -m unittest scripts.cash-skills.tests.test_bundle_version_history.BundleHistoryTests`，確認所有isolated history fixtures通過。

## 3. 實作 portable launcher gate

- [x] 3.1 依 `Portable manifest 啟動信任模式`實作並同步 `Project-local Cash CLI runtime`與 `Cash workflow command surface`：修改 `.cash-skills/bin/cash`，以manifest path的pre-open no-follow `lstat` presence實作互斥信任分支，讓manifest優先且invalid manifest不fallback，保留manifest缺失時的既有receipt行為；要求manifest自身為non-executable Git logical class `100644`，executable manifest以`manifest_invalid` fail closed；新增canonical parser、Git logical mode、受治理的expected inventory與現地runtime missing／extra比較、FD/path identity、digest與runtime-generation驗證並保留verified runtime bytes；在managed import前設定 `sys.dont_write_bytecode = True`，再以 `FileFinder`／`VerifiedSourceLoader`限制project-local library root及verified `cash_cli` package directories，讓 `get_code`直接compile保留bytes而不讀寫cache或影響stdlib。執行launcher targeted tests，確認stale receipt cutover、portable help／concurrency、malicious-but-valid `.pyc`不被執行、unsafe或executable manifest fail closed、兩種umask與完整filesystem零寫入、所有manifest drift fail closed。

## 4. 實作 vendored publisher 與受控 migration

- [x] 4.1 依 `受控 launcher bootstrap migration`與 `Bundle 安裝與 runtime receipt`修改 `.cash-skills/lib/cash_cli/installer.py`：新增 `PORTABLE_MANIFEST_PATH`、`portable_manifest_bytes`、`parse_portable_manifest`與 `(old_digest, new_digest, introduced_version)` schema的 `APPROVED_LAUNCHER_TRANSITIONS`，以本變更前HEAD launcher digest、完成後launcher digest及本次bundle version登錄exact transition，並允許new bytes未變時由更高source version跨版升級；將 `InstallTransaction`升級為可恢復schema v2且以schema v3專用 `launcher`、dynamic `receipt`、manifest cutover與 `receipt_delete` operations實作rollback receipt identity rebind、pre-cutover rollback及 `portable_cutover` roll-forward cleanup，workspace lock inode永不替換。執行journal advance、launcher replace、dynamic receipt、manifest與cleanup前後fault tests，逐一斷言完整舊或新gate可用。
- [x] 4.2 依 `Repo-vendored Cash bundle 發佈`同步 `Installer 與 legacy cleanup filesystem boundaries`及 `手動的 cash 專案 registry`：在 `.cash-skills/lib/cash_cli/installer.py` 以 `install_vendored_target`新增互斥的 `--vendor <project>`、`--vendor --dry-run`與 `--vendor --force` workflow，涵蓋Git top-level、全部planned paths tracked-or-not-excluded preflight與final revalidation、receipt／state／`__pycache__`排除、fresh classification於real run回報`Result: update`、receiptless exact adoption／partial conflict／force replaceable邊界、current／update、manifest cutover後receipt cleanup、safe stale receipt cleanup，以及 direct／register／batch拒絕vendored target；執行 `python3 scripts/cash-skills/tests/test_installer_runtime.py`並修正到全數通過。
- [x] 4.3 依 `Target-local receipt 初始化`修改 `.cash-skills/lib/cash_cli/installer.py`，讓source-only `--self`於同一exclusive lock transaction同步canonical manifest並清除source receipt residue，讓一般vendored target的 `--init-receipt`以 `init_vendored_bundle`零寫入失敗且canonical source仍優先回報 `init_source_repo`；非source target在preflight確認manifest缺失後，取得exclusive stable lock時須再次no-follow重驗manifest仍缺失，若併發publisher已建立任一present shape則在chmod、inventory open或receipt publication前失敗；執行 `python3 scripts/cash-skills/tests/test_init_receipt.py`並修正到全數通過。

## 5. 發佈 canonical manifest 與使用者文件

- [x] 5.1 以完成後的 deterministic serializer產生 `.cash-skills/manifest.tsv`，確認它的version、runtime generation、ordered records、digest與Git logical mode對應現行source inventory，且 `.cash-skills/receipt.tsv` record集合完全不變；執行 `python3 scripts/cash-skills/tests/test_bundle_version_history.py`並修正到isolated fixtures與現行repository history檢核全數通過。
- [x] [P] 5.2 依 `Repo-vendored Cash 團隊交付與指引`同步修改 `AGENTS.md`與 `CLAUDE.md`的Cash-owned guidance：manifest存在時clone／pull後直接使用且舊receipt不遮蔽；只有不含manifest的receipt-based target遇到 `bootstrap_invalid`才使用完整 `--init-receipt`指令，invalid manifest不得fallback。
- [x] [P] 5.3 依 `現行文件反映 cash 所有權與清理`修改 `CASH-SKILLS.md`，將 `--vendor`列為團隊建議路徑，記載dry-run／force、portable信任邊界、Git logical mode、manifest-presence優先序、planned path excludes、receiptless認養、更新與轉換、launcher rebind migration、維護者commit責任及既有receipt workflows。
- [x] [P] 5.4 修改 `CASH-INIT-RECEIPT.md`，將它定位為receipt-only direct／legacy target指南，移除所有clone都要init、launcher無條件receipt gate與launcher bytes不變的舊敘述，補上portable分流、`init_vendored_bundle`、source `--self`與installer mode矩陣。
- [x] 5.5 完成 `scripts/cash-skills/tests/skill-checks.fish`的新契約斷言並執行 `fish scripts/cash-skills/tests/skill-checks.fish`，確認雙variant、兩份文件與receipt inventory規則全數通過。

## 6. 整合驗證

- [x] 6.1 依序執行 `python3 scripts/cash-skills/tests/test_bundle_version_history.py`、`python3 scripts/cash-skills/tests/test_installer_runtime.py`、`python3 scripts/cash-skills/tests/test_init_receipt.py`與 `fish scripts/cash-skills/tests/skill-checks.fish`，確認所有contract tests（包含現行repository history檢核）通過且沒有temporary／journal檔殘留。
- [x] 6.2 在temporary Git worktrees以umask `022`與`002`各建立fresh vendored clone，分別在無cache與預置可被一般import接受但payload不同的 `.pyc`狀態，驗證receipt缺失或stale時均從verified source執行 `.cash-skills/bin/cash list --json`與help，且包含ignored files、directories及mtime的完整filesystem snapshot前後相同；再驗證unsafe／invalid／executable manifest不fallback、fresh publication回報`Result: update`、全planned path Git excludes、receiptless adoption、valid receipt conversion、skipped-version／unapproved launcher drift，以及pre／post-cutover publication fault recovery的預期gate／exit／classification。
- [x] 6.3 執行 `.cash-skills/bin/cash validate add-repo-vendored-cash-bundle`與 `git diff --check`，逐項驗證所有`MODIFIED Requirements`標題逐byte存在於 `openspec/specs/cash-cli/spec.md`或 `openspec/specs/cash-skill-workflows/spec.md`，並對照 `proposal.md` Impact清單，確認production、tests、docs與spec artifacts皆在宣告範圍內且沒有未申報檔案。
