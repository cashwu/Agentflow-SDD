## 1. 先寫驗證（TDD 紅燈）

- [ ] 1.1 [P] 在 `scripts/cash-cli/tests/test_creation_task_lifecycle.py` 新增 `touched record` 的十一個案例，全部經由 `tasks.execute()` 驅動。先新增一個與 `scripts/cash-cli/tests/test_runtime_and_errors.py` 的 `enter_workspace` 同形的 helper：建立 temp git root、寫入 `.cash.yaml` 與 `openspec/config.yaml`、建立 0644 的 `.cash-workspace.lock`、`os.chdir` 進 root 並以 `self.addCleanup(os.chdir, previous)` 還原（`cli-checks.fish` 以單一 process 執行 `python3 -m unittest discover`，chdir 是跨檔共享的全域狀態，還原不可省）。案例依 design.md C4 的 (a) 至 (k)：(a) 先 `touched ensure` 再 record 既存 signal 檔且全程無 snapshot；(b) 與 per-task 條目並存、record 的路徑之一與該條目重疊、頂層 `files` 為排序聯集且無重複項；(c) 重複 record 時 bytes／`st_ino`／`st_mtime_ns` 不變；(d) 無 `--path` 與 `--path` 缺值為 `invalid_arguments`；(e) 絕對路徑、含 `..`、`.git/` 前綴、`.cash-skills/state/` 前綴四者為 `touched_invalid`；(f) `openspec/changes/` 前綴與 `.cash-skills/receipt.tsv` 為 `touched_invalid`，而 `.cash-skills/lib/` 之下的既存一般檔案記錄成功；(g) 不存在路徑與目錄路徑為 `touched_invalid`；(h) change 目錄不存在為 `change_not_found`；(i) 未先 ensure 為 `touched_invalid`；(j) record 前後 snapshot 與 `tasks.md` 的 bytes 皆不變；(k) 單次呼叫混合多個合法路徑與一個非法路徑時為 `touched_invalid` 且 state 檔 bytes 逐字不變。失敗案例皆須斷言零寫入。驗證目標：執行 `scripts/cash-cli/tests/cli-checks.fish creation-task-lifecycle`，案例 (d) 因現行 `execute()` 對 `touched record ...` 已一律回 `invalid_arguments` 而**通過**，其餘十個案例失敗；既有測試維持通過。
- [ ] 1.2 [P] 在 `scripts/cash-skills/tests/skill-checks.fish` 新增斷言：在既有 codex consumer matrix 的字面句清單加入 `"$cash_cli" touched record "<change-name>" --path <path>`；對四個 review-loop SKILL 檔（`.agents` 與 `.claude` 的 `cash-propose`、`cash-apply`）逐檔斷言 `record every signal file this step created or updated`、`record the files that round's Fix Actions modified outside the change directory`、`carry this warning into the final completion output`、`"$cash_cli" touched ensure "<change-name>"`、`"$cash_cli" touched record "<change-name>" --path <path>`、`rebuild the receipt before the next cash command`；對兩個 `cash-commit/SKILL.md` 斷言 `### Review Loop Outputs`、`a shared signal file cannot be split by change`、`only when that other change directory still exists`。驗證目標：執行 `scripts/cash-skills/tests/skill-checks.fish codex-command-matrix`，因六個 SKILL 檔皆尚未修改而失敗。

## 2. CLI verb 實作

- [ ] 2.1 在 `.cash-skills/lib/cash_cli/commands/tasks.py` 的 `execute()` 內、`command == "touched"` 分支新增 `record` 子命令，依 design.md C1 實作：驗證 change 目錄存在（否則 `change_not_found`）、要求 state 檔已存在（否則 `touched_invalid` 且不執行 legacy import）、每個 `--path` 依序通過 `_safe_source_path`／前綴拒絕（`openspec/changes/` 與 `.cash-skills/receipt.tsv`，與 `_IGNORED_PREFIXES` 對齊；MUST NOT 拒絕整個 `.cash-skills/` 前綴）／`workspace.path_kind()` 為 `file` 三段驗證、任一失敗即零寫入、通過者併入 `review-loop` 保留條目並重算頂層 `files` 聯集、保留 `legacy_import`、合併結果未變時不寫入。不提供 `--json`。既有 `ensure` 分支行為不得改變；不得改動 `mark_task_done`、`start_in_progress`、`ensure_touched`、`load_or_import_touched`、`git_fingerprints`、`_safe_source_path`，也不得改動 `.cash-skills/lib/cash_cli/main.py`。**本任務的收尾步驟**：在標記本任務完成之前，MUST 先於 project root 執行 `./install-cash-skills.fish --self` 重建 `.cash-skills/receipt.tsv`。理由：`tasks.py` 為 replaceable runtime record，launcher 在任何 dispatch 之前逐檔比對 receipt 記錄的 runtime digest；未重建時所有 `.cash-skills/bin/cash` 指令會以 `receipt_invalid: runtime record drift` 失敗，而標記本任務完成所用的 `task done` 正是下一個要執行的 CLI 指令。`.cash-skills/receipt.tsv` 為 gitignore 檔，不列入 affected code。驗證目標：執行 `scripts/cash-cli/tests/cli-checks.fish creation-task-lifecycle` 全部通過，且重建後 `.cash-skills/bin/cash validate --all` exit 0。

## 3. review loop 呼叫時點

- [ ] 3.1 在 `.claude/skills/cash-propose/SKILL.md` 依 design.md C2 新增兩條規則：在 `<!-- SIGNALS-WRITE-STEP -->` 區塊的 failure handling 之前，新增「先 `"$cash_cli" touched ensure "<change-name>"`，再以該步驟實際建立或更新的每個 signal 檔路徑呼叫 `"$cash_cli" touched record "<change-name>" --path <path>`」，逐字包含 `record every signal file this step created or updated`，並同樣套用 design.md C2 的呼叫協定（濾除、濾除後為空則不呼叫且不產生警告、整批失敗逐路徑重試）；在 `**Fix actions**` 區塊新增「若該輪 `## Fix Actions` 記錄了位於 `openspec/changes/<change>/` 之外的已修改檔案，先執行 `"$cash_cli" touched ensure "<change-name>"` 再以那些路徑呼叫 `touched record`」（在 `cash-propose` 中此處是全流程第一次接觸 touched state，漏掉 ensure 會使該次 record 失敗），並依 design.md C2 寫明：該輪改到 `.cash-skills/` 之下 runtime 檔時先重建 receipt（逐字包含 `rebuild the receipt before the next cash command`）、路徑須為 project-root-relative 且先濾除 `openspec/changes/` 之下者、整批失敗時逐路徑重試取最大合法子集，逐字包含 `record the files that round's Fix Actions modified outside the change directory`。兩處皆須寫明失敗處理：印警告並繼續、不使 workflow 失敗、不改變任何 round file 的 `decision`、警告列出未能記錄的路徑與 `error.code`，並逐字包含 `carry this warning into the final completion output`。驗證目標：對該單一檔案逐一執行 `rg -F` 命中上述三個字面句與 `"$cash_cli" touched ensure "<change-name>"`、`"$cash_cli" touched record "<change-name>" --path <path>`、`rebuild the receipt before the next cash command`，且 `scripts/cash-skills/tests/skill-checks.fish grader-immutability` 通過（確認改動位於 grader 區塊之外）。
- [ ] 3.2 將 3.1 的同一段內容寫入 `.claude/skills/cash-apply/SKILL.md` 的對應位置，文字與 3.1 逐字相同。驗證目標：對該單一檔案逐一執行 `rg -F` 命中三個字面句與 `"$cash_cli" touched ensure "<change-name>"`、`"$cash_cli" touched record "<change-name>" --path <path>`、`rebuild the receipt before the next cash command`，且 `scripts/cash-skills/tests/skill-checks.fish grader-immutability` 通過。
- [ ] 3.3 把 3.1 與 3.2 的內容分別寫入 `.agents/skills/cash-propose/SKILL.md` 與 `.agents/skills/cash-apply/SKILL.md`（invocation 前綴為 `$cash-`），使各自與 `.claude` 變體在前綴正規化後的差異維持既有狀態。驗證目標：對兩個檔案各自逐一執行 `rg -F` 命中三個字面句與 `"$cash_cli" touched ensure "<change-name>"`、`"$cash_cli" touched record "<change-name>" --path <path>`、`rebuild the receipt before the next cash command`；執行 `scripts/cash-skills/tests/skill-checks.fish variant-parity` 通過；並以 `git diff --exit-code scripts/cash-skills/variant-parity/cash-propose.diff` 確認該 manifest 未被修改。

## 4. cash-commit 呈現與裁決

- [ ] 4.1 在 `.claude/skills/cash-commit/SKILL.md` 依 design.md C3 修改：step 2 解析 touched 後把 `touched` 陣列拆為 per-task 條目與 `review-loop` 保留條目；step 5 新增獨立的 `### Review Loop Outputs` 區段並指明其屬於提交集合；對保留條目中位於 `openspec/signals/` 的檔案讀 frontmatter `links`，只有當某條 link 指向的其他 change 其 `openspec/changes/<other>/` 或 `openspec/changes/.parked/<other>/` 目前仍存在時才標示為共用，逐字包含 `only when that other change directory still exists`；共用檔以 AskUserQuestion 要求使用者選擇整檔納入或整檔排除，逐字包含 `a shared signal file cannot be split by change`，並明寫不得靜默納入或靜默排除；選擇排除時該檔改列於 `### Unrelated Changes (not included)` 並註明使用者裁決排除（此為 step 4 既有判定的明確例外，須寫成正面規則）、最終輸出提醒該檔仍為 dirty；step 6 的 `Commit as shown` 說明明示涵蓋 `### Review Loop Outputs`，且 `Include all dirty files` 與 `Customize` 的加回路徑若涵蓋已裁決排除的共用檔須先告知會推翻裁決並取得確認、`Customize` 移除已裁決納入的共用檔時一併移入 Unrelated 並沿用註記；step 6a 的 Updated Commit Plan 區段清單同樣保留 `### Review Loop Outputs`；step 2a 的來源允許清單中位於 `openspec/signals/` 的路徑套用同一組判定與裁決。既有的 step 2a 偵測條件與 step 3／4／5／6／7 其餘規則不得改變。驗證目標：對該單一檔案逐一執行 `rg -F` 命中 `### Review Loop Outputs`、`a shared signal file cannot be split by change`、`only when that other change directory still exists`。
- [ ] 4.2 將 4.1 的同一段內容寫入 `.agents/skills/cash-commit/SKILL.md`，使兩個變體在 invocation 前綴正規化後逐字相同。驗證目標：對該單一檔案逐一執行 `rg -F` 命中三個字面句，並執行 `scripts/cash-skills/tests/skill-checks.fish variant-parity` 與 `scripts/cash-skills/tests/skill-checks.fish codex-command-matrix`，兩者皆通過。

## 5. 判準實測與 Bundle 關卡

- [ ] 5.1 對現有 `openspec/signals/*.md` 實跑 design.md 決策九的共用判準，確認以本 workspace 現況誤判數為零：統計帶 `links` 的 signal 檔總數、其中 links 跨越多個 change 者、以及跨越多個**仍進行中（active 或 parked）之 change** 者。驗證目標：誤判數為 0，即不存在僅因指向已封存 change 的歷史 link 而被判為共用的 signal 檔（真陽性計數與 signal 總數皆會隨迴圈變動，不得作為 pass 條件），（本檢查驗證的是判準被正確執行而非資料性質——決策九的判準內含存在性條件，正確實作時該數必然為 0，退回只比名稱的實作才會失敗；可行的觀察步驟是先以只比名稱的判準取得候選集合，再逐一確認加上存在性條件後全部落空），並把實測數字記入 `openspec/changes/track-review-loop-outputs-in-allowlist/implementation-notes.md`。
- [ ] 5.2 讀取當下的 `cash-skills.version` 與 `git show HEAD:cash-skills.version`，將 `cash-skills.version` 寫為嚴格大於 HEAD 值的下一個 minor 版本。不得寫死常數而不比對 HEAD：本 workspace 另有進行中的 change 也宣告要提升該檔，先落地時 HEAD 值會改變。驗證目標：執行 `python3 scripts/cash-skills/tests/test_bundle_version_history.py` 通過。
- [ ] 5.3 依 design.md C4 的 receipt 常規再執行一次 `./install-cash-skills.fish --self`，使 receipt 反映版本提升後的狀態。此為本變更的最後一次重建，MUST 在任務 5.2 的版本提升之後、任務 5.4 的回歸執行之前。驗證目標：`.cash-skills/bin/cash validate --all` exit 0。
- [ ] 5.4 執行 `scripts/cash-cli/tests/cli-checks.fish all`、`scripts/cash-skills/tests/skill-checks.fish all`，以及 `.cash-skills/bin/cash validate track-review-loop-outputs-in-allowlist`，全部通過。
